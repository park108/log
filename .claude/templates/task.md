# 작업 지시서: {{제목}}

> **Task ID**: TSK-YYYYMMDD-NN
> **관련 명세**: `specs/spec/{{YYYYMMDD-slug}}.md` §{{섹션번호}}
> **관련 요구사항**: `specs/requirements/{{YYYYMMDD-slug}}.md` (REQ-..., FR-.., NFR-..)
> **작성일**: YYYY-MM-DD / **작성자**: -
> **상태**: Todo | In Progress | Review | Done | Blocked
> **담당**: -
> **규모**: XS (≤30m) / S (≤2h) / M (≤half day) / L (≤1day)
> **분할 기준**: 1 작업 = 1 PR(또는 1 커밋) 크기. L 초과 시 반드시 분할.

---

## 1. 목적 (Objective)
이 작업이 달성할 한 가지 결과를 한 문장으로. 명세서의 어느 부분을 구현하는지 명시.

> 예: spec §4의 3번 파일 변경(`markdownParser.js` 탐지 pass에 depth 계산 추가)을 완료한다.

## 2. 선행 조건 (Prerequisites)
- [ ] 선행 작업: TSK-... (있다면)
- [ ] 필요한 브랜치/환경: ...
- [ ] 설치/설정: ...
- [ ] 읽고 이해해야 하는 코드/문서: `path:line`, `specs/spec/...`

## 3. 변경 명세 (Changes)

### 3.1 파일 단위 변경
| # | 파일 | 동작 | 핵심 변경 | 라인(예상) |
|---|------|------|-----------|------------|
| 1 | `src/.../foo.jsx` | 수정 | ... | L10–L25 |
| 2 | `src/.../foo.test.jsx` | 추가 | 신규 케이스 N개 | - |
| 3 | `...` | 삭제 | ... | - |

> 동작: 추가 / 수정 / 삭제 / 이동(rename)

### 3.2 공개 인터페이스 변경
외부에서 관찰 가능한 시그니처/props/라우트 변화가 있으면만 기술. 없으면 "없음".
- 신규: `fnName(args) → return`
- 변경: before → after
- 제거:

### 3.3 컨벤션 전제
코드베이스의 기존 규칙 중 이 작업에 영향을 주는 것만.
- 들여쓰기/네이밍/파일 구성:
- 테스트 스타일:
- 기타:

## 4. 구현 단계 (Steps)
순서대로, 각 단계는 독립 검증 가능해야 한다. 3~7단계 권장.

1. ...
2. ...
3. ...

> Tip: 각 단계가 끝난 직후 실행할 단위 명령을 함께 적어두면 리뷰가 쉬워진다. (예: `npm test -- path/to/file`)

## 5. 검증 (Verification)

### 5.1 자동 검증
- [ ] 단위 테스트: `npx vitest run path/to/file.test.jsx`
- [ ] 전체 테스트: `npm test`
- [ ] 린트: `npm run lint`
- [ ] 빌드: `npm run build`

### 5.2 수동 검증 (UI/통합 필요 시)
- [ ] 시나리오 1: [동작] → 기대 결과: [결과]
- [ ] 시나리오 2: ...

### 5.3 회귀 기준
- 수정 없이 통과해야 하는 기존 테스트:
- 영향 가능 영역:

## 6. 완료 기준 (Definition of Done)
- [ ] 5.1/5.2 모든 체크 통과
- [ ] 명세서 §{{…}} 의 해당 항목 체크 완료
- [ ] 관련 요구사항의 FR/NFR/US 수용 기준 충족
- [ ] 코드 리뷰 승인
- [ ] 커밋 메시지에 `TSK-…`, 관련 `SPEC`, `REQ` 참조 포함

## 7. 범위 밖 (Out-of-Scope)
이 작업에서 다루지 않는 것. 혼동 여지 있는 인접 작업을 명시해 스코프 크립을 방지.
- ...
- 후속 작업으로 넘길 것: TSK-...

## 8. 롤백 (Rollback)
운영 영향 있는 변경일 때만.
- 트리거:
- 절차: (예) `git revert <sha>` → 배포
- 데이터 복구:

## 9. 리스크 / 메모
- 구현 중 발견할 가능성 있는 이슈:
- 결정이 필요한 미결 질문:
- 후속 과제 후보:

---

## 체크리스트 (빠른 요약)
- [ ] 선행 조건 충족
- [ ] 변경 파일 모두 반영
- [ ] 신규/수정 테스트 작성
- [ ] 자동/수동 검증 통과
- [ ] DoD 모든 항목 체크
- [ ] 명세서/요구사항 문서에 역참조 기록(완료 표시)
