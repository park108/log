# blocked: TSK-20251231-09 — 픽스처 사유서 (처방 소비 판정의 우변)

> **판정**: 확장 선언이 불허인데 유일한 해결 파일이 변경 범위 밖.
> **격리 시각**: 픽스처 (시점 무의미)

## 요지

발행 시 확인 범위가 좁아 결합을 보지 못했다. 아래 처방은 재발행 문서의 스코프로 흘러야 한다.

## 재발행 시 필요한 것 (planner)

`## 변경 범위` 에 아래 두 경로를 함께 담으면 그대로 착지한다.

- `package.json`
- `.husky/pre-commit`

자기 참조 파일명(`TSK-20251231-09-fixture-prior-axis_reason.md`)은 저장소 실재 필터에서
자동으로 빠진다 — 디렉터리 없는 이름은 어떤 경로로도 열리지 않는다.

## 수행하지 않은 것

- 격리 트리에 대한 쓰기·이동. 이 게이트는 그 트리를 읽기만 한다.
