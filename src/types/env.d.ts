/// <reference types="vite/client" />
// Project-specific env extension (currently none). Ensures @/types/env resolves.
// Future VITE_* vars should extend `ImportMetaEnv` here, e.g.:
//   interface ImportMetaEnv { readonly VITE_API_BASE: string }
export {};
