# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T(session+3)Z
# Session: 1 promotion (geturl-audit → blue) + 2 blocked moves (fake-timer-residual, react-19-findby) + 0 new carves.
#   승격 (green → blue):
#     - common/geturl-render-path-test-audit-spec.md (hash b3137923 → 10cff9b3 — inspector flip, DoD 전원 `[x]` + 변경 이력 TSK-20260421-44 / 96c37df 박제 확인) → mv specs/30.spec/green/common/ → specs/30.spec/blue/common/. ledger 제거.
#   blocked (green → 50.blocked/spec):
#     - deps/fake-timer-teardown-residual-cleanup-spec.md (hash 불변 97cedb50, stale 2 → 3 ≥3cycle). TSK-42 는 done/c18132d 이나 inspector 가 DoD flip 유보 ("marker 유지" 결정, a51d35c). planner writer 영역 내 해소 불가 → `50.blocked/spec/` + `_reason.md`.
#     - deps/react-19-findby-timing-stabilization-spec.md (hash 불변 b5d9e93e, stale 2 → 3 ≥3cycle). TSK-43 은 `50.blocked/task/` 격리 유지 (RULE-05 수동 개입 관할, planner 재carve 금지). → `50.blocked/spec/` + `_reason.md`.
#   pending green (승격 조건 미충족, 진행 중):
#     - common/test-isolation-shuffle-safety-spec.md (hash 불변 bd27261f): TSK-20260421-45 ready 큐 생존. 수용 기준 10+건 `[ ]` unchecked. stale 1 → 2.
# ready 큐: TSK-20260421-45 (count=1, TASK_READY_MAX=10 여유). TSK-44 done 이동 (96c37df), TSK-43 blocked 이동.
# carve 계보 (본 세션 추가 없음):
#   - TSK-45 는 직전 세션 carve 이후 developer 미소진 상태 (depends_on: TSK-42 는 이미 done 으로 완료 선결 만족).
#   - TSK-43 은 blocked 격리 → planner 재carve 금지. 대응 spec 도 동일 축으로 blocked 진입 (본 세션).
# grep dry-run (본 세션 신규 carve 0건이므로 실측 없음). blocked 이동 전 파이프라인 정체 해소 우선.
# 총 grep dry-run 0/0 match (본 세션 carve 0 — 승격 1 + blocked 2 수행).
30.spec/green/common/test-isolation-shuffle-safety-spec.md bd27261fdbd3c24b4ad4db3ac13e37413841ebfdad575f31138773dcfd06b4bc 2
