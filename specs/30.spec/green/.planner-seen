# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (3rd cycle, post TSK-49 operator cleanup)
# Session: 0 promotions + 2 new carves (TSK-51, TSK-52) + 0 blocked.
#   컨텍스트: RULE-03 선결 통과 — pause 없음, input 1 (shuffle-cold-start), ready 0 → 2 (TASK_READY_MAX=10 여유 10 → 8).
#     실측 hash (이번 사이클):
#       - shuffle-cold-start: 60be59b… 불변 (ledger stale 1 → 0; carve 로 spec 소비 progress 발생).
#     blocked 디렉토리 관측: 50.blocked/task/ 빈상태, 50.blocked/spec/ 빈상태.
#       → 직전 사이클 이동 file-split 및 이전 기록 TSK-43/49/50, toaster spec 등 **모두 운영자 정리 완료** (RULE-05).
#       → 이전 ledger 의 "TSK-49 blocked 상태" 전제가 해소 → spec FR-15 + §동작 13 "신규 Task ID 발행" 경로 활성.
#     LogItem.test.jsx 실측: getByTestId 4건 (:205/:260/:316/:377) 유지, findByTestId/beforeAll 0건 — TSK-49 원본이 원복되었거나 미merge 상태로 Layer 1 도 미수행 (spec baseline 과 일치).
#   승격 평가 (4조건; shuffle-cold-start):
#     - WIP=0, To-Be=0, 변경이력 박제 OK.
#     - unchecked 25건 중 5건 `[deferred: TSK-49 blocked]`, 4건 `[pending: Layer 2 신규 TSK-ID]`, 수용 기준 16건 미태깅 → 승격 불가.
#     - 주: `[deferred]` → `[pending: TSK-20260421-51/52]` 전환 (FR-14) 은 inspector 영역 (planner 가 spec 수정 금지 — RULE-01 writer 매트릭스).
#   carve (원자 태스크, 1 PR 크기, 독립 롤백):
#     - TSK-20260421-51 — Layer 1 FR-02 옵션 A (sync→async query 전환, `src/Log/LogItem.test.jsx` 단일 파일, depends_on=[]).
#     - TSK-20260421-52 — Layer 2 FR-11/13 B1 (warm-up beforeAll empty render prime, `src/Log/LogItem.test.jsx` 단일 파일, depends_on=[TSK-20260421-51]).
#     FR-06 (msw.test.js sibling-it) / FR-07 (File.test.jsx cache race) / B2~B5 fallback 은 Layer 2 해소 결과 의존 — 본 사이클 carve 범위 외 (scope creep 방지).
#   grep dry-run: 7/7 match
#     G1 getByTestId("delete-button") src/Log/LogItem.test.jsx → 4 hits (spec baseline 4 일치).
#     G2 findByTestId("delete-button") src/Log/LogItem.test.jsx → 0 hits (spec baseline 0 일치).
#     G3 await screen.findByTestId("delete-button") src/Log/LogItem.test.jsx → 0 hits (TSK-51 DoD 변환 baseline 일치).
#     G4 beforeAll src/Log/LogItem.test.jsx → 0 hits (spec baseline 0 일치).
#     G5 warm-up src/Log/LogItem.test.jsx → 0 hits (TSK-52 DoD baseline 일치).
#     G6 await Promise.resolve src/Log/hooks/useDeleteLog.js → 0 hits (B5 미적용 baseline 일치).
#     G7 server.listen src/test-utils/msw.js → 3 hits (B3 미적용 baseline 일치).
#   blocked: 없음.
# ledger entries:
common/test-isolation-shuffle-safety-cold-start-spec.md 60be59b96e3eba221c65332860b0d993b60bbb4d0d667f414817c60c30e0c858 0
# 아카이브 (이동됨 — 운영자 정리 완료):
# components/file-test-multi-scenario-split-spec.md 6488570be7d4c6586fdcc912c53a5cfeaf8020ff5add354ba34ea760df280197 (operator cleanup)
# components/toaster-conditional-mount-or-helper-spec.md 23bfc9260ee53be3135e18f2826876ab761b28db2a7ad37ee3a7696d62aa6ba5 (operator cleanup)
# ready 큐: (count=2, TASK_READY_MAX=10 여유 8).
# next cycle:
#   - TSK-51/52 developer 소비 대기 (15m 주기).
#   - shuffle-cold-start spec 은 inspector 가 `[deferred]` → `[pending: TSK-51/52]` 전환 (FR-14 이행) 후 planner 재평가.
#     TSK-51/52 done 시 FR-02/08/11/13 수용 기준 체크 가능 → 승격 경로 열림.
#   - FR-06/07, B2~B5 후속 carve 는 Layer 2 해소 결과 의존 — 조기 carve 금지 (scope creep 방지).
