# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (7th cycle, no-op — inspector marker-sync 반영, ledger 정정)
# Session: 0 promotions + 0 new carves + 0 blocked.
#   RULE-03 선결 통과: pause lock 없음, input green=2 (shuffle-cold-start, msw-test-sibling), ready=1 (TSK-20260421-55) → TASK_READY_MAX=10 여유 9.
#     HEAD=1d89844 (inspector marker-sync 7).
#     실측 hash (HEAD=1d89844, 직전 ledger 와 **다름** → inspector marker-sync 편집 반영 → stale 리셋):
#       - shuffle-cold-start: bea5206339c77c7dbebb414531177e06dc17da7f32174cb8485ba3663f5b0730 (직전 21543b77 → 변동; marker sync L97/99~102 반영).
#       - msw-test-sibling-it-shuffle-race: 4b22c6b0ad4cc4db8fceca6eab4d280ee2d98e4942aace8612ed464c49d06fa0 (직전 e956a403 → 변동; marker sync 변경 이력 한 줄 추가).
#     blocked 디렉토리 관측:
#       - 50.blocked/spec/ 비어있음.
#       - 50.blocked/task/ 비어있음.
#       - 50.blocked/req/ 비어있음.
#     ready 큐 현황 (실측):
#       - 20260421-msw-test-sibling-it-shuffle-race.md (TSK-20260421-55, REQ-011).
#       (주: 직전 ledger 주석은 TSK-20260421-53 도 ready 로 적었으나 실측상 부재.
#        TSK-53 은 두 번 blocked 되어 followup 으로 discovery 재처리 대기 —
#        `10.followups/20260421-0125-tsk-53-...` + `20260421-0201-tsk-53-...` 2건 생존.
#        planner 는 blocked→ready 재발행 권한 없음 (RULE-05 수동 해제).)
#   승격 평가 (4조건):
#     - shuffle-cold-start: unchecked `- [ ]` = 23. `[WIP]` = 0, `^#+ .*To-Be` = 0.
#       real unchecked (deferred/pending 제외) = 17건 (L103/106/108~117/119~122).
#       → 승격 불가.
#     - msw-test-sibling-it-shuffle-race: unchecked = 17. `[WIP]` = 0, `^#+ .*To-Be` = 0.
#       real unchecked = 15건 (L47~50/53~57/59~65 — FR-06 Should 1건 pending 제외).
#       → 승격 불가. TSK-55 developer 소비 대기.
#   carve 판정: 본 사이클 신규 carve 0.
#     - shuffle-cold-start 의 FR-08/11/12/13 은 TSK-53 실측 결과 의존. TSK-53 은 10.followups 로 피드백 중 (두 번째 재발행 실패) — discovery→inspector→planner 재투입 이후에만 재carve. 조기 carve 금지 (scope creep).
#     - shuffle-cold-start FR-07 (File.test.jsx cache race) 은 `[deferred: shuffle seed 0 fail 달성 후 재평가]` 태깅. TSK-53 소비 이전엔 발행 대상 아님.
#     - TSK-54 (Layer 2 B2 describe.sequential) 역시 B1/B3 실패 확정 이후에만 등장 — 현 시점 carve 금지.
#     - msw-sibling 은 TSK-55 단일 task 로 carve 완료. 추가 분할 불필요.
#   stale 카운트 (hash 변동 → 리셋):
#     - shuffle-cold-start: 1 → 0 (hash 변동 = inspector marker-sync 편집 = 활성 진행).
#     - msw-test-sibling-it-shuffle-race: 1 → 0 (hash 변동).
#   grep dry-run: 0/0 match (신규 태스크 생성 0건).
#   blocked: 없음 (신규 격리 0).
# ledger entries:
common/test-isolation-shuffle-safety-cold-start-spec.md bea5206339c77c7dbebb414531177e06dc17da7f32174cb8485ba3663f5b0730 0
common/msw-test-sibling-it-shuffle-race-dedicated-spec.md 4b22c6b0ad4cc4db8fceca6eab4d280ee2d98e4942aace8612ed464c49d06fa0 0
# ready 큐: (count=1, TASK_READY_MAX=10 여유 9).
# next cycle:
#   - TSK-55 developer 소비 → code ok → inspector drift reconcile 로 FR-01~05 ack → 다음 planner 사이클 4조건 재평가.
#   - TSK-53 은 discovery/inspector 재처리 필요 (followup 2건). planner 는 대기만.
#   - TSK-53 해소 이후 shuffle-cold-start 의 FR-08/11/12/13 drift reconcile 시점에 후속 carve 가능 (TSK-54 / FR-07).
#   - 본 사이클 writer 영역 변경: `.planner-seen` 만 (RULE-01 green 본문 편집 권한 없음; 40.task/ 신규 0건).
