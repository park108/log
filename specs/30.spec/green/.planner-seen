# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (24th cycle) — 1 promotion + 0 carves + 0 blocked.
# Session summary:
#   HEAD=08bfe52 (`feat: isAdmin() cognito:groups 이관 + env 경로 제거 + test describe 교체`).
#   본 세션 입력 (green): 3 spec files —
#     (1) `common/a11y.md` (hash=2830b440.. 동일; 이전 23rd cycle 과 동일 내용 — inspector M4/M5/M10/audit 미편집).
#         unchecked 정합: §테스트 현황 3건 [ ] (L155 LogItemInfo M4 row, L159 M5 실측, L160 M10 결정, L161 audit), §수용 기준 5건 [ ] (FR-02/03/05/06/07/08) 잔존.
#     (2) `components/common.md` (hash=0fd6bc7b..; 이전 dba93002.. → 변경 — inspector 가 TSK-20260421-74 @08bfe52 Phase 1 ack 반영 편집함).
#         unchecked=0 / WIP=0 / To-Be=0 / 변경 이력 TSK-20260421-74 @08bfe52 박제 → 4조건 통과, **promote O**.
#     (3) `foundation/regression-gate.md` (hash=005052a5.. 동일; 이전 23rd cycle 과 동일 내용 — TSK-20260421-75 developer 실현 + inspector ack 미도착).
#         unchecked=2 (§테스트 현황 FR-01 typecheck / FR-02 coverage thresholds) 잔존.
#
#   RULE-03 선결 점검:
#     (1) pause lock 없음 (`.claude/locks/` 부재).
#     (2) `.claude/pipeline.json` 부재 → `TASK_READY_MAX=10` 기본.
#     (3) 입력 큐: 3 green spec files.
#     (4) 하류 임계치: `40.task/` = 1 (TSK-75 잔존) → 본 세션 +0 = 1, 여유 9 (OK).
#     (5) 처리 진입.
#
#   세션 시작 시 git 상태 점검 (RULE-02):
#     working tree 에 모두 타 에이전트 writer 영역:
#       - `.github/workflows/ci.yml` (modified, developer 영역, TSK-75 진행 중 가설)
#       - `vite.config.js` (modified, developer 영역, TSK-75 진행 중 가설)
#       - `specs/30.spec/green/components/common.md` (modified, inspector 영역)
#     staged 0 항목 → unstage 불요. planner 는 위 3 파일에 손대지 않으며 본 세션 자기 영역 변경만 단일 커밋.
#     세션 시작 HEAD=08bfe52, 본 세션 동안 다른 에이전트 커밋 유입 가능성 — 커밋 직전 재점검 원칙 유지.
#
#   각 spec 승격 판정 (4조건: unchecked=0, WIP=0, To-Be=0, 변경 이력 TSK/커밋 박제):
#     - `common/a11y.md` (hash=2830b440..., stale 0 → **1**):
#         unchecked=10 (LogItemInfo row M4 L155 + M5 L159 + M10 L160 + audit L161 + §수용 기준 5건 L167-172),
#         WIP=0, To-Be=0,
#         변경 이력 4행 박제 ✓
#         → **미승격**. M4/M5/M10/audit 4 분기 결정·실측 선행 필요 (inspector writer 영역). planner 단독 carve 부적절.
#         stale=1 유지 (2회 이상 정체 시 blocked 후보).
#     - `components/common.md` (hash=0fd6bc7b..., stale 0 재시작 — inspector 편집):
#         unchecked=0, WIP=0, To-Be=0,
#         변경 이력 10행 박제 (최초 + REQ-022/025/032/038 흡수 + TSK-61/62/66/67/68/74 ack) ✓
#         → **승격 O**. mv `green/components/common.md` → `blue/components/common.md` 수행 (기존 blue 교체).
#         잔존 [~] 감사성 marker 3건 (구 REQ-022 FR-02/03 + REQ-032 FR-08 계약 교체 감사 기록) — 승격 판정 제외 (4조건은 `- [ ]` unchecked 한정, `[~]` 는 감사성).
#     - `foundation/regression-gate.md` (hash=005052a5..., stale 0 → **1**):
#         unchecked=2 (§테스트 현황 FR-01/FR-02 L54/L55 — TSK-20260421-75 실현 + inspector ack 대기),
#         WIP=0, To-Be=0,
#         변경 이력 1행 박제 ✓
#         → **미승격**. TSK-75 는 40.task/ 잔존 상태이며 working tree 상 ci.yml/vite.config.js 수정 존재 (developer 진행 중 가설). 본 세션 종료 시점 커밋 미도착 → inspector ack 도착 후 재평가.
#         stale=1 유지.
#
#   carve 발생: **0 건**.
#     - a11y.md: M4/M5/M10/audit 결정 분기 및 실측 선행 필요 (inspector 편집 영역) — carve 유보.
#     - regression-gate.md: TSK-75 이미 40.task/ 큐에 발행 상태 (23rd cycle carve). 중복 carve 금지. developer/inspector 완결 대기.
#     - components/common.md: 승격 완료 — carve 불요.
#
#   Task ID 중복 검증: 본 세션 신규 carve 0 건 → 중복 검증 대상 없음.
#
#   grep dry-run: 본 세션 신규 task 0 건 → grep dry-run 대상 없음. (이전 23rd cycle TSK-74/75 gate 는 각 task 의 §스코프 규칙에 이미 박제; TSK-74 는 @08bfe52 ack 완료로 gate 수렴 확인.)
#
#   승격 mv: **1건** — `30.spec/green/components/common.md` → `30.spec/blue/components/common.md` (기존 blue 교체).
#   blocked mv: **0건**.
#
#   RULE-04/RULE-06 점검: 본 세션 신규 task 발행 0 → grep 게이트 검증 대상 없음.
#   RULE-07 정합:
#     - components/common.md 승격 — § admin gate 계약 (cognito:groups 매트릭스 + 4 불변식 + 운영 전제) + § auth() 쿠키 속성 계약 + § 환경 분기 계약 은 모두 평서형 시스템 불변식, 시점 비의존, 반복 검증 가능. RULE-07 양성. blue/components/common.md 교체 (REQ-022/025/032 흡수 버전 → REQ-022/025/032/038 흡수 + TSK-74 ack 버전).
#     - a11y.md 잔존 — §drift 정상화 표 + §예외 (M5/M10) enumeration + §스코프 규칙 baseline 실측 은 baseline 시점 현상 기록이나 정상화 진척도 측정 기준. RULE-07 양성.
#     - regression-gate.md 잔존 — 2 불변식 (CI typecheck step 포함 / Vitest coverage thresholds 4축 선언) 평서형, 구체 수치 박제 없음. RULE-07 양성.
#
# ledger entries (잔존 green):
# common/a11y.md  2830b440123cf36e60a1f7ef7c594eb10221b6f9a7c112331dcd2b602ac3831c  1  # 24th cycle — 동일 hash 반복 (M4/M5/M10/audit inspector 편집 미진입). stale=1. 다음 사이클에도 동일 hash 유지 시 stale=2 진입 → 50.blocked/spec/ 후보 (RULE-01/05 경로).
# foundation/regression-gate.md  005052a52faebceebce1a99b79ff15bec7b151693e840e2146af1541e1937a42  1  # 24th cycle — 동일 hash 반복 (TSK-75 developer 실현 + inspector ack 미도착). stale=1. 다음 사이클에도 동일 hash 유지 시 stale=2 진입 → 50.blocked/spec/ 후보.
#
# promoted (→ 30.spec/blue/):
# - components/common.md (hash=0fd6bc7b93550f80bcc220f28af5416a278498390d7f7561d0a7a562c3e92462; TSK-74 @08bfe52 Phase 1 ack 반영 버전 — 기존 blue (REQ-032 FR-01/03/04/06/07 버전) 교체).
#
# blocked (→ 50.blocked/spec/):
# - (없음)
#
# ready 큐: count=1 (TSK-75 잔존), TASK_READY_MAX=10 여유 9.
# next cycle (25th+):
#   - **developer (15m)**: TSK-75 완결 (working tree 의 ci.yml + vite.config.js 변경을 `feat: CI typecheck + coverage thresholds` 커밋 + push + result.md 작성 + task mv to 60.done).
#   - **inspector (30m)**:
#       (a) TSK-75 ack → `foundation/regression-gate.md` green §테스트 현황 FR-01/02 [ ] → [x]. §스코프 규칙 grep-baseline (a)(c) 현 수치 갱신 (0→1+ / 0→5).
#       (b) a11y.md 추가 편집 (M4 versions-button 실측 / M5 Edit Link 귀결 / M10 img listitem 결정 / audit 도입 유무 결정) 시 marker 플립 및 §drift 표 갱신.
#   - **planner (25th+)**:
#       (i) regression-gate.md 4조건 전원 충족 시 **승격 O**.
#       (ii) a11y.md 의 M4/M5/M10/audit 4 분기 carve — 각각 1 PR 크기 독립 task 또는 결정 task + impl task 2단 분할 후보. inspector 편집 진척도에 따라.
#       (iii) a11y.md / regression-gate.md 동일 hash 2사이클 연속 시 **50.blocked/spec/** 이동 + `_reason.md` 박제 (RULE-01/05 경로).
#   - **본 사이클 writer 영역 변경**:
#       - `30.spec/green/components/common.md` → `30.spec/blue/components/common.md` (승격 mv, 기존 blue 교체).
#       - `30.spec/green/.planner-seen` 갱신 (본 파일).
#     커밋 scope = `spec(planner):`.
