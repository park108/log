husky-pre-push-typecheck 32abb51f432766af64b7e3e317ff81d26e4a3f552f980cc929e2ce42d2df4c43
island-regression-guard 0c2adb1fb76269a077ce8c49a5ee0a9d954d6806d8c3dfbd27037465b23bb005
# 2026-05-17 planner session (3rd tick @HEAD=4c84b86) — typescript 환경 회귀 미해소 지속. tsc --noEmit rc=2 + TS2688/TS6046/TS5070 3 hit (모두 tsconfig 경로 / src/** 0 hit). 직전 tick (2nd) 의 행동지침 (typecheck-exit-zero 동일 hash b682e83 유지 시 2회차 격리) 충족 — typecheck-exit-zero.md hash b682e83a 직전과 동일 → 50.blocked/spec/foundation/ 격리 + _reason.md 박제.
# 본 세션 carve 0건. island-regression-guard (1차 노출) + husky-pre-push-typecheck (1차 노출) 둘 다 carve 유보:
#   (i) husky-pre-push-typecheck: 본 spec §동작 FR-07 명시 의존 ("파생 task 발행은 typecheck-exit-zero FR-01 수렴 후") 미충족. spec 박제 자체는 유효 (시점 분리 평서화).
#   (ii) island-regression-guard: 환경 회귀 (ESLint `@eslint/js` 모듈 ERR_MODULE_NOT_FOUND) 로 `npm run lint` baseline 재현 불가 — RULE-06 grep dry-run / game-day 검증 불가능. 8 island 0 hit static baseline 은 정합하나 task 발행 시 RULE-06 grep-baseline 실측 박제 요건 충족 불가.
# 다음 세션 행동지침:
#   (1) 운영자가 환경 회귀 (typescript@4.9.3 installed + moduleResolution Bundler 미인식 + @eslint/js 모듈 미해석 — npm install 또는 devDep 정합) 해소 → planner 재진입.
#   (2) 본 2 spec hash 가 본 세션 박제 (32abb51f / 0c2adb1f) 와 동일 유지된 채 환경 회복되면 다음 tick 에서 carve 진행.
#   (3) typecheck-exit-zero 는 RULE-05 /revisit 경로로만 복귀 — green 큐로 재진입 금지 (writer 경계). 직전 tick blocked 3건 (coverage-determinism · src-typescript-migration · tsconfig-test-ambient-globals) 은 4c84b86 revisit 으로 followup 승격 후 삭제 완료.
#   (4) 본 2 spec hash 가 본 세션 박제와 동일 유지된 채 환경 미회복 다음 tick 진입 시 정체 1회차 재카운트 (시점 정체 vs 환경 정체 분리). 2회차 진입 시 격리 후보.
# 2026-05-17 planner session (4th tick @HEAD=ffaa752) — 환경 회귀 미회복 1회차 재카운트.
#   재실행 baseline (HEAD=ffaa752 실측):
#     - tsc --noEmit: TS2688 vitest/globals + TS6046 moduleResolution + TS5070 resolveJsonModule 3 hit 잔존 (직전 tick 과 동일 — tsconfig 경로 / src/** 0 hit).
#     - npm run lint: ERR_MODULE_NOT_FOUND `@eslint/js` 잔존 (직전 tick 과 동일 — `eslint.config.js` 의 `@eslint/js` import 미해석).
#     - green spec hash 정합: husky-pre-push-typecheck 32abb51f (3rd tick 박제와 동일) + island-regression-guard 0c2adb1f (3rd tick 박제와 동일) — inspector 미터치 검증.
#     - 50.blocked/spec/ 비어있음 (직전 tick 박제 typecheck-exit-zero 는 81c08d1 chore(revisit) 1건 + 4c84b86 chore(revisit) 3건으로 followup 승격 후 삭제 완료 — 정식 RULE-05 경로 정합).
#     - 10.followups/ 비어있음 (discovery 흡수 또는 done 처리 완료).
#   본 세션 처리:
#     - carve 0건: (i) husky-pre-push-typecheck 는 spec FR-07 의존 게이트 (typecheck-exit-zero FR-01 rc=0 수렴 후 task 발행) 미충족 — spec 자체 박제 의존 작동 중 (정체 사유가 spec 평서문 자체).
#     - carve 0건: (ii) island-regression-guard 는 RULE-06 grep-baseline 실측 박제 의무 + game-day 게이트 fire/silence 픽스처 박제 의무 둘 다 `npm run lint` ERR_MODULE_NOT_FOUND 로 충족 불능.
#     - 격리 보류: 직전 tick (3rd) 행동지침 (4) 의 "환경 정체 재카운트 1회차" 본 tick 해당 — 격리 시점은 5th tick (재카운트 2회차 진입) 도달 시.
#     - 승격 0건: 두 spec 모두 §테스트 현황 + §수용 기준 unchecked `- [ ]` 다수 잔존 (FR-01~07 측정 항목 + FR-01~06 수용 항목) — RULE-01 승격 4조건 (`[WIP]` 0 + unchecked 0 + `^#+ .*To-Be` 0 + 변경 이력 박제) 중 unchecked 0 미충족. 박제 자체 정합 (시점 박제 의존 분리 평서화).
#   다음 세션 (5th tick) 행동지침:
#     (1) 환경 회귀 운영자 해소 필요 — `npm install` 후 typescript devDep 정합 + `@eslint/js` 모듈 설치/devDep 추가. 본 회귀는 planner writer 영역 밖.
#     (2) 환경 회복 + 두 spec hash 동일 유지 시: husky-pre-push-typecheck 는 FR-07 게이트 통과 (tsc rc=0 + error TS 0 hit) 후 carve 진행. island-regression-guard 는 npm run lint baseline 실측 (게이트 (c)(d) 효능 부재 negative 재검증) + game-day fixture (src/Comment/Probe.jsx 도입 시 rc ≠ 0) 픽스처 박제 검증 후 carve 진행.
#     (3) 환경 미회복 5th tick 진입 + 두 spec hash 동일 유지 시: 본 행동지침 (4)(직전 tick 박제) 의 "2회차 진입 시 격리 후보" 충족 → 50.blocked/spec/foundation/ 두 spec 격리 + _reason.md 박제 (typescript + ESLint 환경 회귀 잔존). 정식 복귀 경로는 RULE-05 /revisit.
#     (4) 본 tick 박제 hash 와 5th tick 진입 시점 hash 달라지면 (inspector 가 spec 재편집) 정체 카운트 리셋 → 환경 회복 가정 carve 절차로 재진입.
# 2026-05-17 planner session (5th tick @HEAD=946dca1) — 환경 회귀 미회복 2회차 누적 + 신규 spec 1차 노출 분리 처리.
#   재실행 baseline (HEAD=946dca1 실측):
#     - tsc --noEmit: TS2688 vitest/globals + TS6046 moduleResolution + TS5070 resolveJsonModule 3 hit 잔존 (3rd / 4th tick 박제와 동일 — tsconfig 경로 / src/** 0 hit).
#     - npm run lint: ERR_MODULE_NOT_FOUND `@eslint/js` 잔존 (3rd / 4th tick 박제와 동일).
#     - green spec hash 정합: husky-pre-push-typecheck 32abb51f (3rd/4th tick 박제와 동일) + island-regression-guard 0c2adb1f (3rd/4th tick 박제와 동일) — inspector 미터치 검증.
#     - green 신규: toolchain-version-coherence 82bf016d (5th tick 1차 노출 — 4th tick 후 inspector 가 REQ-061 흡수 신규 등록). 본 spec 은 환경 회귀 자체를 불변식으로 박제한 spec (devDep ↔ installed ↔ tsconfig enum 정합 룰).
#     - 50.blocked/spec/foundation/: 본 tick 진입 시 비어있음. 본 tick 종료 시 2건 격리 (husky-pre-push-typecheck + island-regression-guard).
#     - .inspector-seen: writer 경계 미접촉.
#   본 세션 처리:
#     - carve 0건: (i) husky-pre-push-typecheck → 본 tick 환경 미회복 + hash 동일 2회차 누적 → 50.blocked/spec/foundation/ 격리 + _reason.md 박제 (4th tick 행동지침 (3) 충족).
#     - carve 0건: (ii) island-regression-guard → 본 tick 환경 미회복 + hash 동일 2회차 누적 → 50.blocked/spec/foundation/ 격리 + _reason.md 박제 (4th tick 행동지침 (3) 충족).
#     - carve 0건: (iii) toolchain-version-coherence → 1차 노출. 본 spec 의 task 발행은 `package.json` / `node_modules` / `tsconfig.json` / `package-lock.json` 변경 — developer writer 영역 (`src/`, `10.followups/`) 밖 침투 가능성 + spec 자체가 환경 회복 의존 메타 spec (수단 중립 박제) 으로 carve 절차 적용 부적합. 1차 노출 + 정체 카운트 0 이므로 격리 부적합. 본 tick 은 carve / 격리 둘 다 보류.
#     - 승격 0건: 두 격리 spec 모두 §테스트 현황 + §수용 기준 unchecked `- [ ]` 다수 잔존 (FR-01~07 / FR-01~06 측정 항목) → RULE-01 승격 4조건 미충족 (unchecked 0 항목 위반). toolchain-version-coherence 도 §테스트 현황 + §수용 기준 unchecked 다수 (FR-01~05 + NFR-01~02) → 승격 부적격.
#   다음 세션 (6th tick) 행동지침:
#     (1) 50.blocked/spec/foundation/ 의 2건 (husky-pre-push-typecheck + island-regression-guard) 은 RULE-05 정식 복귀 경로 — /revisit 스킬이 _reason.md 판정 후 10.followups/ 승격. planner 영역 미접촉 (writer 경계).
#     (2) toolchain-version-coherence 박제 후 운영자가 §동작 1~5 정합 수렴 시 (devDep 다운그레이드 또는 installed 업그레이드 또는 lockfile 재정합 — 본 spec 수단 중립) → tsc preprocessing 카테고리 0 hit + npm run lint 정상화 동시 달성 가능.
#     (3) toolchain-version-coherence 의 task 발행은 본 spec 의 §역할 박제 ("의도적으로 하지 않는 것: 특정 메이저로의 업/다운그레이드 자체") 와 RULE-01 developer writer 영역 (`src/`, `10.followups/`) 경계의 정합 검증 후에만 가능. 다음 tick 진입 시점 hash 가 본 tick 박제 (82bf016d) 와 동일 + 환경 미회복 유지면 정체 1회차 재카운트. 동일 hash 2회차 진입 + 환경 미회복 시 격리 후보 (4th tick 의 husky/island 격리 룰과 동일 적용).
#     (4) 본 tick 진입 시점 hash 와 다음 tick 진입 시점 hash 달라지면 (inspector 가 spec 재편집) 정체 카운트 리셋.
#     (5) 환경 회복 + green spec 신규 진입 시 carve 절차 재진입 — RULE-01 Task ID 중복 검증 + RULE-06 grep-baseline 실측 박제 + grep dry-run 검증 의무.
# 2026-05-17 planner session (6th tick @HEAD=eac3549) — toolchain-version-coherence 정체 1회차 재카운트 + 환경 회귀 미회복.
#   재실행 baseline (HEAD=eac3549 실측):
#     - tsc --noEmit: rc=2 + TS6046+TS5070+TS2688 카테고리 3 hit 잔존 (3rd/4th/5th tick 박제와 동일 — tsconfig 경로 / src/** 0 hit).
#     - npm ls typescript: `invalid:` 7 hit 잔존 (msw peer 등 — 5th tick 박제와 동일 패턴).
#     - installed: `node_modules/typescript@4.9.3` 잔존 (devDep `^6.0.3` 과 메이저 격차 2 — 5th tick 동일).
#     - green spec hash 정합: toolchain-version-coherence 813b6cf3 (5th tick 박제 short hash `82bf016d` 와 prefix 차이 — git log 단일 커밋 `946dca1` 잔존 + working tree clean 으로 spec 본문 미변경 검증 완료. 5th 박제 시점의 short hash transcription 표기 차이로 해석. inspector writer 경계상 본문 비변경이 확인되었으므로 hash 동일성 정합).
#     - 50.blocked/spec/foundation/: 본 tick 진입 시 비어있음 (5th tick 격리 2건은 81c08d1 `chore(revisit): scanned=2, followup=2, deleted=2` 통해 10.followups/ 정식 승격 후 삭제 — RULE-05 정합).
#     - 10.followups/: 2건 (husky-pre-push-typecheck-from-blocked + island-regression-guard-from-blocked — discovery writer 영역, planner 미접촉).
#     - 20.req/: 1건 (20260517-island-prop-types-removal — inspector writer 영역, planner 미접촉).
#     - .inspector-seen: writer 경계 미접촉.
#   본 세션 처리:
#     - carve 0건: toolchain-version-coherence (정체 1회차 재카운트) — 본 spec 의 § 역할 박제 ("의도적으로 하지 않는 것: 특정 메이저로의 업/다운그레이드 자체") 가 수단 중립 메타 spec 성질을 명시. 본 spec 의 § 동작 1~5 정합 수렴은 (i) devDep `^6.0.3` 다운그레이드 (4.9.x lock), (ii) `node_modules/typescript` 6.x 업그레이드 (`npm install`), (iii) lockfile 재정합 (`package-lock.json` 메이저 동기화) 중 하나 — 본 spec 자체는 수단 결정 권한이 없음. 따라서 단일 PR 크기 / 독립 롤백 가능 / depends_on 명시의 원자 task 분할 결정 불능. 추가로 본 spec 의 변경 산출물은 `package.json` / `node_modules/typescript/package.json` / `tsconfig.json` / `package-lock.json` 으로 RULE-01 developer writer 영역 (`src/`, `10.followups/`) 경계 밖 침투 가능성. 5th tick 행동지침 (3) 의 "정합 검증 후에만 task 발행 가능" 게이트 미충족 — task 발행 보류.
#     - 격리 0건: 정체 1회차 누적 — 5th tick 행동지침 (3) 의 "동일 hash 2회차 진입 + 환경 미회복 시 격리 후보" 조건 미충족 (본 tick 은 1회차 재카운트). 격리 시점은 7th tick (2회차 진입) 도달 + 환경 미회복 + hash 동일 시점.
#     - 승격 0건: § 테스트 현황 12건 + § 수용 기준 5건 = unchecked `- [ ]` 17건 잔존 → RULE-01 승격 4조건 미충족 (`unchecked == 0` 위반). 박제 자체 정합 — 본 spec 은 환경 회복 의존 메타 spec 으로 unchecked 항목은 환경 회복 후 inspector 가 측정 통과 처리.
#   다음 세션 (7th tick) 행동지침:
#     (1) toolchain-version-coherence hash 가 본 tick 박제 (813b6cf3) 와 동일 + 환경 미회복 유지 시 → 정체 2회차 진입 → 50.blocked/spec/foundation/toolchain-version-coherence.md 격리 + _reason.md 박제 (수단 중립 메타 spec 의 task 발행 결정 불능 + developer writer 영역 침투 우려 + 환경 회복 의존 — 정식 복귀 경로는 RULE-05 /revisit).
#     (2) toolchain-version-coherence hash 가 달라지면 (inspector 가 § 역할 수정 또는 baseline 재실측 박제) 정체 카운트 리셋 → carve 절차 재진입 (RULE-06 grep-baseline 재실측 + 수단 결정 평서화 여부 재판정).
#     (3) 환경 회복 (`npm install` 정합 또는 devDep 다운그레이드) 시 toolchain-version-coherence 의 § 동작 3.1 (preprocessing 0 hit) 자연 수렴 — inspector 가 §테스트 현황 + §수용 기준 항목 측정 통과 처리 후 § 변경 이력 박제 → planner 다음 tick 승격 4조건 충족 시 green→blue 승격.
#     (4) 10.followups/ 의 2건 (husky/island from-blocked) 은 discovery 가 7th tick 진입 전 흡수 → 20.req/ 발행 가능. 본 흐름은 planner 영역 밖 — 모니터만.
#     (5) 새 green spec 진입 시 (예: island-prop-types-removal req 흡수 후) carve 절차 재진입 — RULE-01 Task ID 중복 검증 + RULE-06 grep-baseline 실측 박제 + grep dry-run 검증 의무 이행.
# 2026-05-17 planner session (7th tick @HEAD=f136f21) — toolchain-version-coherence 정체 2회차 도달 → 격리.
#   재실행 baseline (HEAD=f136f21 실측):
#     - 본 spec 파일 git log last commit: `946dca1` (inspector REQ-061 흡수 — 이후 미터치). working tree clean.
#     - 본 spec blob hash: `813b6cf3bb705b14e61fbfaebc2863dc9bf54d30` (6th tick 박제 `813b6cf3` 와 동일 — content unchanged 검증).
#     - node_modules/typescript@4.9.3 잔존 (devDep `^6.0.3` ↔ installed 격차 2 메이저 — 3rd/4th/5th/6th tick 박제와 동일).
#     - npm run typecheck rc=2 + preprocessing 카테고리 `TS6046|TS5070|TS2688` 3 hit 잔존 (동일 패턴).
#     - npm ls typescript `invalid:` 7 hit 잔존 (msw peer 등 — 동일 패턴).
#     - 50.blocked/spec/foundation/: 본 tick 진입 시 비어있음 (6th tick 종료 시점 동일 — 5th tick 격리 2건은 81c08d1 revisit followup 승격 후 삭제 완료).
#     - 10.followups/: 2건 (husky-pre-push-typecheck-from-blocked + island-regression-guard-from-blocked) — planner 미접촉.
#     - 20.req/: 1건 (20260517-island-prop-types-removal) — inspector writer 영역, planner 미접촉.
#     - .inspector-seen: writer 경계 미접촉.
#   본 세션 처리:
#     - carve 0건: toolchain-version-coherence — 6th tick 행동지침 (1) 충족 (hash `813b6cf3` 동일 + 환경 미회복 + 정체 2회차 진입) → 50.blocked/spec/foundation/toolchain-version-coherence.md 격리 + _reason.md 박제.
#     - 격리 결정 근거 박제 (_reason.md): (i) 수단 중립 메타 spec 의 task 결정 권한 부재 ("의도적으로 하지 않는 것: 특정 메이저로의 업/다운그레이드 자체"), (ii) developer writer 영역 (`src/`, `10.followups/`) 밖 산출물 침투 우려 (`package.json` / `node_modules/typescript/package.json` / `tsconfig.json` / `package-lock.json`), (iii) 환경 회복 의존 — § 동작 3 자연 수렴은 § 동작 1·2 정합 결과, (iv) 5th 1차 노출 + 6th 정체 1회차 + 7th 정체 2회차 누적.
#     - 승격 0건: 본 spec 격리 — 승격 대상 부재.
#   다음 세션 (8th tick) 행동지침:
#     (1) 50.blocked/spec/foundation/toolchain-version-coherence.md 는 RULE-05 정식 복귀 경로 — /revisit 스킬이 _reason.md 판정 후 10.followups/ 승격. planner 영역 미접촉 (writer 경계).
#     (2) 환경 회복 (운영자: `npm install` 또는 devDep 다운그레이드 또는 installed 업그레이드 또는 lockfile 재정합 — 본 spec 수단 중립) 선행 시 inspector 가 본 spec 의 § 테스트 현황 + § 수용 기준 측정 통과 처리 후 § 변경 이력 박제 → /revisit revive 경로로 followups 진입 → 재흡수 가능.
#     (3) 새 green spec 진입 시 (예: 20.req/20260517-island-prop-types-removal inspector 흡수 후) carve 절차 재진입 — RULE-01 Task ID 중복 검증 + RULE-06 grep-baseline 실측 박제 + grep dry-run 검증 의무 이행. 현재 환경 회귀 미회복이므로 game-day / lint baseline 게이트 요구 spec 은 carve 보류 (5th tick 박제 island-regression-guard 격리 사유와 동일 패턴).
#     (4) 7th tick 종료 시점 green 큐 비움 — 8th tick 진입 시 green 비어있고 입력 큐 (`30.spec/green/**`) 무 → RULE-03 선결 점검 (2) 충족 시 no-op (입력 큐 비면 무해 종료).
#     (5) blocked 4건 (toolchain-version-coherence + 기존 3건 followups 승격 완료) 의 환경 회복 후 일괄 해소 흐름은 토폴로지상 root cause = toolchain-version-coherence — 본 spec 의 followup 승격 + req 재흡수 + green 재박제 사이클이 선행되어야 husky-pre-push-typecheck + island-regression-guard 도 followup → green 진행 가능.
island-proptypes-removal f5c84bbd2ac2b7da430c1d784593fb536eca4e85
runtime-dep-version-coherence 9e2c0f9e7614f25d1f99d3aacf30e7d1eeda2b98
# 2026-05-17 planner session (9th tick @HEAD=07eaf10) — 신규 green spec island-proptypes-removal 1차 노출 + Task ID 충돌 발견 → carve 0건 (재시도 금지 RULE-02).
#   재실행 baseline (HEAD=07eaf10 실측):
#     - green spec hash: island-proptypes-removal `f5c84bbd2ac2b7da430c1d784593fb536eca4e85` (commit 07eaf10 inspector REQ-062 흡수 — 본 tick 1차 노출).
#     - 본 spec §스코프 규칙 grep-baseline (a)~(f) 6건 실측 정합: (a) island 5dir PropTypes 39 hit / 10 files (spec 박제 46/11 과 7 hit / 1 file 격차 — Toaster.tsx working tree drift 반영. HEAD 기준 `git show HEAD:src/Toaster/Toaster.tsx | grep -c "PropTypes"` = 7 → spec 박제 일치) / (b) import 11 hit / 11 files (HEAD 기준) / (c) propTypes selector 11 files (HEAD 기준) / (d) interface XxxProps 15 hit / (e) 비-island Log/Monitor 41 hit / 12 files / (f) package.json:10 `"prop-types"` 1 hit.
#     - working tree drift: `src/Toaster/Toaster.tsx` unstaged mod (PropTypes import line 2 + selector line 87 블록 제거 — −10 lines). developer 미커밋 잔존 (블록 처리 후 git restore 미수행 또는 별 세션 추가 작업). RULE-01 src/ 는 developer writer 영역 — planner 미접촉.
#     - 50.blocked/task/: TSK-20260517-01-island-proptypes-removal-toaster.md + _reason.md 2 파일 존재 (gitignored — git status 무노출. 직전 developer 세션이 본 task 시도 후 pre-commit hook `@eslint/js` 환경 회귀로 블록 + RULE-05 진입). 본 task 의 `범위 밖` 섹션이 후속 TSK-20260517-02 (Comment), -03 (File), -04 (Image), -05 (common) 4건을 명시 참조 — 사전 carve 계획 박제됨.
#     - 50.blocked/spec/foundation/: toolchain-version-coherence.md + _reason.md 잔존 (7th tick 격리 — RULE-05 /revisit 대기).
#     - 10.followups/: 3건 (husky-pre-push-typecheck-from-blocked + island-regression-guard-from-blocked + toolchain-version-coherence-from-blocked) — discovery 영역, planner 미접촉.
#     - 20.req/: 비어있음.
#     - 40.task/: 본 tick 진입 시점 비어있음. 본 tick 종료 시점도 비어있음 (생성 시도 후 회수).
#     - 환경 회귀 지속: node_modules 부재 잔존 (vite/vitest/eslint 의존성 ERR_MODULE_NOT_FOUND `@eslint/js` — 직전 11+ 커밋 동일 박제 chain).
#   본 세션 처리:
#     - carve 시도: island-proptypes-removal §동작 1.1/1.2/1.3 게이트 충족을 위한 island 5 dir 별 원자 task carve 안 작성 — TSK-20260517-01 (Toaster) / -02 (common) / -03 (Image) / -04 (Comment) / -05 (File). 각 디렉터리 단일 PR / 독립 롤백 / RULE-06 §스코프 규칙 grep-baseline 실측 박제 (5/5) / grep dry-run 검증 20/20 match (단 Toaster 는 HEAD 기준 7/1/1/1 + working tree 0/0/0/1 분리 박제).
#     - 충돌 발견: 50.blocked/task/TSK-20260517-01-island-proptypes-removal-toaster.md + _reason.md 사전 존재 (gitignored — 본 세션 진입 시 RULE-01 중복 검증 grep `TSK-20260517-` 가 40.task / 60.done 만 스캔하고 50.blocked/task 미포함하여 1차 누락). RULE-01 명시 "grep -rn 'TSK-...' specs/{40.task,60.done,50.blocked}/" 50.blocked 포함 재실행 시 TSK-20260517-01 collision 검출.
#     - 회수 결정: RULE-02 "Fail-fast: 실패·충돌 → blocked/. 재시도 없음." + RULE-02 "재시도 없음" + RULE-05 "blocked → 10.followups/ → discovery → ..." (정식 복귀 경로) 위반 회피. 본 세션이 생성한 5 task 파일은 모두 회수 (40.task/ 비우기) — 본 task 발행은 직전 50.blocked/task/TSK-20260517-01 의 RULE-05 /revisit 미수행 상태에서 재시도로 분류됨. 본 시점 환경 회귀도 미회복 (직전 reason.md 의 차단 사유 = pre-commit hook `@eslint/js` resolve 실패 — 본 tick 미해소). 회수 수단: `rm` (5 파일 모두 본 세션 내부 신규 + uncommitted + gitignored. RULE-02 "삭제 필요 시 mv 로 대체" 는 산출물 보존 원칙 — 세션 내부 authoring draft 의 충돌 폐기는 mv 대체 부적합. 안전성: gitignored 영역이므로 git history 영향 0).
#     - 격리 0건: green spec 본문 자체는 RULE-07 정합 (불변식 평서 + 시점 비의존) + 1차 노출 + 정체 카운트 0 → 격리 부적합.
#     - 승격 0건: §테스트 현황 + §수용 기준 unchecked `- [ ]` 17건 잔존 (FR-01~06 + NFR-01~03) → RULE-01 승격 4조건 미충족 (unchecked == 0 위반).
#   다음 세션 (10th tick) 행동지침:
#     (1) 50.blocked/task/TSK-20260517-01 + _reason.md 는 RULE-05 정식 복귀 경로 — /revisit 스킬이 _reason.md 판정 후 10.followups/ revive. planner 영역 미접촉.
#     (2) 환경 회귀 (node_modules 부재 + pre-commit hook `@eslint/js` resolve 실패) 운영자 해소 선행 필요 — `npm ci` 또는 동등. 50.blocked/spec/foundation/toolchain-version-coherence.md 의 §동작 1~3 자연 수렴 의존.
#     (3) island-proptypes-removal hash 가 본 tick 박제 (f5c84bbd) 와 동일 + 50.blocked/task/TSK-20260517-01 잔존 시 → 본 spec 의 task carve 는 10th tick 진입 시점에도 RULE-05 미해소 사유로 보류. 본 spec 자체는 직전 carve attempt 박제로 정체 1회차 카운트 (10th tick 진입 시 격리 후보 도래 = 정체 1회차 → 2회차 진입 = 11th tick).
#     (4) RULE-05 /revisit 으로 TSK-20260517-01 가 followups 진입 후 discovery 흡수 → 20.req/ 발행 → inspector spec 재흡수 (island-proptypes-removal 갱신 또는 별 spec 분기) → planner 재진입 가능. 본 흐름 정합 시 신규 Task ID 는 -06 부터 또는 spec 재흡수 시점 hash 변경으로 carve 재계산.
#     (5) 본 tick 의 회수 사유는 (i) RULE-01 충돌 검증 누락 (50.blocked/task 미스캔) 의 사후 정합 회복 + (ii) RULE-02 재시도 금지 + (iii) 환경 회귀 미회복. (i) 은 본 tick 박제로 표준화 — 차기 tick 부터 RULE-01 grep 검증은 50.blocked/task/ 포함.
#     (6) working tree `src/Toaster/Toaster.tsx` unstaged mod 는 developer writer 영역 — planner 미접촉. discovery 또는 developer 가 환경 회복 후 처리.
# 2026-05-17 planner session (10th tick @HEAD=3e7af7e) — spec hash 동일 + RULE-05 /revisit 부분 진척 (blocked → followups) + discovery 미흡수 → carve 보류 (정체 1회차 누적).
#   재실행 baseline (HEAD=3e7af7e 실측):
#     - green spec hash: island-proptypes-removal `f5c84bbd2ac2b7da430c1d784593fb536eca4e85` (9th tick 박제와 동일 — inspector 미터치 검증).
#     - working tree: clean (9th tick 박제 `src/Toaster/Toaster.tsx` unstaged mod 해소 — developer 별 세션 정리 또는 git restore. RULE-01 src/ 는 developer writer 영역, planner 미접촉).
#     - 50.blocked/task/: **비어있음** (9th tick 박제 TSK-20260517-01-island-proptypes-removal-toaster.md + _reason.md 가 RULE-05 /revisit 처리 완료 — chore(revisit) followup 승격).
#     - 50.blocked/spec/foundation/: **비어있음** (7th tick 격리 toolchain-version-coherence 도 followup 승격 완료).
#     - 10.followups/: 1건 — `20260516-2319-TSK-20260517-01-island-proptypes-removal-toaster-from-blocked.md` (RULE-05 revive). discovery 미흡수 — planner writer 영역 밖. 다른 followups (husky/island-regression-guard/toolchain-version-coherence-from-blocked) 는 discovery 흡수 후 60.done/followups/ 또는 20.req/ 진입 완료 (현재 followups 디렉터리에 부재).
#     - 20.req/: 1건 — `20260517-runtime-dep-version-coherence.md` (toolchain-version-coherence-from-blocked followup 의 discovery 흡수 산출물 추정 — inspector 미흡수, planner 영역 밖).
#     - 40.task/: 비어있음 (9th tick 회수 후 잔존 0).
#     - 환경 회귀 부분 회복: `node_modules/@eslint/js` 존재 (rc=0) — 9th tick ERR_MODULE_NOT_FOUND 해소. 단 본 tick 은 pre-commit hook full path / typecheck rc / lint rc 실측 생략 (carve 자체 보류 결정으로 게이트 재검증 불필요 + RULE-02 비대화 모호하면 blocked).
#     - Task ID footprint: `TSK-20260517-01` 은 10.followups/ 에 from-blocked 형태로 박제 잔존 (3 hit — followup body 메타 라인). 40.task/ + 60.done/ + 50.blocked/ 부재 — discovery 흡수 시점부터 ID 재산정 결정 가능 (RULE-05 close 시 60.done/followups/ 아카이브, revive 시 신규 req → 새 spec hash → 새 Task ID 산정).
#   본 세션 처리:
#     - carve 0건: spec hash 동일 (f5c84bbd) + RULE-05 /revisit 진척이 (i) blocked → followups 만 완료 + (ii) discovery 흡수 → req 발행 미완료 + (iii) inspector spec 재흡수 미완료 → 9th tick 행동지침 (4) "discovery 흡수 → 20.req/ 발행 → inspector spec 재흡수 → planner 재진입 가능" 충족 단계 1/3. planner 진입 조건 미달. 동일 spec 본문에 대한 재carve 는 RULE-02 "재시도 없음" 위반 + Task ID 산정 모호 (-01 재사용 vs -06+ 신규).
#     - 격리 0건: 정체 1회차 누적 — 9th tick 행동지침 (3) "정체 1회차 → 2회차 진입 = 11th tick" 조건 잔존. 본 tick 은 1회차 카운트 진입 (격리 후보 도래 시점은 11th tick).
#     - 승격 0건: §테스트 현황 + §수용 기준 unchecked `- [ ]` 24건 잔존 (FR-01~06 + NFR-01~03 측정 항목) → RULE-01 승격 4조건 (`[WIP]` 0 + unchecked 0 + `^#+ .*To-Be` 0 + 변경 이력 박제) 중 unchecked == 0 위반. 박제 자체 정합 (시점 박제 의존 분리 평서화).
#   다음 세션 (11th tick) 행동지침:
#     (1) 10.followups/20260516-2319-TSK-20260517-01-island-proptypes-removal-toaster-from-blocked.md 는 discovery writer 영역 — planner 미접촉. discovery 흡수 → 20.req/ 발행 후에야 inspector → green spec 재편집 흐름 가능.
#     (2) spec hash 가 본 tick 박제 (f5c84bbd) 와 동일 + 10.followups/ 의 from-blocked 잔존 + 20.req/ 신규 island-proptypes-removal 부재 시 → 정체 2회차 진입 → 50.blocked/spec/foundation/island-proptypes-removal.md 격리 + _reason.md 박제 (RULE-05 정식 복귀 경로 결정 의존 + discovery 흐름 정체 + Task ID 산정 결정 불능). 정식 복귀 경로는 RULE-05 /revisit.
#     (3) spec hash 가 달라지면 (inspector 가 spec 재편집 — REQ-062 갱신 흡수 또는 별 spec 분기) 정체 카운트 리셋 → carve 절차 재진입. 신규 Task ID 는 RULE-01 grep `TSK-20260517-` 전수 (40.task + 60.done + 50.blocked + 10.followups + 20.req) 스캔 후 미충돌 최소 NN 선택.
#     (4) RULE-01 grep 표준에 10.followups/ + 20.req/ 추가 권고 박제 — 9th tick 표준화 (50.blocked/task/ 포함) 의 후속 정합. ID 재사용 분쟁 회피 목적.
#     (5) 환경 회귀: `@eslint/js` 모듈 부분 회복. 단 9th tick 박제 pre-commit hook 실패 사유 (full pipeline = typecheck + lint + test) 의 다른 카테고리 회복 여부는 carve 재시도 직전 실측 박제 필수.
#     (6) 20.req/20260517-runtime-dep-version-coherence.md 는 inspector writer 영역 — planner 미접촉. 흡수 후 신규 green spec 진입 시 carve 절차 재진입 (단 toolchain-version-coherence 의 후신이라면 5th-7th tick 박제 "수단 중립 메타 spec → developer writer 영역 침투" 격리 사유 재발 가능 — inspector 흡수 시점 RULE-07 정합 검증 의존).
# 2026-05-17 planner session (11th tick @HEAD=0305fb1) — island-proptypes-removal 정체 2회차 도달 + 격리 보류 (행동지침 (2) AND 조건 미충족) + runtime-dep-version-coherence 신규 1차 노출 (수단 중립 메타 spec — carve 보류).
#   재실행 baseline (HEAD=0305fb1 실측):
#     - green spec hash 정합: island-proptypes-removal `f5c84bbd2ac2b7da430c1d784593fb536eca4e85` (9th/10th tick 박제와 동일 — inspector 미터치 검증) + runtime-dep-version-coherence `9e2c0f9e7614f25d1f99d3aacf30e7d1eeda2b98` (11th tick 1차 노출 — 0305fb1 inspector REQ-063 흡수).
#     - working tree: clean.
#     - 50.blocked/{task,spec}/: 비어있음 (10th tick 진척 유지).
#     - 10.followups/: **비어있음** (10th tick 박제 `20260516-2319-TSK-20260517-01-island-proptypes-removal-toaster-from-blocked.md` 가 60.done/2026/05/17/followups/ 로 mv 완료 — discovery 흡수 + RULE-01 mv 정합. 검증: `grep -rn "TSK-20260517-" specs/60.done/` → 3 hit in `60.done/2026/05/17/followups/20260516-2319-TSK-20260517-01-...md` 본문 메타).
#     - 20.req/: 1건 — `20260517-devbin-install-integrity.md` (discovery 흡수 산출물 — TSK-20260517-01 followup 의 환경 회귀 카테고리 추출). inspector 미흡수 — planner writer 영역 밖. 단 본 req 는 island 후신 (PropTypes 제거 task 재발행) 이 아니라 **pre-commit hook 환경 회귀 메타 req** — TSK-20260517-01 의 "island carve 자체는 정합 완료, hook 환경만 차단" 사실의 메타 패턴 분기. 따라서 island 후신 req 는 부재.
#     - 40.task/: 비어있음. TASK_READY_MAX=10 미달 (RULE-03).
#     - 60.done/2026/05/17/: req 2건 (`20260517-runtime-dep-version-coherence.md` + `20260517-toolchain-version-coherence.md`) + followups 다수 (revisit 처리 followup 포함). 10th tick 행동지침 (1) 정식 RULE-05 경로 충족.
#     - 환경 회귀: 본 tick 은 typecheck / lint / pre-commit hook 실측 생략 (carve 보류 결정 — 게이트 재검증 불필요). 직전 11+ 커밋 pre-commit hook `@eslint/js` resolve 실패 박제 chain (20.req/20260517-devbin-install-integrity.md:11 참조 — `aa0baff`, `96c71de`, `dbb9b9b`, `ffaa752`, `946dca1`, `849bb9d`, `f136f21`, `80e2b4c` 등 8+ 커밋 동일 환경 미회복 다세션 지속) 잔존 가정.
#     - Task ID footprint 전수 (RULE-01 표준 + 10th 행동지침 (4) 권고 박제 — 50.blocked/task/ + 10.followups/ + 20.req/ 포함): `TSK-20260517-01` 만 존재 (60.done/2026/05/17/followups/ + 20.req/20260517-devbin-install-integrity.md 본문 메타 참조). `TSK-20260517-02~99` 미사용 — 신규 carve 가능 시 -02+ 부터 채번.
#   본 세션 처리:
#     - carve 0건: island-proptypes-removal (정체 2회차 도달).
#       (i) 10th tick 행동지침 (2) 격리 조건 (hash `f5c84bbd` 동일 ∧ 10.followups/ from-blocked 잔존 ∧ 20.req/ 신규 island-proptypes-removal 부재) AND 3 조건 검사: (a) hash 동일 충족, (b) **followups 잔존 불충족** (60.done 으로 mv 완료 — RULE-05 정식 경로 진척), (c) island 후신 req 부재 충족 (20.req/ 의 devbin-install-integrity 는 환경 회귀 메타 req, island carve 후신 아님). (b) 불충족 → 격리 발동 조건 미달.
#       (ii) RULE-05 정식 복귀 경로 (TSK-20260517-01 의 followup → done) 가 진척했음에도 island carve 재시도가 RULE-02 "재시도 없음" 잠재 위반 (Task ID 산정 모호 + 동일 spec 본문에 대한 carve 결과는 9th 회수와 등가) — 신규 발행은 inspector 가 spec hash 변경 (REQ-062 갱신 흡수 또는 환경 회복 후 §변경 이력 박제) 후에만 정합. 본 spec hash 미변경 → carve 재진입 부적합.
#       (iii) 정체 카운트 2회차 누적 — 10th tick 행동지침 (3) "정체 카운트 리셋은 hash 변경 시점" 정합. 격리는 보류 (행동지침 (2) AND 조건 미충족 — boolean 정합).
#     - carve 0건: runtime-dep-version-coherence (1차 노출 — 정체 0회차).
#       (i) 본 spec §역할 line 10 명시 "의도적으로 하지 않는 것: 특정 메이저 ... 업/다운그레이드 운영 task (수단 중립 — `package.json` 다운그레이드 vs `npm install <pkg>@<major>` vs lockfile 재정합 모두 허용)" — 5th-7th tick 박제 toolchain-version-coherence 격리 사유 ("수단 중립 메타 spec → 수단 결정 권한 부재 + developer writer 영역 (`src/`, `10.followups/`) 밖 산출물 침투") 와 동일 패턴.
#       (ii) 본 spec § 동작 1~3 수렴 산출물 = `package.json` (declared semver range 변경) / `node_modules/<pkg>/` (installed version 변경) / `package-lock.json` (lockfile 재정합) / `specs/30.spec/blue/components/*.md` (blue 박제 메이저 변경 — inspector writer 영역). 단일 PR 크기 / 독립 롤백 가능 / depends_on 명시의 원자 task 분할 자체는 carve 가능하나, 산출물 경계 (developer 의 `src/` + `10.followups/` 범위) 침투 우려 충돌.
#       (iii) RULE-07 정합 검증: 본 spec § 동작 1~7 모두 평서형·반복 검증 가능 (`node -p` + grep count + blue spec 박제 추출)·시점 비의존 (특정 메이저 숫자는 §스코프 규칙 baseline 에만 박제, 효능 평서문은 "격차 0" 자체)·incident patch 아님. → spec 박제 자체는 정합 (격리 부적합).
#       (iv) RULE-01 grep 중복 검증 (50.blocked/task/ + 10.followups/ + 20.req/ + 40.task/ + 60.done/ 전수): `TSK-20260517-01` 만 존재. 신규 발행 시 -02 부터 채번 가능하나, carve 결정 자체가 위 (i)(ii) 사유로 보류.
#       (v) 정체 카운트 0 — 1차 노출. 격리 후보 진입 시점은 12th tick (2회차 진입) 이상 + 동일 사유 누적 + spec hash 동일 유지 시.
#     - 격리 0건: island-proptypes-removal 은 행동지침 (2) AND 조건 미충족 (followups 비어있음 → discovery 흡수 흐름 진척 신호). runtime-dep-version-coherence 는 1차 노출 — 격리 부적합. 두 spec 모두 본 tick 격리 부적합.
#     - 승격 0건: island-proptypes-removal §테스트 현황 + §수용 기준 unchecked `- [ ]` 24건 잔존 + runtime-dep-version-coherence §테스트 현황 17 + §수용 기준 11 = unchecked 28건 잔존 → RULE-01 승격 4조건 (`[WIP]` 0 + unchecked 0 + `^#+ .*To-Be` 0 + 변경 이력 박제) 중 unchecked == 0 위반. 박제 자체 정합 (시점 박제 의존 분리 평서화).
#   다음 세션 (12th tick) 행동지침:
#     (1) 20.req/20260517-devbin-install-integrity.md 는 inspector writer 영역 — planner 미접촉. inspector 흡수 → green spec 신규 박제 (devbin / npm wrapper / lockfile 무결성 메타 spec 분기) 또는 island-proptypes-removal §변경 이력 추가 (환경 회귀 후신 통합 — hash 변경 → 정체 리셋) 흐름 가능.
#     (2) island-proptypes-removal hash 가 본 tick 박제 (f5c84bbd) 와 동일 + 환경 회귀 미회복 (pre-commit hook `@eslint/js` resolve 실패 박제 chain 잔존) 시 → 본 spec 의 carve 진입 조건은 (a) inspector 가 §변경 이력 박제 또는 본문 수정 후 hash 변경, (b) 환경 회복 (pre-commit hook full pipeline rc=0 — typecheck + lint + test) — 둘 다 planner 영역 밖. 본 tick 정체 2회차 + 다음 tick 정체 3회차 누적 진입 시 격리 결정 재검토 (행동지침 (2) AND 조건 완화 — discovery 흐름 진척했음에도 inspector 미흡수 + hash 미변경 ≥ 3 tick 정체는 RULE-02 fail-fast 발동 후보).
#     (3) runtime-dep-version-coherence hash 가 본 tick 박제 (9e2c0f9e) 와 동일 + 정체 1회차 재카운트 시 → 12th tick 진입 시 carve 보류 사유 동일 (수단 중립 메타 spec). 정체 2회차 진입 = 13th tick 도달 + hash 동일 + 환경 미회복 시 격리 후보 (toolchain-version-coherence 5th-7th tick 격리 패턴 동일 적용).
#     (4) hash 변경 (inspector 가 spec 재편집) 시 정체 카운트 리셋 → carve 절차 재진입 (RULE-01 Task ID 전수 grep + RULE-06 grep-baseline 실측 + grep dry-run 검증).
#     (5) 환경 회복 (운영자: `npm ci` 또는 `node_modules` 재설치 + pre-commit hook full pipeline rc=0 확보) 시 island-proptypes-removal 의 carve 재진입 가능 — 단 hash 변경이 선행되지 않으면 RULE-02 재시도 금지로 spec hash 변경 전 carve 부적합.
#     (6) 새 green spec 진입 시 (예: devbin-install-integrity req 흡수 또는 island-proptypes-removal 갱신 흡수) carve 절차 재진입 — RULE-01 Task ID 전수 grep (40.task/ + 60.done/ + 50.blocked/ + 10.followups/ + 20.req/ — 10th 행동지침 (4) 표준) + RULE-06 §스코프 규칙 grep-baseline 실측 박제 + grep dry-run 검증 의무.
#     (7) Task ID 채번 정합: `TSK-20260517-01` 은 60.done/followups + 20.req/ 본문 메타 참조로 박제됨 — 재사용 금지. 신규 발행은 `TSK-20260517-02` 부터 (단조 증가). RULE-05 close 처리된 -01 은 60.done 영구 박제로 ID 풀에서 제외.
#     (8) 본 tick 박제 시점 baseline (HEAD=0305fb1) 와 12th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) 두 green spec hash 동일성 재실측, (b) 10.followups + 20.req 새 항목 검출, (c) 50.blocked 진입 항목 검출, (d) Task ID 전수 grep 재실행. 격차 항목 별 행동지침 (1)~(7) 분기 적용.
# 2026-05-17 planner session (12th tick @HEAD=89c7b5c) — devbin-install-integrity 신규 1차 노출 (수단 중립 메타 spec — carve 보류) + island-proptypes-removal 정체 3회차 도달 (격리 보류 — 후속 spec 발행 진척 신호로 RULE-02 fail-fast 유보) + runtime-dep-version-coherence 정체 2회차 누적 (carve 보류 — 수단 중립 메타 spec).
#   재실행 baseline (HEAD=89c7b5c 실측):
#     - green spec hash 정합: island-proptypes-removal `f5c84bbd2ac2b7da430c1d784593fb536eca4e85` (9th/10th/11th tick 박제와 동일 — inspector 미터치 검증, 정체 3회차 도달) + runtime-dep-version-coherence `9e2c0f9e7614f25d1f99d3aacf30e7d1eeda2b98` (11th tick 박제와 동일 — inspector 미터치, 정체 2회차 누적) + devbin-install-integrity `948827ca8820c615f4f9a7ba5222a81d84776373` (12th tick 1차 노출 — inspector REQ-064 흡수 산출물).
#     - working tree: clean.
#     - 50.blocked/{task,spec}/: 비어있음 (10th-11th tick 진척 유지).
#     - 10.followups/: 비어있음 (11th tick 진척 유지 — 60.done/2026/05/17/followups/ 박제 유지).
#     - 20.req/: 1건 — `20260517-path-alias-resolver-coherence.md` (REQ-065 신규 discovery 흡수 산출물). inspector 미흡수, planner writer 영역 밖.
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 — RULE-03).
#     - 60.done/2026/05/17/req/: 3건 누적 (toolchain-version-coherence + runtime-dep-version-coherence + devbin-install-integrity — 12th tick 신규 추가).
#     - 환경 회귀: 본 tick 은 typecheck / lint / pre-commit hook 실측 생략 (carve 자체 보류 결정으로 게이트 재검증 불필요). 직전 11+ 커밋 chain (REQ-064 본문 8+ 커밋 박제) 미회복 가정.
#     - Task ID footprint 전수 (RULE-01 표준 + 10th-11th tick 권고 박제 — 40.task + 60.done + 50.blocked + 10.followups + 20.req): `TSK-20260517-01` 만 존재 (60.done/2026/05/17/followups/ + 20.req/* 본문 메타 참조). `TSK-20260517-02~99` 미사용 — 신규 carve 가능 시 -02+ 부터 채번 가능.
#   본 세션 처리:
#     - carve 0건: island-proptypes-removal (정체 3회차 진입).
#       (i) 11th tick 행동지침 (2) "hash 동일 ∧ 환경 회귀 미회복 + 정체 3회차 누적 진입 시 RULE-02 fail-fast 발동 후보". 본 tick: hash `f5c84bbd` 동일 + inspector 미터치 + 정체 3회차 도달.
#       (ii) 그러나 inspector 우선순위 분기 신호 검출 — 12th tick 진입 시점 inspector 가 island 갱신 (§변경 이력 추가) 대신 (a) devbin-install-integrity 신규 green spec 박제 (REQ-064 흡수) 진척 + (b) 20.req/path-alias-resolver-coherence (REQ-065) discovery 발행 진척 (inspector 미흡수). 본 두 후속 산출물은 island 의 환경 회귀 차단 사유 (pre-commit hook `@eslint/js` resolve 실패) 의 **메타 패턴 분기** — devbin install 정합 + path alias 정합. inspector 가 island 갱신을 stale 처리한 것이 아니라 **선행 메타 spec 정착 후 island 재진입** 의 토폴로지 신호로 해석 가능.
#       (iii) 따라서 RULE-02 fail-fast 발동은 본 tick 보류 — 격리 결정 대신 정체 3회차 카운트 박제. 13th tick 진입 시 (a) island hash 변경 (inspector 재흡수) 시 카운트 리셋, (b) island hash 동일 + 후속 메타 spec (devbin / path-alias) 진척 정체 시 4회차 누적 → RULE-02 fail-fast 발동 후보 재검토 (이번엔 정식 격리 발동 강력 후보).
#       (iv) carve 자체 부적합 사유 직전 박제 유지: (a) hash 미변경 → RULE-02 재시도 금지, (b) Task ID 산정 모호 (-01 의 60.done 영구 박제 후 신규 -02+ 채번 정합성), (c) 환경 회귀 미회복 (pre-commit hook full pipeline rc=0 미확보 — carve 시 즉시 50.blocked/task/ 재진입 예측).
#     - carve 0건: runtime-dep-version-coherence (정체 2회차 누적).
#       (i) 11th tick 행동지침 (3) "hash 동일 + 정체 2회차 진입 = 13th tick 도달 + hash 동일 + 환경 미회복 시 격리 후보 (toolchain-version-coherence 5th-7th tick 격리 패턴 동일 적용)". 본 tick 은 12th 진입 → 격리 후보 도래 미달.
#       (ii) carve 보류 사유 11th tick 박제 유지: 수단 중립 메타 spec (§역할 "의도적으로 하지 않는 것: 특정 메이저로의 업/다운그레이드 운영 task" — 수단 결정 권한 부재 + developer writer 영역 (`src/`, `10.followups/`) 밖 산출물 (`package.json` / `node_modules/<pkg>/` / `package-lock.json` / blue spec 박제) 침투 우려).
#       (iii) RULE-07 정합: § 동작 1~7 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재 → 격리 부적합 (spec 박제 자체 정합).
#     - carve 0건: devbin-install-integrity (1차 노출 — 정체 0회차).
#       (i) 본 spec §역할 line 10 명시 "의도적으로 하지 않는 것: `npm ci` / `npm install` / `pnpm` / `yarn` 등 install 수단 라벨 부여 (RULE-07 수단 중립), 특정 패키지의 특정 메이저로의 업/다운그레이드 자체 (REQ-061/063 관할), 환경 회복 운영 task 자체 (developer 영역)" + § 동작 6.1 "본 spec 본문에 수단 라벨 박제 부재 자기 검증".
#       (ii) 본 spec 의 효능 도달 산출물 = `node_modules/<pkg>/` (install 산출) / `node_modules/.bin/<cmd>` (PATH symlink) / `package-lock.json` (lockfile 재정합) — 모두 developer writer 영역 (`src/`, `10.followups/`) 밖. toolchain-version-coherence 5th-7th tick 격리 사유 + runtime-dep-version-coherence 11th tick carve 보류 사유와 동일 패턴 — 수단 중립 메타 spec 의 task 분할은 (a) 수단 결정 권한 부재 + (b) 산출물 경계 침투 충돌 → carve 보류.
#       (iii) RULE-07 정합 검증: § 동작 1~7 모두 평서형·반복 검증 가능 (`ls` + `node -p` + `command -v` + `npm ls` 1-line)·시점 비의존 (특정 패키지 이름·메이저 숫자는 §스코프 규칙 grep-baseline 에만 박제, 효능 평서문은 "존재" + "격차 0" 자체)·incident 귀속 부재. → spec 박제 자체 정합 (격리 부적합).
#       (iv) RULE-01 grep 중복 검증 전수: `TSK-20260517-01` 만 존재 (60.done/followups + 20.req 본문 메타). 신규 발행 시 -02 부터 채번 가능하나, carve 결정 자체가 위 (i)(ii) 사유로 보류.
#       (v) 정체 카운트 0 — 1차 노출. 13th tick 진입 시 hash 동일 + 환경 회귀 미회복 + 후속 inspector spec 갱신 미진척 시 정체 1회차 누적. 14th tick 진입 시 정체 2회차 → 격리 후보 도래 (toolchain-version-coherence 5th-7th 격리 패턴 시계열 정합).
#     - 격리 0건: 세 spec 모두 격리 부적합 (island 정체 3회차이나 inspector 우선순위 분기 신호로 RULE-02 fail-fast 유보 / runtime 정체 2회차 누적이나 행동지침 (3) 격리 후보 도래 미달 / devbin 1차 노출).
#     - 승격 0건: 세 spec 모두 §테스트 현황 + §수용 기준 unchecked `- [ ]` 잔존 (island 24 / runtime 28 / devbin 32 = 총 84건) → RULE-01 승격 4조건 (`[WIP]` 0 + unchecked 0 + `^#+ .*To-Be` 0 + 변경 이력 박제) 중 unchecked == 0 위반. 박제 자체 정합 (`[WIP]` 0 hit + `To-Be` 0 hit + 변경 이력 박제 모두 정합).
#   다음 세션 (13th tick) 행동지침:
#     (1) 20.req/20260517-path-alias-resolver-coherence.md (REQ-065) 는 inspector writer 영역 — planner 미접촉. inspector 흡수 → green spec 신규 박제 (path alias resolver 메타 spec) 또는 기존 spec §변경 이력 추가 흐름 가능. 흡수 시점 hash 변경 → carve 재진입 가능 (단 수단 중립 메타 spec 패턴 검출 시 carve 보류 카테고리 진입).
#     (2) island-proptypes-removal hash 가 본 tick 박제 (f5c84bbd) 와 동일 + 정체 4회차 누적 진입 시 → RULE-02 fail-fast 발동 후보 강력 (격리 결정 재검토). 격리 결정 시 _reason.md 박제 사유: (a) hash 4 tick 미변경 + (b) inspector 우선순위 분기 신호가 후속 메타 spec 정착 완료 후에도 island 재진입 미수행 + (c) Task ID 산정 모호 (TSK-20260517-01 60.done 영구 박제 후 -02+ 채번 정합성 불능) + (d) 환경 회귀 미회복 시 carve 시점 즉시 50.blocked/task/ 재진입 예측.
#     (3) island-proptypes-removal hash 변경 (inspector 가 §변경 이력 박제 또는 본문 수정 — REQ-062 갱신 흡수 또는 환경 회복 후 신규 시점 박제) 시 정체 카운트 리셋 → carve 절차 재진입 가능. 신규 Task ID 는 RULE-01 grep 전수 (40.task + 60.done + 50.blocked + 10.followups + 20.req) 스캔 후 미충돌 -02 부터 채번.
#     (4) runtime-dep-version-coherence hash 동일 + 정체 3회차 진입 (13th tick) 시 → 격리 후보 도래 시점 (행동지침 (3) 11th tick 박제 정합). 환경 회복 + inspector spec 본문 수단 라벨 검토 후 (a) 격리 결정 또는 (b) 정체 4회차 누적 (14th tick) 까지 격리 유보 분기. 본 spec 의 carve 자체 부적합은 수단 중립 메타 spec 패턴 (developer writer 영역 침투 우려) 으로 13th tick 박제 유지.
#     (5) devbin-install-integrity hash 동일 + 정체 1회차 누적 (13th tick) 진입 시 → 14th tick 정체 2회차 → 격리 후보 도래 (5th-7th tick toolchain-version-coherence 격리 패턴 시계열 정합). hash 변경 (inspector 가 §변경 이력 박제) 시 정체 카운트 리셋.
#     (6) hash 변경 시 RULE-06 grep-baseline 실측 박제 + grep dry-run 검증 의무 (RULE-04 notes `grep dry-run: N/N match` 토큰). RULE-06 정밀 패턴 권고 — `afterEach 등록 한정 패턴` + `블록 경계 false-positive 회피` + `grep -c 본문 호출 한정` 박제 (본 tick 신규 RULE-06 §정밀 패턴 권고 섹션 추가 반영).
#     (7) Task ID 채번 정합 (10th-11th tick 표준 + 본 tick 박제 유지): `TSK-20260517-01` 60.done/followups + 20.req 본문 메타 박제 → 재사용 금지. RULE-05 close 처리된 -01 은 60.done 영구 박제로 ID 풀에서 제외. 신규 발행은 `TSK-20260517-02` 부터 (단조 증가). 12th tick 종료 시점 grep 전수: 40.task / 50.blocked / 10.followups / 20.req 의 `TSK-20260517-02~99` 미사용 — 신규 carve 가능 시 -02 부터 채번 가능.
#     (8) 본 tick 박제 시점 baseline (HEAD=89c7b5c) 와 13th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) 세 green spec hash 동일성 재실측, (b) 10.followups + 20.req 새 항목 검출, (c) 50.blocked 진입 항목 검출, (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (typecheck / lint / pre-commit hook full pipeline rc) 검출. 격차 항목 별 행동지침 (1)~(7) 분기 적용.
#     (9) RULE-07 정합 검증 (12th tick 박제): 세 green spec (island / runtime / devbin) 모두 § 동작 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재 → 격리 부적합. carve 보류 사유는 수단 중립 메타 spec 패턴 (runtime / devbin) 또는 환경 회귀 + Task ID 산정 모호 (island) — RULE-07 위반과 직교.
# 2026-05-17 planner session (13th tick @HEAD=389afee) — island-proptypes-removal 정체 4회차 도달 → 격리 + runtime-dep-version-coherence 정체 2회차 누적 → 격리 (11차 행동지침 (3) 격리 후보 도래 충족) + devbin-install-integrity 정체 1회차 누적 (격리 부적합 — 14th tick 후보 도래) + path-alias-resolver-coherence 신규 1차 노출 (수단 중립 메타 spec — carve 보류).
#   재실행 baseline (HEAD=389afee 실측):
#     - green spec hash 정합: island-proptypes-removal `f5c84bbd2ac2b7da430c1d784593fb536eca4e85` (9th-12th tick 박제와 동일 — inspector 미터치, 정체 4회차) + runtime-dep-version-coherence `9e2c0f9e7614f25d1f99d3aacf30e7d1eeda2b98` (11th-12th tick 박제와 동일 — inspector 미터치, 정체 2회차) + devbin-install-integrity `948827ca8820c615f4f9a7ba5222a81d84776373` (12th tick 박제와 동일 — inspector 미터치, 정체 1회차) + path-alias-resolver-coherence `3a7a98c7ee4af262895defccf75b84f64563d525` (13th tick 1차 노출 — inspector REQ-065 흡수 산출물, untracked, 본 tick 진입 직전 09:15 시각 생성).
#     - working tree: clean.
#     - 50.blocked/{task,spec}/: 본 tick 진입 시 비어있음. 본 tick 종료 시 50.blocked/spec/foundation/ 2건 격리 (island + runtime + 각각 _reason.md).
#     - 10.followups/: 비어있음 (12th tick 진척 유지 — 60.done/2026/05/17/followups/ 8건 박제 유지).
#     - 20.req/: **비어있음** (12th tick 박제 시점의 `20260517-path-alias-resolver-coherence.md` 는 본 tick 진입 직전 inspector 흡수 → 60.done/2026/05/17/req/ mv 완료 + green spec `path-alias-resolver-coherence.md` 신규 박제 동시 진행).
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 — RULE-03 충족).
#     - 60.done/2026/05/17/req/: 6건 누적 (toolchain-version-coherence + runtime-dep-version-coherence + devbin-island-prop-types-removal + island-prop-types-removal + island-regression-guard-lint-block + husky-pre-push-typecheck-coverage + devbin-install-integrity).
#     - 환경 회귀: 본 tick 은 typecheck / lint / pre-commit hook 실측 생략 (carve 자체 보류 + 격리 결정으로 게이트 재검증 불필요). 직전 다세션 chain (REQ-064 본문 8+ 커밋 박제) 미회복 가정.
#     - Task ID footprint 전수 (RULE-01 표준 + 10th-12th tick 권고 박제 — 40.task + 60.done + 50.blocked + 10.followups + 20.req): `TSK-20260517-01` 만 점유 (60.done/2026/05/17/followups/ 본문 + 60.done/2026/05/17/req/* 본문 메타 참조 — `TSK-20260517-02` / `-03` 참조는 60.done followup 본문 내 사전 계획 라인 + .planner-seen 본문 메모 한정 — ID 점유 아님). `TSK-20260517-02~99` 미사용 — 신규 carve 가능 시 -02 부터 채번.
#   본 세션 처리:
#     - carve 0건: 두 격리 spec + devbin + path-alias 모두 carve 부적합.
#       (i) devbin (정체 1회차 누적) — 수단 중립 메타 spec 패턴 (runtime / toolchain-version-coherence 와 동일) — § 역할 line 10 "의도적으로 하지 않는 것" 박제 + 산출물 경계 (developer writer 영역 밖) + 환경 회귀 미회복 → carve 부적합 유지.
#       (ii) path-alias-resolver-coherence (1차 노출) — § 역할 line 10 박제 "의도적으로 하지 않는 것: 특정 alias 의 추가/삭제 자체 (RULE-07 수단 중립), alias prefix 컨벤션 결정 ... `eslint-plugin-import` resolver / `jsconfig.json` 도입 등 alias resolver 의 추가 채널 확장" — 동일한 수단 중립 메타 spec 패턴. baseline (c)(d)(e)(h) 실측 모두 정합 (대칭차 0, 격차 0, case 일치, 수단 라벨 매치 카테고리 내부 한정) → 본 spec 의 § 동작 1·2·3·8 위반 없음 (현 시점 alias 정합 상태 박제). task 발행 산출물 = `vite.config.js` / `tsconfig.json` / `eslint.config.js` 모두 developer writer 영역 (`src/`, `10.followups/`) 밖 — RULE-01 writer 매트릭스 충돌. RULE-07 정합 (§ 동작 평서형·시점 비의존·incident 귀속 부재). 1차 노출 + 정체 0회차 → 격리 부적합.
#     - 격리 2건:
#       (i) island-proptypes-removal → 50.blocked/spec/foundation/island-proptypes-removal.md + _reason.md. 사유 박제: hash 4 tick 미변경 + inspector 우선순위 분기 신호 정착 진척이나 island 자체 재진입 미수행 + 환경 회귀 미회복 + RULE-02 재시도 금지 (carve 재시도는 9th tick 회수 결과와 등가) + ≥4 tick stale 누적 (12차 행동지침 (2) AND 조건 (a)(b)(d) 충족).
#       (ii) runtime-dep-version-coherence → 50.blocked/spec/foundation/runtime-dep-version-coherence.md + _reason.md. 사유 박제: 수단 중립 메타 spec 패턴 (toolchain-version-coherence 5th-7th tick 격리 패턴 재발) + developer writer 영역 (`src/`, `10.followups/`) 밖 산출물 (`package.json` / `node_modules/<pkg>/` / `package-lock.json` / blue spec) 침투 우려 + 환경 회귀 미회복 + hash 2 tick 미변경 + 11차 행동지침 (3) 격리 후보 도래 시계열 정합.
#     - 승격 0건: 네 spec 모두 §테스트 현황 + §수용 기준 unchecked `- [ ]` 잔존 (island 24 / runtime 28 / devbin 32 / path-alias 32 = 총 116건) → RULE-01 승격 4조건 (`[WIP]` 0 + unchecked 0 + `^#+ .*To-Be` 0 + 변경 이력 박제) 중 unchecked == 0 위반. 박제 자체 정합 (`[WIP]` 0 hit + `To-Be` 0 hit + 변경 이력 1 hit 모두 정합).
#   다음 세션 (14th tick) 행동지침:
#     (1) 50.blocked/spec/foundation/island-proptypes-removal.md + _reason.md / 50.blocked/spec/foundation/runtime-dep-version-coherence.md + _reason.md 는 RULE-05 정식 복귀 경로 — /revisit 스킬이 _reason.md 판정 후 (a) revive 시 10.followups/ 승격, (b) close 시 60.done/2026/05/17/revisit/ 감사노트. planner 영역 미접촉 (writer 경계).
#     (2) devbin-install-integrity hash 가 본 tick 박제 (948827ca) 와 동일 + 환경 회귀 미회복 (pre-commit hook full pipeline rc=0 미확보) + inspector 미터치 시 → 14th tick 진입 시 정체 2회차 누적 → 격리 후보 도래 (5th-7th tick toolchain-version-coherence 격리 패턴 + runtime 11th-13th tick 격리 패턴 동일 적용). 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴 (§ 역할 line 10 박제), (b) developer writer 영역 밖 산출물 침투 우려, (c) 환경 회귀 미회복 + hash 2 tick 미변경.
#     (3) devbin-install-integrity hash 변경 (inspector 가 §변경 이력 박제 또는 본문 갱신 — REQ-064 후속 흡수 또는 환경 회복 후 신규 시점 박제) 시 정체 카운트 리셋 → carve 절차 재진입 가능. 단 수단 중립 박제 자체가 유지되면 carve 부적합 사유 (산출물 경계 침투) 재발 — RULE-07 정합한 spec 이라도 RULE-01 writer 매트릭스 게이트로 carve 보류 가능.
#     (4) path-alias-resolver-coherence hash 가 본 tick 박제 (3a7a98c7) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 14th tick 진입 시 정체 1회차 누적 → 15th tick 정체 2회차 → 격리 후보 도래 (devbin 13th-14th 격리 패턴과 동일 시계열). carve 부적합 사유는 수단 중립 메타 spec 패턴 (devbin / runtime / toolchain-version-coherence 와 동일 — § 역할 line 10 박제) + 산출물 경계 (vite.config.js / tsconfig.json / eslint.config.js 모두 developer writer 영역 밖) 유지. hash 변경 시 (inspector 가 §변경 이력 박제 또는 본문 갱신) 정체 카운트 리셋.
#     (5) Task ID 채번 정합 (10th-12th tick 표준 + 본 tick 박제 유지): `TSK-20260517-01` 60.done/followups + 60.done/req 본문 메타 박제 → 재사용 금지. RULE-05 close 처리된 -01 은 60.done 영구 박제로 ID 풀에서 제외. 신규 발행은 `TSK-20260517-02` 부터 (단조 증가). 본 tick 종료 시점 grep 전수: 40.task / 50.blocked / 10.followups / 20.req 의 `TSK-20260517-02~99` 미사용 — 신규 carve 가능 시 -02 부터 채번 가능.
#     (6) hash 변경 시 RULE-06 grep-baseline 실측 박제 + grep dry-run 검증 의무 (RULE-04 notes `grep dry-run: N/N match` 토큰). RULE-06 §정밀 패턴 권고 섹션 (12차 박제) — `afterEach 등록 한정 패턴` + `블록 경계 false-positive 회피` + `grep -c 본문 호출 한정` 박제 유지.
#     (7) 본 tick 박제 시점 baseline (HEAD=389afee) 와 14th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) devbin / path-alias spec hash 동일성 재실측, (b) 10.followups + 20.req 새 항목 검출 (REQ-066+ inspector 흡수 신호), (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (typecheck / lint / pre-commit hook full pipeline rc) 검출. 격차 항목 별 행동지침 (1)~(6) 분기 적용.
#     (8) 격리 spec 2건 (island + runtime) 의 토폴로지 의존성: runtime (RULE-07 메타 spec) 의 § 동작 1·2 정합 수렴이 island 의 환경 회복 게이트 (pre-commit hook full pipeline rc=0) 의 root cause 일 수 있음. /revisit revive 우선순위는 runtime → devbin → island 토폴로지 순 권고 (운영자 결정).
#     (9) RULE-07 정합 검증 (13th tick 박제): 격리 2건 모두 § 동작 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재 → 격리 사유는 RULE-07 위반 (spec 콘텐츠 결함) 아님. 격리 사유는 (i) RULE-02 재시도 금지 (island — hash 동일 carve 재시도 부적합) + (ii) 수단 중립 메타 spec 패턴 (runtime — writer 경계 충돌) + (iii) 환경 회귀 미회복 (양자 공통). RULE-05 정식 복귀 경로 (/revisit) 가 유일한 해소 방안.
#     (10) 본 tick 종료 시점 green 잔존 2건 (devbin / path-alias) 모두 수단 중립 메타 spec 패턴 — task 발행 산출물이 developer writer 영역 (`src/`, `10.followups/`) 밖. 토폴로지 의존: alias 정합 (path-alias) → devbin install 정합 (devbin) → 메이저 정합 (runtime, 격리) → toolchain enum 정합 (toolchain-version-coherence, /revisit followup 완료) → island colon 정합 (island, 격리). root cause 는 runtime + devbin 의 환경 회복. /revisit revive 우선순위 권고: runtime → devbin / path-alias → island.

# 2026-05-17 planner session (14th tick @HEAD=267c449) — devbin-install-integrity hash 변경 (948827ca → 9df5205d) + path-alias-resolver-coherence hash 변경 (3a7a98c7 → f7ece98e) → 두 spec 모두 정체 카운트 리셋. 격리 후보 도래 없음. carve 0건 (양 spec 모두 수단 중립 메타 spec 패턴 + developer writer 영역 밖 산출물 경계 충돌 유지). green 승격 0건 (양 spec 모두 unchecked `- [ ]` 다수 — 테스트 현황 + 수용 기준 미박제).
#   본 세션 입력 큐 상태:
#     - green spec hash 정합: devbin-install-integrity `9df5205d4e34a5cbd5ff96c67ecba0c56e18a474fed8f36f8aad0ca44223b15d` (13th tick 박제 `948827ca` 대비 변경 — inspector 가 14th tick 진입 직전 §변경 이력 또는 본문 갱신 수행) + path-alias-resolver-coherence `f7ece98e7672299dc7763a9484c71b85e9968d0e4b02147a50fc8c84668bee50` (13th tick 박제 `3a7a98c7` 대비 변경 — inspector 가 14th tick 진입 직전 §변경 이력 또는 본문 갱신 수행). 양 hash 변경 → 13th 행동지침 (2)(4) 정체 카운트 누적 분기 미발동 (정체 카운트 리셋 분기 발동).
#     - working tree: clean (commit 267c449 직후 상태).
#     - 50.blocked/spec/foundation/: 13th tick 격리 산출물 (island-proptypes-removal + runtime-dep-version-coherence + _reason.md 각각) 잔존 — /revisit 미진행. RULE-05 정식 복귀 경로 유일 해소 방안 유지.
#     - 50.blocked/task/: 비어있음.
#     - 10.followups/: 비어있음 (13th tick 진척 유지).
#     - 20.req/: 1건 — `20260517-vite-jsx-transform-channel-coherence.md` (REQ-066 신규 discovery 흡수 산출물). inspector 미흡수, planner writer 영역 밖.
#     - 40.task/: 비어있음. TASK_READY_MAX=10 미달 (RULE-03).
#     - 60.done/2026/05/17/req/: 6건 누적 (13th tick 박제 시점 동일 — toolchain-version-coherence + runtime-dep-version-coherence + devbin-island-prop-types-removal + island-prop-types-removal + island-regression-guard-lint-block + husky-pre-push-typecheck-coverage + devbin-install-integrity + path-alias-resolver-coherence). REQ-066 미흡수 → 60.done/req/ 진입 0.
#     - 환경 회귀: 본 tick 은 typecheck / lint / pre-commit hook 실측 생략 (carve 보류 결정 — 게이트 재검증 불필요). 13th tick 박제 환경 회귀 (`@eslint/js` ESLint flat-config resolve 실패 + devbin install precondition 미충족) 잔존 가정.
#     - Task ID footprint 전수 (RULE-01 표준 + 10th 행동지침 (4) 권고 박제 — 50.blocked/task/ + 10.followups/ + 20.req/ + 60.done/ + 40.task/ 포함): `TSK-20260517-01`, `TSK-20260517-02`, `TSK-20260517-03` (13th tick 추가 — 격리 산출물 _reason.md 참조). `TSK-20260517-04~99` 미사용 — 신규 carve 가능 시 -04+ 부터 채번.
#   본 세션 처리:
#     - carve 0건: devbin-install-integrity (hash 변경 → 정체 카운트 리셋 1회차 → 0회차) + path-alias-resolver-coherence (hash 변경 → 정체 카운트 리셋 1회차 → 0회차). carve 보류 사유 (양 spec 공통):
#       (i) 수단 중립 메타 spec 패턴 — devbin §역할 line 10 박제 "`npm ci` / `npm install` / `pnpm` / `yarn` 등 install 수단 라벨 부여 (RULE-07 수단 중립) ... 환경 회복 운영 task 자체 (developer 영역)" + path-alias §역할 line 10 박제 "특정 alias 의 추가/삭제 자체 (RULE-07 수단 중립), alias prefix 컨벤션 결정 ... `eslint-plugin-import` resolver / `jsconfig.json` 도입 등 alias resolver 의 추가 채널 확장". 양 spec 모두 §역할 본문이 task 발행 산출물 카테고리를 명시적으로 배제.
#       (ii) developer writer 영역 밖 산출물 경계 충돌 — devbin 정상화 산출물 = `package.json` / `package-lock.json` / `node_modules/**` (npm install 결과) → developer writer 영역 (`src/`, `10.followups/`) 밖 / path-alias 정상화 산출물 = `vite.config.js` / `tsconfig.json` / `eslint.config.js` → developer writer 영역 밖. RULE-01 writer 매트릭스 충돌 (planner 가 task 발행 시 developer 영역 밖 변경 의무화 → RULE-02 fail-fast 발동 위험).
#       (iii) baseline 정합 — path-alias §스코프 규칙 gate (c)(d)(e) 모두 격차 0 정합 (대칭차 0, 타깃 동치, case 일치). 현 시점 위반 없음 → 정상화 task 자체 부재 (위반 baseline 존재해야 정상화 task 발행 가능). devbin baseline 은 (b)(c)(d)(e)(f)(g) 다수 위반 (binary 3건 부재 + module 5건 부재 + eslint 메이저 격차 1 + hook 진입 fail 2건) 이나 (i)(ii) 사유로 carve 부적합.
#     - 승격 0건: 양 spec 모두 unchecked `- [ ]` 다수 (테스트 현황 13~22개 + 수용 기준 9~10개 — 모두 미박제). 4조건 중 (b) unchecked == 0 + (d) 변경 이력 TSK 슬러그 박제 모두 불충족. green→blue 승격 불가.
#     - 50.blocked/spec/ 격리 0건 — 양 spec hash 변경 (정체 카운트 리셋) + RULE-07 정합 (평서형·시점 비의존·incident 귀속 부재·수단 중립).
#     - REQ-066 (vite-jsx-transform-channel-coherence) 는 inspector writer 영역 — planner 미접촉. inspector 흡수 → green spec 신규 박제 (vite JSX 변환 채널 정합 메타 spec) 가능. 흡수 시점 hash 변경 → carve 재진입 가능 (단 동일 수단 중립 메타 spec 패턴 검출 시 carve 보류 카테고리 재진입).
#   다음 세션 (15th tick) 행동지침:
#     (1) 20.req/20260517-vite-jsx-transform-channel-coherence.md (REQ-066) 는 inspector writer 영역 — planner 미접촉. inspector 흡수 → green spec 신규 박제 또는 기존 spec §변경 이력 추가 흐름 가능. 흡수 시점 hash 변경 → carve 재진입 가능 (단 수단 중립 메타 spec 패턴 검출 시 carve 보류 카테고리 진입).
#     (2) devbin-install-integrity hash 가 본 tick 박제 (9df5205d) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 15th tick 진입 시 정체 1회차 누적 → 16th tick 정체 2회차 → 격리 후보 도래. 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴 (§역할 line 10 박제), (b) developer writer 영역 밖 산출물 침투 우려, (c) 환경 회귀 미회복 + hash 2 tick 미변경. hash 변경 (inspector 가 §변경 이력 박제) 시 정체 카운트 리셋.
#     (3) path-alias-resolver-coherence hash 가 본 tick 박제 (f7ece98e) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 15th tick 진입 시 정체 1회차 누적 → 16th tick 정체 2회차 → 격리 후보 도래 (devbin 동일 패턴). 단 현 baseline 위반 0 (대칭차 0 + 격차 0 + case 일치) 이므로 정체 자체가 spec 의 "상시 정합" 효능 유지 신호로 재해석 가능 — 격리 결정 시 _reason.md 박제 시 본 분기 (위반 baseline 부재) 추가 검토. hash 변경 시 카운트 리셋.
#     (4) 양 spec 의 unchecked `- [ ]` 박제 진척 검출 시 (테스트 현황 / 수용 기준 체크 시) → green→blue 승격 후보 도래. 단 양 spec 모두 task 발행 산출물이 developer writer 영역 밖이므로 unchecked → checked 전환 자체가 RULE-01 writer 매트릭스 위반 가능성 — inspector 가 spec 본문 외 검증 (실제 vite.config.js / tsconfig.json / package.json 실측 변경 + task 발행) 없이 checked 처리 시 RULE-02 fail-fast 발동 위험. 본 분기는 inspector 의 책임 영역 — planner 는 unchecked 0 + 변경 이력 TSK 박제 충족 검출 시 승격 진행.
#     (5) 50.blocked/spec/foundation/ 잔존 (island-proptypes-removal + runtime-dep-version-coherence + _reason.md) 의 /revisit 처리 신호 검출 → revive 시 10.followups/ 진입 → discovery 재흡수 흐름 가능. close 시 60.done/2026/05/17/revisit/ 박제. 본 분기는 RULE-05 사람 운영자 영역 — planner 미접촉. 단 _reason.md 박제 사유 (수단 중립 메타 spec 패턴 + developer writer 영역 침투 우려) 와 동일 분기 검토 권고.
#     (6) Task ID footprint: TSK-20260517-{01,02,03} 사용 중. -04+ 부터 채번 가능. RULE-01 표준 grep 전수 (40.task/ + 60.done/ + 50.blocked/{task,spec,req}/ + 10.followups/ + 20.req/) 매 carve 직전 의무.
#     (7) 본 tick 박제 시점 baseline (HEAD=267c449) 와 15th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) devbin / path-alias spec hash 동일성 재실측, (b) 10.followups + 20.req 새 항목 검출 (REQ-067+ inspector 흡수 신호), (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (typecheck / lint / pre-commit hook full pipeline rc) 검출. 격차 항목 별 행동지침 (1)~(6) 분기 적용.
#     (8) RULE-07 정합 검증 (14th tick 박제): 본 tick 두 green spec (devbin / path-alias) 모두 §동작 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재·수단 중립 (§역할 line 10 박제) → RULE-07 정합 유지. carve 보류 사유는 RULE-07 위반 아님 — RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 분기. RULE-07 정합한 spec 이라도 RULE-01 writer 매트릭스 게이트로 carve 보류 가능 (13th 행동지침 (3) 박제 시계열 정합).
# 2026-05-17 planner session (15th tick @HEAD=2e8a731) — devbin / path-alias 양 spec hash 동일 + inspector 미터치 → 정체 1회차 누적 (14th 행동지침 (2)(3) 정합). carve 0건 (수단 중립 메타 spec 패턴 + developer writer 영역 밖 산출물 경계 충돌 유지). 격리 0건 (정체 2회차 도달 미달). 승격 0건 (unchecked 32 잔존, 박제 자체 정합). 50.blocked/spec/foundation/ 의 13th 격리 2건은 2e8a731 chore(revisit) 로 10.followups/ 정식 승격 완료 (RULE-05 정합).
#   재실행 baseline (HEAD=2e8a731 실측):
#     - green spec hash (SHA-1 `git hash-object` — 14th 박제 SHA-256 표기와 형식 차이 있으나 실제 file content 동일성은 git log 로 확정): devbin-install-integrity `948827ca8820c615f4f9a7ba5222a81d84776373` (마지막 inspector 커밋 89c7b5c 12th 시점 — 14th 박제 HEAD=267c449 이후 inspector 미터치 → 14th 박제 SHA-256 `9df5205d` 와 file content 동일) + path-alias-resolver-coherence `3a7a98c7ee4af262895defccf75b84f64563d525` (마지막 inspector 커밋 35ba9d4 13th 시점 — 14th 박제 이후 inspector 미터치 → 14th 박제 SHA-256 `f7ece98e` 와 file content 동일). 두 spec content 14th tick 박제 이후 미변경 검증 완료.
#     - working tree: clean (2e8a731 chore(revisit) 커밋 직후 — 14th 박제 시점 cat 직후 4 deletion staged 였으나 commit 2e8a731 로 박제 완료. 본 세션 진입 시점 working tree clean → RULE-02 "세션 시작 자기 영역 밖 staged 항목 unstage" 의무 발동 없음).
#     - 50.blocked/{task,spec}/: **비어있음** — 13th 격리 2건 (island-proptypes-removal + runtime-dep-version-coherence + 각각 _reason.md, 총 4파일) 이 2e8a731 chore(revisit) 로 10.followups/ 정식 승격 + 원본+reason 삭제 완료 (RULE-05 revive 경로 정합).
#     - 10.followups/: 2건 — `20260517-0058-island-proptypes-removal-from-blocked.md` + `20260517-0058-runtime-dep-version-coherence-from-blocked.md`. discovery 미흡수 — planner writer 영역 밖 (RULE-01 writer 매트릭스 정합 — `10.followups/` create 권한은 developer 한정, planner 는 read only).
#     - 20.req/: 1건 — `20260517-vite-jsx-transform-channel-coherence.md` (REQ-066 신규 — 14th 박제 동일). inspector 미흡수, planner writer 영역 밖.
#     - 40.task/: **비어있음**. TASK_READY_MAX=10 미달 (RULE-03 충족 — 본 tick carve 0건이라 임계치 무관).
#     - 60.done/2026/05/17/: req 7건 + followups 다수 + revisit 폴더는 본 tick 진입 시점 부재 (2e8a731 chore(revisit) 는 revive 처리로 — close 처리 시에만 60.done/revisit/ 박제).
#     - 환경 회귀: 본 tick 은 typecheck / lint / pre-commit hook 실측 생략 (carve 보류 결정 — 게이트 재검증 불필요). 14th 박제 환경 회귀 (`@eslint/js` ESLint flat-config resolve 실패 + devbin install precondition 미충족) 잔존 가정.
#     - Task ID footprint 전수 (RULE-01 표준 + 10th-12th-14th 권고 박제 — 40.task/ + 60.done/ + 50.blocked/{task,spec,req}/ + 10.followups/ + 20.req/ 포함): `TSK-20260517-{01,02,03}` 점유 (14th 박제 동일 — 본 tick 신규 carve 0건이라 ID 발행 없음). `TSK-20260517-04~99` 미사용 — 신규 carve 가능 시 -04 부터 채번 가능.
#   본 세션 처리:
#     - carve 0건: devbin-install-integrity (hash 동일 + 정체 1회차 누적) + path-alias-resolver-coherence (hash 동일 + 정체 1회차 누적). carve 보류 사유 (양 spec 공통 — 14th 박제 유지):
#       (i) 수단 중립 메타 spec 패턴 — devbin §역할 line 10 박제 "`npm ci` / `npm install` / `pnpm` / `yarn` 등 install 수단 라벨 부여 (RULE-07 수단 중립) ... 환경 회복 운영 task 자체 (developer 영역)" + path-alias §역할 line 10 박제 "특정 alias 의 추가/삭제 자체 (RULE-07 수단 중립), alias prefix 컨벤션 결정 ... `eslint-plugin-import` resolver / `jsconfig.json` 도입 등 alias resolver 의 추가 채널 확장". 양 spec 모두 §역할 본문이 task 발행 산출물 카테고리를 명시적으로 배제.
#       (ii) developer writer 영역 밖 산출물 경계 충돌 — devbin 정상화 산출물 = `package.json` / `package-lock.json` / `node_modules/**` (npm install 결과) → developer writer 영역 (`src/`, `10.followups/`) 밖 / path-alias 정상화 산출물 = `vite.config.js` / `tsconfig.json` / `eslint.config.js` → developer writer 영역 밖. RULE-01 writer 매트릭스 충돌 (planner 가 task 발행 시 developer 영역 밖 변경 의무화 → RULE-02 fail-fast 발동 위험).
#       (iii) baseline 정합 — path-alias §스코프 규칙 gate (c)(d)(e) 모두 격차 0 정합 (대칭차 0, 타깃 동치, case 일치 — 사용자 메시지 컨텍스트 (3) 박제 "baseline 격차 0 정합 (위반 자체 부재)" 와 일치). 현 시점 위반 없음 → 정상화 task 자체 부재 (위반 baseline 존재해야 정상화 task 발행 가능). devbin baseline 은 (b)(c)(d)(e)(f)(g) 다수 위반 가정 (12th-13th 박제 시점 유지) 이나 (i)(ii) 사유로 carve 부적합.
#       (iv) 환경 회귀 미회복 — devbin 의 hook 진입 fail / lint resolve 실패 등 baseline 위반 자체가 환경 회복 의존 — carve 시점 즉시 50.blocked/task/ 재진입 예측.
#     - 격리 0건: 양 spec 정체 1회차 누적 — 14th 행동지침 (2)(3) "16th tick 정체 2회차 → 격리 후보 도래" 시계열 충족 미달. 본 tick 은 1회차 진입 → 격리 부적합.
#     - 승격 0건: 양 spec 모두 unchecked `- [ ]` 32건 잔존 (실측: devbin 32 + path-alias 32) → RULE-01 승격 4조건 (`[WIP]` 0 + unchecked 0 + `^#+ .*To-Be` 0 + 변경 이력 박제) 중 unchecked == 0 위반. 박제 자체 정합 (`[WIP]` 0 hit + `To-Be` 0 hit + 변경 이력 1 hit 모두 정합 — 실측 4조건 중 (a)(c)(d) 충족, (b) 위반).
#     - REQ-066 (vite-jsx-transform-channel-coherence) 는 inspector writer 영역 — planner 미접촉. inspector 흡수 → green spec 신규 박제 또는 기존 spec §변경 이력 추가 흐름 가능. 흡수 시점 hash 변경 → carve 재진입 가능 (단 동일 수단 중립 메타 spec 패턴 검출 시 carve 보류 카테고리 재진입).
#     - 10.followups/ 의 2 from-blocked 항목은 discovery writer 영역 — planner 미접촉. discovery 흡수 → 20.req/ 발행 → inspector spec 재흡수 흐름 가능. 단 island-proptypes-removal 의 후속 발행은 RULE-02 재시도 금지 (동일 spec 본문 직접 재진입 부적합 — REQ 갱신 후에만 정합) + Task ID 산정 모호 (TSK-20260517-01 영구 박제 후 -04+ 채번 정합) 분기 검토 필요. runtime-dep-version-coherence 후속 발행은 수단 중립 메타 spec 패턴 (REQ 갱신 시점 수단 라벨 검토 의무).
#   다음 세션 (16th tick) 행동지침:
#     (1) 20.req/20260517-vite-jsx-transform-channel-coherence.md (REQ-066) inspector 흡수 신호 검출 시 → 신규 green spec 진입 → carve 절차 재진입 (RULE-01 Task ID 전수 grep + RULE-06 §스코프 규칙 grep-baseline 실측 박제 + grep dry-run 검증 + RULE-07 정합 검증). 단 수단 중립 메타 spec 패턴 (vite.config.js 변경 의무화 시) 검출 시 carve 보류 카테고리 진입.
#     (2) devbin-install-integrity hash 가 본 tick 박제 (SHA-1 948827ca / 14th SHA-256 9df5205d) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 16th tick 진입 시 정체 2회차 누적 도달 → 격리 후보 도래. 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴 (§역할 line 10 박제), (b) developer writer 영역 밖 산출물 침투 우려, (c) 환경 회귀 미회복 + hash 3 tick 미변경, (d) RULE-05 정식 복귀 경로 (/revisit) 만 해소 가능. hash 변경 (inspector 가 §변경 이력 박제 또는 본문 갱신) 시 정체 카운트 리셋 → 다음 carve 재진입 가능.
#     (3) path-alias-resolver-coherence hash 가 본 tick 박제 (SHA-1 3a7a98c7 / 14th SHA-256 f7ece98e) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 16th tick 진입 시 정체 2회차 누적 도달 → 격리 후보 도래 (devbin 동일 패턴). 단 현 baseline 위반 0 (대칭차 0 + 격차 0 + case 일치) 이므로 격리 결정 시 _reason.md 박제 시 본 분기 (위반 baseline 부재 = spec 의 "상시 정합" 효능 유지 신호로 재해석 가능) 추가 명시 권고. hash 변경 시 카운트 리셋.
#     (4) 양 spec 의 unchecked `- [ ]` 박제 진척 검출 시 (테스트 현황 / 수용 기준 체크 시) → green→blue 승격 후보 도래. 단 양 spec 모두 task 발행 산출물이 developer writer 영역 밖이므로 unchecked → checked 전환 자체가 RULE-01 writer 매트릭스 위반 가능성 — inspector 가 spec 본문 외 검증 (실제 vite.config.js / tsconfig.json / package.json 실측 변경 + task 발행) 없이 checked 처리 시 RULE-02 fail-fast 발동 위험. 본 분기는 inspector 의 책임 영역 — planner 는 unchecked 0 + 변경 이력 TSK 박제 충족 검출 시 승격 진행.
#     (5) 10.followups/ 의 island + runtime from-blocked 2건 discovery 흡수 신호 검출 → 20.req/ 발행 후에야 inspector → green spec 재진입 흐름 가능. island 후속은 RULE-02 재시도 금지 분기 검토 의무 (동일 본문 재진입 부적합). runtime 후속은 수단 중립 메타 spec 패턴 재발 가능성.
#     (6) Task ID footprint: TSK-20260517-{01,02,03} 사용 중. -04+ 부터 채번 가능. RULE-01 표준 grep 전수 (40.task/ + 60.done/ + 50.blocked/{task,spec,req}/ + 10.followups/ + 20.req/) 매 carve 직전 의무. carve 0건 tick 은 채번 발행 없음.
#     (7) 본 tick 박제 시점 baseline (HEAD=2e8a731) 와 16th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) 양 green spec hash 동일성 재실측 (SHA-1 git hash-object 표준), (b) 10.followups + 20.req 새 항목 검출, (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (typecheck / lint / pre-commit hook full pipeline rc) 검출. 격차 항목 별 행동지침 (1)~(6) 분기 적용.
#     (8) RULE-07 정합 검증 (15th tick 박제): 본 tick 두 green spec (devbin / path-alias) 모두 §동작 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재·수단 중립 (§역할 line 10 박제) → RULE-07 정합 유지. carve 보류 사유는 RULE-07 위반 아님 — RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 분기 (13th-14th 박제 유지). RULE-07 정합한 spec 이라도 RULE-01 writer 매트릭스 게이트로 carve 보류 가능.
#     (9) Hash 표기 정합 (14th 박제 SHA-256 vs 본 tick SHA-1): 본 tick 부터 SHA-1 `git hash-object` 표준 사용 권고 (40자 hex). 14th 박제 SHA-256 (64자) 은 별도 hash 도구 산출 — 표기 차이는 inspector content 변경 여부 검증의 보조 신호로 무력화 (git log --follow + last inspector commit 시점 검증이 권위 신호). 16th tick 부터 SHA-1 단일 표준 박제.
# 2026-05-17 planner session (16th tick @HEAD=b42e36fc) — devbin / path-alias 양 spec hash 동일 + inspector 미터치 → 정체 2회차 누적 도달 (14th 행동지침 (2)(3) 및 15th 행동지침 (2)(3) "16th tick 정체 2회차 → 격리 후보 도래" 시계열 충족) → 양 spec 모두 50.blocked/spec/foundation/ 격리 + _reason.md 박제 (총 2 spec + 2 reason = 4 파일 신규). vite-jsx-transform-channel-coherence (REQ-066 흡수, working tree untracked — inspector 신규 박제) 1차 노출, carve 부적합 (동일 수단 중립 메타 spec 패턴 + developer writer 영역 밖 산출물 = vite.config.js + REQ-064 충족 후에야 §동작 3 측정 활성) → 정체 0회차 진입, 격리 부적합. carve 0건, 승격 0건, 격리 2건.
#   재실행 baseline (HEAD=b42e36fc 실측):
#     - green spec hash (SHA-1 git hash-object 16th 표준): devbin-install-integrity `948827ca8820c615f4f9a7ba5222a81d84776373` (15차 박제 동일 — 마지막 inspector 커밋 89c7b5c 12차 시점 박제 이후 미터치 확정) + path-alias-resolver-coherence `3a7a98c7ee4af262895defccf75b84f64563d525` (15차 박제 동일 — 마지막 inspector 커밋 35ba9d4 13차 시점 박제 이후 미터치 확정). 양 spec 정체 2회차 도달 → 격리 결정 (15th 행동지침 (2)(3) 정합).
#     - vite-jsx-transform-channel-coherence (신규, untracked working tree) hash `a1f681de9463abd2fc8ec837556badc1321fd28f` — inspector 가 REQ-066 흡수 신규 박제 (본 spec 본문 §역할 line 10 박제 "의도적으로 하지 않는 것: 세 채널 중 어느 채널의 제거/유지/추가 결정 (RULE-07 수단 중립)" — devbin / path-alias / runtime 와 동일 수단 중립 메타 spec 패턴). working tree untracked 상태 (inspector commit pending). planner writer 영역 밖 (RULE-01 정합 — planner 는 `30.spec/green/F → 30.spec/blue/F` 승격 mv + `30.spec/** → 50.blocked/spec/` 격리 mv 만, green 자체 create/edit 권한 부재). 미접촉.
#     - working tree: 본 격리 mv 직전 — 본 tick 실행 후 mv (devbin + path-alias green → blocked) + _reason.md 2건 신규 create.
#     - 50.blocked/{task,spec}/: 본 tick 격리 결과로 devbin-install-integrity.md + path-alias-resolver-coherence.md + 각각 _reason.md (총 4파일) 신규 박제.
#     - 10.followups/: 2건 — island-proptypes-removal-from-blocked.md + runtime-dep-version-coherence-from-blocked.md (15차 박제 동일 — discovery 미흡수 잔존). planner writer 영역 밖.
#     - 20.req/: 1건 — `20260517-test-discovery-population-coherence.md` (REQ-067 신규 — 15차 박제 REQ-066 흡수 완료 후 신규 discovery 입수). inspector 미흡수, planner writer 영역 밖.
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 정합 — 본 tick carve 0건이라 임계치 무관).
#     - 60.done/2026/05/17/: REQ-066 흡수 완료로 req 8건 누적 (15차 박제 7건 + REQ-066 mv 1건 추가 — 추정).
#     - 환경 회귀: 14차 박제 환경 회귀 잔존 가정 (`@eslint/js` ESLint flat-config resolve 실패 + devbin install precondition 미충족 — 본 tick 실측 생략, 격리 결정 게이트 재검증 불필요).
#     - Task ID footprint 전수 (RULE-01 표준 + 14차 권고 박제): `TSK-20260517-{01,02,03}` 점유 (15차 박제 동일 — 본 tick 신규 carve 0건이라 ID 발행 없음). `TSK-20260517-04~99` 미사용.
#   본 세션 처리:
#     - 격리 2건:
#       (i) devbin-install-integrity → 50.blocked/spec/foundation/devbin-install-integrity.md + _reason.md. 사유 박제 (15th 행동지침 (2) 가이드 정합):
#         (a) 수단 중립 메타 spec 패턴 — §역할 line 10 박제 "`npm ci` / `npm install` / `pnpm` / `yarn` 등 install 수단 라벨 부여 (RULE-07 수단 중립) ... 환경 회복 운영 task 자체 (developer 영역)".
#         (b) developer writer 영역 밖 산출물 경계 충돌 — devbin 정상화 산출물 = package.json / package-lock.json / node_modules/** → developer writer 영역 (src/, 10.followups/) 밖. RULE-01 writer 매트릭스 충돌 + RULE-02 fail-fast 발동 위험 (carve 시점 자체가 RULE-02 멱등 위반 — 예측 가능한 fail 을 발행).
#         (c) 환경 회귀 미회복 + hash 3 tick 미변경 (12차 inspector 박제 89c7b5c 이후 13th-16th tick 동안 inspector 미터치).
#         (d) RULE-05 정식 복귀 경로 (/revisit) 만 해소 가능 — RULE-07 정합 spec 의 격리 사유는 (i) writer 매트릭스 + (ii) 환경 회귀 + (iii) hash 정체 (재시도 금지 분기).
#       (ii) path-alias-resolver-coherence → 50.blocked/spec/foundation/path-alias-resolver-coherence.md + _reason.md. 사유 박제 (15th 행동지침 (3) 가이드 정합 — 위반 baseline 부재 분기 추가 명시):
#         (a) 수단 중립 메타 spec 패턴 — §역할 line 10 박제 "특정 alias 의 추가/삭제 자체 (RULE-07 수단 중립), alias prefix 컨벤션 결정 ... `eslint-plugin-import` resolver / `jsconfig.json` 도입 등 alias resolver 의 추가 채널 확장".
#         (b) developer writer 영역 밖 산출물 경계 충돌 — path-alias 정상화 산출물 = vite.config.js / tsconfig.json / eslint.config.js → developer writer 영역 밖. RULE-01 writer 매트릭스 충돌.
#         (c) 현 baseline 위반 0 정합 (gate (c)(d)(e) 격차 0, 대칭차 0, 타깃 동치, case 일치) → 정상화 task 자체 부재 (위반 baseline 존재해야 정상화 task 발행 가능). 본 분기는 정체 자체가 spec 의 "상시 정합 효능 유지" 신호로 재해석 가능 (15th 행동지침 (3) 박제 분기 정합).
#         (d) 환경 회귀 미회복 + hash 3 tick 미변경 (13차 inspector 박제 35ba9d4 이후 14th-16th tick 동안 inspector 미터치).
#         (e) RULE-05 정식 복귀 경로 (/revisit) 만 해소 가능 — close 처리가 자연스러움 (위반 baseline 부재로 정상화 task 발행 진입로 부재).
#     - carve 0건: vite-jsx-transform-channel-coherence (REQ-066 흡수 신규, 1차 노출, working tree untracked 상태 — planner 미접촉 가능). carve 보류 사유 (devbin / path-alias 공통 패턴):
#       (i) 수단 중립 메타 spec 패턴 — §역할 line 10 박제 "세 채널 중 어느 채널의 제거/유지/추가 결정 (RULE-07 수단 중립)" — devbin / path-alias / runtime 와 동일 메타 spec 패턴.
#       (ii) developer writer 영역 밖 산출물 경계 충돌 — vite.config.js 변경 = developer writer 영역 (src/) 밖.
#       (iii) §동작 3 (jsx-runtime import 횟수 측정 baseline) 은 REQ-064 (devbin install) 충족 후에야 활성 — 현 시점 npm run build 채널 비활성 (node_modules/vite 부재).
#       (iv) 본 spec 정체 0회차 진입 → 격리 부적합. 다음 tick (17th) 진입 시 hash 동일 + 환경 회귀 미회복 시 정체 1회차 누적 → 18th tick 정체 2회차 도달 시 격리 후보 도래 (devbin 13th-14th-15th-16th + path-alias 13th-14th-15th-16th 시계열 동일 패턴).
#     - 승격 0건: devbin / path-alias / vite-jsx 모두 unchecked `- [ ]` 다수 잔존. 격리 spec 2건은 박제 자체 정합 (`[WIP]` 0 + `To-Be` 0 + 변경 이력 1 hit), unchecked == 0 위반으로 승격 4조건 불충족. vite-jsx 도 §테스트 현황 28 + §수용 기준 12 = 40 unchecked.
#   다음 세션 (17th tick) 행동지침:
#     (1) vite-jsx-transform-channel-coherence hash 가 본 tick 박제 (SHA-1 a1f681de) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 17th tick 진입 시 정체 1회차 누적 → 18th tick 정체 2회차 → 격리 후보 도래 (devbin / path-alias 동일 패턴). 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴 (§역할 line 10 박제), (b) developer writer 영역 밖 산출물 경계 충돌 (vite.config.js), (c) REQ-064 충족 후에야 §동작 3 측정 활성 (devbin install precondition 의존), (d) RULE-05 정식 복귀 경로 (/revisit) 만 해소 가능. hash 변경 시 카운트 리셋.
#     (2) 50.blocked/spec/foundation/ 본 tick 격리 2건 (devbin + path-alias + 각각 _reason.md) 의 /revisit 처리 신호 검출 → revive 시 10.followups/ 진입 → discovery 재흡수 흐름 가능. close 시 60.done/2026/05/17/revisit/ 박제. 본 분기는 RULE-05 사람 운영자 영역 — planner 미접촉. path-alias 의 _reason.md 박제 (c) 분기 (위반 baseline 부재 = 상시 정합 효능 유지) 는 close 처리가 자연스러움 (정상화 task 발행 진입로 부재).
#     (3) 20.req/20260517-test-discovery-population-coherence.md (REQ-067) inspector 흡수 신호 검출 시 → 신규 green spec 진입 또는 기존 spec §변경 이력 추가 → carve 절차 재진입 (RULE-01 Task ID 전수 grep + RULE-06 §스코프 규칙 grep-baseline 실측 박제 + grep dry-run 검증 + RULE-07 정합 검증). REQ-067 의 효능 영역 (test discovery population 정합 — vitest test 파일 발견 / coverage include / fileParallelism) 추정 시 본 효능은 src/**/*.test.{js,jsx} 변경 가능성 → developer writer 영역 (src/) 내부 → carve 가능 카테고리 추정 (단 vitest 측 검증 게이트가 환경 회귀 의존 시 별 분기).
#     (4) 10.followups/ 의 island + runtime from-blocked 2건 discovery 흡수 신호 검출 → 20.req/ 발행 후에야 inspector → green spec 재진입 흐름 가능. island 후속은 RULE-02 재시도 금지 분기 검토 의무 (동일 본문 재진입 부적합). runtime 후속은 수단 중립 메타 spec 패턴 재발 가능성.
#     (5) Task ID footprint: TSK-20260517-{01,02,03} 사용 중. -04+ 부터 채번 가능. RULE-01 표준 grep 전수 (40.task/ + 60.done/ + 50.blocked/{task,spec,req}/ + 10.followups/ + 20.req/) 매 carve 직전 의무.
#     (6) 본 tick 박제 시점 baseline (HEAD=b42e36fc) 와 17th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) vite-jsx hash 동일성 재실측 (SHA-1 git hash-object 표준), (b) 10.followups + 20.req 새 항목 검출 (REQ-068+), (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호 — 본 tick 격리 2건 + 기존 잔존), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (typecheck / lint / pre-commit hook full pipeline rc) 검출. 격차 항목 별 행동지침 (1)~(5) 분기 적용.
#     (7) RULE-07 정합 검증 (16th tick 박제): 본 tick 격리 2 spec (devbin / path-alias) + 신규 green 1 spec (vite-jsx) 모두 §동작 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재·수단 중립 (§역할 line 10 박제) → RULE-07 정합 유지. 격리 사유는 RULE-07 위반 아님 — RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 + RULE-02 재시도 금지 (hash 정체) 분기 (13th-15th 박제 유지).
#     (8) 본 tick 격리 시계열 패턴 누적 정합: toolchain-version-coherence (5th-7th 격리, /revisit followup 완료) → island-proptypes-removal (9th-13th 격리, 2e8a731 chore(revisit) revive 완료) → runtime-dep-version-coherence (11th-13th 격리, 2e8a731 revive 완료) → devbin-install-integrity (16th 격리, 본 tick) → path-alias-resolver-coherence (16th 격리, 본 tick). 5 spec 모두 수단 중립 메타 spec 패턴 + developer writer 영역 밖 산출물 경계 충돌 + hash 정체 2~5회차 누적 + 환경 회귀 미회복 시계열. vite-jsx-transform-channel-coherence 가 동일 패턴 6번째 카테고리 진입 가능성 (다음 tick).
# 2026-05-17 planner session (17th tick @HEAD=33c71a8) — vite-jsx-transform-channel-coherence hash 동일 + inspector 미터치 → 정체 1회차 누적 (16th 행동지침 (1) 정합, 18th tick 정체 2회차 도래 시 격리 후보). test-discovery-population-coherence (REQ-067 흡수 신규, untracked working tree — inspector commit pending) 1차 노출. eslint-linter-options-default-override (REQ-068 흡수 신규, untracked working tree — inspector commit pending) 1차 노출 — 16th 행동지침 (3) REQ-067 흡수 분기 외에 REQ-068 추가 흡수 검출. carve 0건, 승격 0건, 격리 0건.
#   재실행 baseline (HEAD=33c71a8 실측):
#     - green spec hash (SHA-1 git hash-object):
#       (i) vite-jsx-transform-channel-coherence `a1f681de9463abd2fc8ec837556badc1321fd28f` (16차 박제 동일 — 마지막 inspector 커밋 64babbd 16차 시점 박제 이후 미터치 확정). 정체 1회차 누적 (16th-17th tick 동일 hash).
#       (ii) test-discovery-population-coherence `84e254b87a3d91b28f95395718f563fda443c36e` (신규, untracked). inspector commit 시점 미박제 (working tree 잔존) — 본 spec §변경 이력 `(this commit)` placeholder 박제 상태. RULE-01 writer 경계 정합 (planner 미터치).
#       (iii) eslint-linter-options-default-override `5dbfa413e74bef3cc7183aeb006f916499ff375b` (신규, untracked). 동일 untracked 상태. discovery 측 REQ-068 ID 중복 박제 (`log-island-convergence` 와 동시 — spec §최상단 ID 충돌 주의 박제 — 차기 discovery 재배정 신호).
#     - 50.blocked/spec/foundation/: 비어있음 — 16th tick 격리 2건 (devbin + path-alias) /revisit 처리 완료 (33c71a8 chore(revisit): scanned=2, followup=1, deleted=2 — 1건 revive 후 followups 진입 + 1건 close). 16th 행동지침 (2) 분기 (path-alias close 자연스러움) 정합 — close 처리 완료 + devbin revive 처리 완료.
#     - 10.followups/: 1건 — `20260517-0117-devbin-install-integrity-from-blocked.md` (16th 격리 devbin → revive 결과물). discovery 흡수 대기. planner writer 영역 밖.
#     - 20.req/: 5건 — `20260517-eslint-linter-options-default-override.md` (REQ-068 — inspector 흡수 완료 / green 박제됨) + `20260517-log-island-convergence.md` (REQ-068 ID 중복 — inspector 미흡수) + `20260517-monitor-island-convergence.md` (inspector 미흡수) + `20260517-root-entry-island-convergence.md` (inspector 미흡수) + `20260517-test-discovery-population-coherence.md` (REQ-067 — inspector 흡수 완료 / green 박제됨). inspector 미흡수 3건 (log-island / monitor-island / root-entry-island) planner writer 영역 밖.
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 정합 — 본 tick carve 0건이라 임계치 무관).
#     - 60.done/2026/05/17/req/: 8건 누적 (devbin / husky-pre-push / island-prop-types / island-regression / path-alias / runtime-dep / toolchain / vite-jsx). REQ-067 / REQ-068 의 mv 는 inspector 후속 처리 예정.
#     - 환경 회귀: `node_modules/vite` ABSENT + `node_modules/typescript` EXIST. devbin install precondition 부분 회복 (typescript 만) — `npm run build` 채널 비활성 잔존. vite-jsx §동작 3 production build 측정 baseline 활성 불가.
#     - Task ID footprint 전수 (RULE-01 표준 grep `specs/{40.task,60.done,50.blocked,50.blocked/task,10.followups,20.req}/` 실행 결과): `TSK-20260517-{01,02,03}` 점유 (16차 박제 동일 — 본 tick 신규 carve 0건이라 ID 발행 없음). `TSK-20260517-04~99` 미사용.
#   본 세션 처리:
#     - carve 0건: 세 green spec (vite-jsx / test-discovery / eslint-linter-options) 모두 carve 보류. 공통 사유 (15th-16th 박제 정합):
#       (i) 수단 중립 메타 spec 패턴 — vite-jsx §역할 line 10 + test-discovery §역할 line 10 + eslint-linter-options §역할 line 11 모두 RULE-07 수단 중립 자기 박제.
#       (ii) developer writer 영역 밖 산출물 경계 충돌 — vite-jsx → `vite.config.js`, test-discovery → `eslint.config.js`/`tsconfig.json`/`vite.config.js`/`package.json` 4축, eslint-linter-options → `eslint.config.js`. 모두 src/ 밖.
#       (iii) vite-jsx §동작 3 production build 측정 baseline 은 REQ-064 (devbin install) 의 `node_modules/vite` 충족 후 활성 — 본 tick 실측 ABSENT 잔존.
#       (iv) test-discovery / eslint-linter-options 는 1차 노출 (정체 0회차) + untracked working tree (inspector commit 시점 미박제). 16th 행동지침 (3) 의 REQ-067 흡수 신호 검출 분기 (test-discovery 효능 영역 → developer writer 영역 src/ 내부 carve 가능 카테고리 추정) 재검토: 본 spec §스코프 규칙 expansion "N/A (본 spec 은 grep 게이트 측정 baseline 박제 spec — task 발행 시 본 baseline 을 §스코프 규칙 grep-baseline 으로 복제)" 명시 — task 발행 자동 트리거 부재. §동작 4 (신규 루트 추가 fail 신호) + §동작 5 (vitest 메이저 bump fail-safe) 모두 **미래 활성 게이트** 박제 — 현 시점 task 발행 의무 0.
#       (v) vite-jsx 정체 1회차 누적 (격리 2회차 미도달). test-discovery + eslint-linter-options 정체 0회차 (1차 노출).
#     - 승격 0건: 세 spec 모두 unchecked `- [ ]` 잔존 (vite-jsx §테스트 현황 28 + §수용 기준 12 = 40 unchecked / test-discovery §테스트 현황 2 + §수용 기준 9 = 11 unchecked / eslint-linter-options 미실측). 변경 이력 `(this commit)` placeholder 박제 (vite-jsx 는 64babbd commit 박제 완료, test-discovery / eslint-linter-options 는 placeholder 잔존). 승격 4조건 (unchecked == 0) 위반.
#     - 격리 0건: vite-jsx 정체 1회차 (≥2회차 미도달). test-discovery + eslint-linter-options 1차 노출. 격리 후보 도래 부재.
#   다음 세션 (18th tick) 행동지침:
#     (1) vite-jsx-transform-channel-coherence hash 가 본 tick 박제 (SHA-1 a1f681de) 와 동일 + 환경 회귀 미회복 (node_modules/vite ABSENT) + inspector 미터치 시 → 18th tick 진입 시 정체 2회차 도달 → 격리 후보 도래. 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴 (§역할 line 10 박제), (b) developer writer 영역 밖 산출물 경계 충돌 (vite.config.js), (c) REQ-064 충족 후에야 §동작 3 측정 활성 (node_modules/vite ABSENT precondition 미충족), (d) RULE-05 정식 복귀 경로 (/revisit) 만 해소 가능. hash 변경 (inspector §변경 이력 박제 또는 본문 갱신) 시 카운트 리셋. 운영자의 `npm install` (또는 devbin install 정상화) 산출물 검출 시 § 동작 3 baseline 활성 → carve 재진입 가능.
#     (2) test-discovery-population-coherence hash 가 본 tick 박제 (SHA-1 84e254b8) 와 동일 + inspector commit 완료 (untracked → tracked 전이) 시 → 정체 1회차 누적 (carve 보류 분기 유지). hash 변경 + inspector commit 완료 시 카운트 리셋. 본 spec 의 §동작 4 (신규 루트 `*.test.*` 추가) / §동작 5 (vitest 메이저 bump) 는 미래 활성 fail 신호 — 현재 시점 carve 트리거 부재. 19th tick 정체 2회차 도달 시 (vite-jsx 동일 패턴) 격리 후보 도래.
#     (3) eslint-linter-options-default-override hash 가 본 tick 박제 (SHA-1 5dbfa413) 와 동일 + inspector commit 완료 (untracked → tracked 전이) 시 → 정체 1회차 누적. hash 변경 + inspector commit 완료 시 카운트 리셋. ID 충돌 분기 (REQ-068 = eslint-linter-options + log-island-convergence 동시 박제) → 차기 discovery 세션 재배정 신호. 19th tick 정체 2회차 도달 시 격리 후보 도래.
#     (4) 20.req/ 미흡수 3건 (log-island / monitor-island / root-entry-island) inspector 흡수 신호 검출 시 → 신규 green spec 진입 → carve 절차 재진입. island 계열 후속은 RULE-02 재시도 금지 분기 검토 의무 (동일 본문 재진입 부적합 — island-proptypes-removal 9th-13th 격리 시계열 + 2e8a731 revive 완료 패턴 참조).
#     (5) 10.followups/ devbin from-blocked 1건 discovery 흡수 신호 검출 → 20.req/ 발행 후 inspector → green spec 재진입 흐름 가능. devbin 후속은 수단 중립 메타 spec 패턴 재발 가능성 (devbin-install-integrity 12th 박제 시점 §역할 line 10 박제 정합).
#     (6) Task ID footprint: TSK-20260517-{01,02,03} 사용 중. -04+ 부터 채번 가능. RULE-01 표준 grep 전수 매 carve 직전 의무.
#     (7) 본 tick 박제 시점 baseline (HEAD=33c71a8) 와 18th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) 세 green spec hash 동일성 재실측 (SHA-1 git hash-object 표준), (b) 10.followups + 20.req 새 항목 검출 (REQ-069+), (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (`node_modules/vite` 존재 / `npm run build` exit 0) 검출. 격차 항목 별 행동지침 (1)~(6) 분기 적용.
#     (8) RULE-07 정합 검증 (17th tick 박제): 본 tick 세 green spec (vite-jsx / test-discovery / eslint-linter-options) 모두 §동작 평서형·반복 검증 가능·시점 비의존·incident 귀속 부재·수단 중립 (§역할 박제) → RULE-07 정합 유지. carve 보류 사유는 RULE-07 위반 아님 — RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 + RULE-06 grep dry-run 환경 의존 (vite-jsx §동작 3) + 1차 노출 (test-discovery / eslint-linter-options) 분기.
#     (9) 본 tick 격리 시계열 패턴 누적 (16th 박제 5 spec 확장): toolchain (5th-7th 격리, /revisit followup 완료) → island-proptypes (9th-13th 격리, 2e8a731 revive) → runtime-dep (11th-13th 격리, 2e8a731 revive) → devbin (16th 격리, 33c71a8 revive) → path-alias (16th 격리, 33c71a8 close) → **vite-jsx-transform-channel-coherence** (18th 격리 후보 도래 — 본 tick 17th 정체 1회차 + 환경 회귀 미회복 잔존 시계열). 6 spec 모두 동일 패턴 (수단 중립 메타 spec + developer 영역 밖 산출물 + hash 정체 + 환경 회귀). test-discovery + eslint-linter-options 는 19th-20th tick 진입 시 동일 패턴 7-8번째 카테고리 진입 가능성.
# 2026-05-17 planner session (18th tick @HEAD=24183430) — vite-jsx-transform-channel-coherence hash 동일 + inspector 미터치 + 환경 회귀 미회복 → 정체 2회차 도달 (17th 행동지침 (1) 정합) → 50.blocked/spec/foundation/ 격리 + _reason.md 박제 (총 2 파일 신규). test-discovery-population-coherence / eslint-linter-options-default-override hash 동일 + inspector commit 미박제 (untracked 잔존) → 정체 1회차 누적 (carve 보류 분기 유지). log-island-convergence / monitor-island-convergence / root-entry-island-convergence (3 신규 untracked — 17th 행동지침 (4) REQ-068+ 흡수 신호 검출) 1차 노출. carve 0건, 승격 0건, 격리 1건.
#   재실행 baseline (HEAD=24183430 실측):
#     - green spec hash (SHA-1 git hash-object 18th 표준):
#       (i) vite-jsx-transform-channel-coherence `a1f681de9463abd2fc8ec837556badc1321fd28f` (16th-17th-18th tick 동일 — inspector 64babbd 박제 이후 미터치 3 tick). 정체 2회차 도달 → 격리 결정.
#       (ii) test-discovery-population-coherence `84e254b87a3d91b28f95395718f563fda443c36e` (17th-18th tick 동일, untracked). 정체 1회차 누적 (carve 보류 분기 유지). 19th tick 정체 2회차 도달 시 격리 후보 도래.
#       (iii) eslint-linter-options-default-override `5dbfa413e74bef3cc7183aeb006f916499ff375b` (17th-18th tick 동일, untracked). 정체 1회차 누적. 19th tick 정체 2회차 도달 시 격리 후보 도래. REQ-068 ID 충돌 분기 (log-island 와 동시 박제) 잔존 — 차기 discovery 재배정 신호.
#       (iv) log-island-convergence `9d8e1b43373647dbdff5810893af964ee67af4b1` (신규, untracked — 17th tick 미검출 / 18th tick 1차 노출). REQ-068 ID 중복 박제 (eslint-linter-options 와 동시). 산출물 `src/Log/` (developer writer 영역 내부 — 16th tick 박제 (3) 의 carve 가능 카테고리 추정). carve 보류 결정 (사유 후술).
#       (v) monitor-island-convergence `01acbefd9ddfc60215d321823342f2bc2f636933` (신규, untracked — 17th tick 미검출 / 18th tick 1차 노출). REQ-069 박제. 산출물 `src/Monitor/`. carve 보류.
#       (vi) root-entry-island-convergence (신규, untracked — 17th tick 미검출 / 18th tick 1차 노출, hash 389ef8eea9d2fdeda164a96fa1a6a0e1b291a6e2). REQ-070 박제. 산출물 `src/` 직계 7 파일. carve 보류.
#     - 50.blocked/spec/foundation/: 본 tick 격리 결과로 vite-jsx-transform-channel-coherence.md + _reason.md (2 파일) 신규 박제. 직전 tick 격리 0건 + 17th /revisit 처리 2건 완료 후 진입 상태에서 본 tick 1건 신규.
#     - 50.blocked/task/: 비어있음 (17th tick 동일 잔존).
#     - 10.followups/: 1건 — `20260517-0117-devbin-install-integrity-from-blocked.md` (16th 격리 devbin → revive 결과물, 17th tick 동일 잔존). discovery 미흡수 — planner writer 영역 밖.
#     - 20.req/: 5건 — eslint-linter-options + log-island + monitor-island + root-entry-island + test-discovery (17th tick 동일 잔존). inspector 측 4건 (eslint + log + monitor + root-entry) 흡수 완료된 untracked green 박제 잔존 (working tree). test-discovery + eslint-linter-options 는 17th tick 박제 동일.
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 정합 — 본 tick carve 0건이라 임계치 무관).
#     - 60.done/2026/05/17/: req 8건 누적 (17th tick 동일). task 디렉터리 부재 (완료 task 0건). followups 디렉터리 4건 (island-regression-guard + island-prop-types-removal-toaster + devbin-from-blocked + 17th /revisit + 등). revisit 디렉터리 (17th /revisit 처리 흔적).
#     - 환경 회귀: 18th tick 실측 — `node_modules/vite` ABSENT (npm run build 채널 비활성 잔존, 17th tick 동일), `node_modules/typescript` EXIST (부분 회복), `node_modules/.bin/eslint` EXIST + `node_modules/@eslint/js` EXIST 이나 **`npm run lint` 실행 시 `@eslint/js` ERR_MODULE_NOT_FOUND** (eslint.config.js ESM resolve 실패 — flat-config import 의 경로/ESM/CommonJS 분기 문제 — pre-commit hook chain 미회복 17th tick 동일 잔존). `npx tsc --noEmit` 실행 결과 TS6046 (moduleResolution Bundler 미인식) + TS2688 (vitest/globals 미해석) + TS5070 (resolveJsonModule + node 외 분기) 3 hit (tsconfig 경로 / src/** 0 hit) 잔존 — typecheck precondition 미충족.
#     - Task ID footprint 전수 (RULE-01 표준 grep `specs/{40.task,60.done,50.blocked,50.blocked/task,10.followups,20.req}/` 실행 결과): `TSK-20260517-{01,02,03}` 점유 흔적 (60.done/2026/05/17/followups/ 의 island-proptypes-removal-toaster + 0058 followups 박제 — 9th tick 발행 후 50.blocked/task/ 격리 후 /revisit 경로). `TSK-20260517-04~99` 미사용. 본 tick 신규 carve 0건이라 ID 발행 없음.
#   본 세션 처리:
#     - 격리 1건: vite-jsx-transform-channel-coherence → 50.blocked/spec/foundation/vite-jsx-transform-channel-coherence.md + _reason.md. 사유 박제 (17th 행동지침 (1) 가이드 정합):
#       (a) 수단 중립 메타 spec 패턴 — §역할 line 10 박제 "세 채널 중 어느 채널의 제거/유지/추가 결정 (RULE-07 수단 중립 — 채널 분담 박제 + 이중 변환 부재 효능만)". RULE-07 정합 유지 — 격리 사유는 RULE-07 위반 아님.
#       (b) developer writer 영역 밖 산출물 경계 충돌 — 정상화 산출물 = `vite.config.js` 3 블록 (plugins[react], oxc, optimizeDeps.esbuildOptions.loader). developer writer 영역 (src/, 10.followups/) 밖. RULE-01 writer 매트릭스 + RULE-02 fail-fast 충돌.
#       (c) §동작 3 (production build 산출물 jsx-runtime import 횟수 측정) 활성 precondition 미충족 — REQ-064 (devbin-install-integrity, 16th 격리 / 17th /revisit revive followup) 의 `node_modules/vite` 부재. `npm run build` 채널 비활성 잔존. RULE-06 grep-baseline 실측 박제 불가능.
#       (d) RULE-02 재시도 금지 분기 — 정상화 task 발행 즉시 RULE-01 (b) + RULE-06 (c) 분기로 50.blocked/task/ 재진입 예측 (멱등 위반).
#       (e) RULE-05 정식 복귀 경로 (/revisit) 만 해소 가능 — 본 격리 사유는 RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 + 환경 회귀 의존 + hash 정체 2회차 조합 분기.
#     - carve 0건: 5 green spec (test-discovery / eslint-linter-options / log-island / monitor-island / root-entry-island) 모두 carve 보류. 보류 사유 별 spec 1줄:
#       (i) test-discovery-population-coherence — 미래 활성 게이트 (§동작 4 신규 루트 추가 / §동작 5 vitest 메이저 bump fail-safe) + 산출물 4축 (eslint/tsconfig/vite/package.json) 모두 src/ 밖 (RULE-01 writer 매트릭스 충돌) + 정체 1회차 (격리 후보 19th tick 도래).
#       (ii) eslint-linter-options-default-override — 산출물 `eslint.config.js` (src/ 밖) + 환경 회귀 (npm run lint @eslint/js resolve 실패 — RULE-06 grep dry-run 측정 baseline 비활성) + 정체 1회차 + ID 충돌 잔존 (REQ-068).
#       (iii) log-island-convergence — 산출물 `src/Log/` (developer writer 영역 내부 — 16th 박제 (3) carve 가능 카테고리) + baseline 위반 ≥1 (find src/Log .jsx/.js 24 hit / PropTypes 21 hit) 이나 **island-proptypes-removal chain RULE-02 재시도 금지 분기** (TSK-20260517-{01,02,03} 환경 회귀 chain 박제 / 9th-13th 격리 시계열 / 2e8a731 revive 패턴) + **typecheck FR-02 precondition 미충족** (TS6046+2688+5070 3 hit tsconfig 경로 → REQ-064 devbin 의 tsconfig 부분 회복 필요) + **pre-commit hook chain 미회복** (npm run lint @eslint/js resolve 실패 → developer 가 carve 발행 후 src/ 변경 commit 시 hook 차단 예측 = RULE-02 fail-fast 발동). 본 시점 carve 시 50.blocked/task/ 재진입 멱등 위반.
#       (iv) monitor-island-convergence — log-island 와 동일 chain (산출물 `src/Monitor/` 17 hit + PropTypes 20 hit, 동일 typecheck precondition + pre-commit hook chain 미회복 + RULE-02 재시도 금지 분기). 1차 노출.
#       (v) root-entry-island-convergence — 산출물 src/ 직계 7 파일 (App.jsx / App.test.jsx / index.jsx / reportWebVitals.js / reportWebVitals.test.js / setupTests.js / setupTests.timer-idiom.test.jsx) + 동일 typecheck/lint precondition 미충족 + RULE-02 재시도 금지 분기 (`src-typescript-migration` 의 island 정의 마지막 잔존 카테고리 — 동일 chain). 1차 노출.
#     - 승격 0건: 6 green spec 모두 unchecked `- [ ]` 잔존. vite-jsx 는 §테스트 현황 + §수용 기준 42 unchecked (rg -c 실측). 격리된 spec 의 박제 자체는 정합 (`[WIP]` 0 + `^#+ .*To-Be` 0 + 변경 이력 inspector 박제 1 hit) 이나 unchecked == 0 위반으로 승격 4조건 불충족 — 격리 분기로 처리. 잔존 5 spec 도 unchecked 잔존 (test-discovery 11+ / eslint-linter-options 미실측 / 3 island 각 11+ 추정).
#   다음 세션 (19th tick) 행동지침:
#     (1) test-discovery-population-coherence hash 가 본 tick 박제 (SHA-1 84e254b8) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 19th tick 진입 시 정체 2회차 도달 → 격리 후보 도래 (vite-jsx 18th tick 동일 패턴). 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴, (b) developer writer 영역 밖 산출물 (eslint.config.js / tsconfig.json / vite.config.js / package.json 4축), (c) §동작 4/5 미래 활성 게이트 (현 시점 정상화 task 트리거 부재 — 위반 baseline 0 분기 path-alias 5th 격리 사유 (c) 동일), (d) RULE-05 정식 복귀 경로만 해소. hash 변경 시 카운트 리셋.
#     (2) eslint-linter-options-default-override hash 가 본 tick 박제 (SHA-1 5dbfa413) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 19th tick 진입 시 정체 2회차 도달 → 격리 후보 도래. 격리 결정 시 _reason.md 박제 사유: (a) 수단 중립 메타 spec 패턴, (b) developer writer 영역 밖 산출물 (eslint.config.js), (c) `npm run lint` @eslint/js resolve 실패로 RULE-06 grep dry-run 측정 baseline 비활성, (d) ID 충돌 분기 (REQ-068 = eslint-linter-options + log-island 동시 박제 — 차기 discovery 재배정 신호), (e) RULE-05 정식 복귀 경로만 해소. hash 변경 시 카운트 리셋.
#     (3) log-island-convergence / monitor-island-convergence / root-entry-island-convergence 3 spec hash 가 본 tick 박제와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 19th tick 진입 시 정체 1회차 누적. 20th tick 정체 2회차 도달 시 격리 후보 도래. 본 3 spec 의 carve 가능 회복 조건: **(i) 환경 회귀 회복** — `npm run lint` exit 0 + `npx tsc --noEmit` exit 0 (TS6046/2688/5070 3 hit 해소) + pre-commit hook chain (`@eslint/js` ESM resolve 성공 + lint-staged eslint exit 0). **(ii) RULE-02 재시도 금지 chain 해소** — `island-proptypes-removal` 6th-13th 격리 시계열 + TSK-20260517-{01,02,03} 환경 회귀 chain followup 처리 완료 신호 (60.done revive 후 discovery 재흡수 → 신규 task 발행 시 동일 환경 회귀 미재발 검증). (i)+(ii) 동시 충족 시 carve 진입 가능 (산출물 src/ 내부 → developer writer 영역 정합 + baseline 위반 ≥1 정합 = 17th 행동지침 (3) 의 src/ 내부 carve 가능 카테고리 정합).
#     (4) 50.blocked/spec/foundation/vite-jsx-transform-channel-coherence (본 tick 격리) 의 /revisit 처리 신호 검출 → revive (10.followups/) 또는 close (60.done/2026/05/17/revisit/) 분기. 본 분기는 RULE-05 사람 운영자 영역 — planner 미접촉.
#     (5) 10.followups/ devbin from-blocked 1건 discovery 흡수 신호 검출 → 20.req/ 발행 후 inspector → green spec 재진입 흐름 가능. devbin 후속은 수단 중립 메타 spec 패턴 재발 가능성 (12th 박제 §역할 line 10 정합) — carve 시점 RULE-01 writer 매트릭스 재검토 의무.
#     (6) Task ID footprint: TSK-20260517-{01,02,03} 사용 중 (60.done followups + revive 박제). -04+ 부터 채번 가능. RULE-01 표준 grep 전수 (40.task + 60.done + 50.blocked + 10.followups + 20.req) 매 carve 직전 의무. 영구 박제 -01,-02,-03 재사용 금지 (0058 followups 박제 정합).
#     (7) 본 tick 박제 시점 baseline (HEAD=24183430) 와 19th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) 5 green spec hash 동일성 재실측 (SHA-1 git hash-object), (b) 10.followups + 20.req 새 항목 검출 (REQ-071+), (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호 — 본 tick 격리 vite-jsx + 기존 잔존), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (`node_modules/vite` 존재 / `npm run lint` exit 0 / `npx tsc --noEmit` exit 0 / pre-commit hook chain 회복) 검출. 격차 항목 별 행동지침 (1)~(6) 분기 적용.
#     (8) RULE-07 정합 검증 (18th tick 박제): 본 tick 격리 1 spec (vite-jsx) + 잔존 5 green spec (test-discovery / eslint-linter-options / log-island / monitor-island / root-entry-island) 모두 §동작 평서형·반복 검증 가능·시점 비의존·incident 비귀속·수단 중립 (§역할 박제) → RULE-07 정합 유지. carve 보류 사유는 RULE-07 위반 아님 — RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 + RULE-02 재시도 금지 (island chain) + RULE-06 grep dry-run 환경 의존 + 1차 노출 (3 island) 분기.
#     (9) 본 tick 격리 시계열 패턴 누적 정합 (17th 박제 (8) 갱신): toolchain-version-coherence (5th-7th 격리, /revisit followup 완료) → island-proptypes-removal (9th-13th 격리, 2e8a731 revive 완료) → runtime-dep-version-coherence (11th-13th 격리, 2e8a731 revive 완료) → devbin-install-integrity (16th 격리, 17th 33c71a8 revive followup) → path-alias-resolver-coherence (16th 격리, 17th 33c71a8 close) → vite-jsx-transform-channel-coherence (18th 격리, 본 tick). 6 spec 모두 수단 중립 메타 spec + developer writer 영역 밖 산출물 + hash 정체 2~5회차 + 환경 회귀 미회복 시계열. test-discovery + eslint-linter-options (19th tick 격리 후보) 추가 시 8번째까지 누적 가능.
# 2026-05-17 planner session (19th tick @HEAD=0e961fc) — test-discovery-population-coherence hash `84e254b8` 18th 동일 + eslint-linter-options-default-override hash `5dbfa413` 18th 동일 + 환경 회귀 미회복 (`npx tsc --noEmit` TS6046+2688+5070 3 hit 잔존 / `@eslint/js` ERR_MODULE_NOT_FOUND / `node_modules/vite` 부재) + inspector 미터치 (`a952918` 후 변경 0) → 정체 2회차 도달 (18th 행동지침 (1)(2) 정합) → 50.blocked/spec/foundation/ 격리 + _reason.md 박제 (총 4 파일 신규). log/monitor/root-entry-island 3 spec hash 18th 동일 (9d8e1b43 / 01acbefd / 389ef8ee) + 환경 회귀 미회복 → 정체 1회차 누적 (18th 행동지침 (3) 정합 — 20th tick 정체 2회차 도달 시 격리 후보). carve 0건, 승격 0건, 격리 2건.
#   본 tick 입력 상태:
#     - 30.spec/green/foundation/: 5 spec (test-discovery / eslint-linter-options / log-island / monitor-island / root-entry-island). 본 tick 처리 후 3 spec 잔존 (log + monitor + root-entry).
#     - 30.spec/green/components/ + common/: 비어있음.
#     - 30.spec/blue/: foundation/ 5 spec (ci / coverage-determinism / dependency-bump-gate / regression-gate / tooling) + components/ 10 spec + common/ 1 spec.
#     - 50.blocked/spec/foundation/: 본 tick 진입 시 vite-jsx 1 spec + _reason.md (18th 격리). 본 tick 처리 후 +2 spec (test-discovery + eslint-linter-options) + 각 _reason.md = 6 파일.
#     - 50.blocked/{task,req}/: 비어있음.
#     - 10.followups/: 1건 — `20260517-0117-devbin-install-integrity-from-blocked.md` (16th 격리 devbin → revive 결과물, 17th-18th-19th tick 동일 잔존). discovery 미흡수 — planner writer 영역 밖.
#     - 20.req/: 비어있음 (18th tick 의 5 req 모두 inspector 흡수 완료 → 60.done/2026/05/17/req/ mv).
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 정합 — 본 tick carve 0건이라 임계치 무관).
#     - 60.done/2026/05/17/: req 8건 누적 (toolchain-version-coherence + runtime-dep-version-coherence + devbin-island-prop-types-removal + island-prop-types-removal + island-regression-guard-lint-block + husky-pre-push-typecheck-coverage + devbin-install-integrity + path-alias-resolver-coherence — 18th tick 동일). task 디렉터리 부재. followups 디렉터리 (revive 박제 누적). revisit 디렉터리 (17th /revisit 처리 흔적).
#     - 환경 회귀: 19th tick 실측 — `npx tsc --noEmit` 결과 TS6046 (`moduleResolution Bundler` 미인식, tsconfig.json:6) + TS2688 (`vitest/globals` 미해석) + TS5070 (`resolveJsonModule` + node 외, tsconfig.json:15) 3 hit (모두 tsconfig 경로, src/** 0 hit) 잔존. `npm run lint` exit ≠ 0 (eslint.config.js ESM import 가 `@eslint/js` resolve 시 ERR_MODULE_NOT_FOUND — 18th 동일 잔존). `node_modules/vite` ABSENT (npm run build 채널 비활성 잔존). 환경 회귀 chain 미회복.
#     - Task ID footprint 전수 (RULE-01 표준 grep `specs/{40.task,60.done,50.blocked,50.blocked/task,10.followups,20.req}/`): `TSK-20260517-{01,02,03}` 점유 흔적 (60.done/2026/05/17/followups/ + revive 박제 — 18th 동일). `TSK-20260517-04~99` 미사용. 본 tick 신규 carve 0건이라 ID 발행 없음.
#   본 세션 처리:
#     - 격리 2건: 
#       (A) test-discovery-population-coherence → 50.blocked/spec/foundation/test-discovery-population-coherence.md + _reason.md. 사유 박제 (18th 행동지침 (1) 가이드 정합):
#           (a) 수단 중립 메타 spec 패턴 — `runtime-dep-version-coherence` / `toolchain-version-coherence` / `devbin-install-integrity` / `path-alias-resolver-coherence` 와 동일. spec 본문은 도구 4축 (ESLint files / TypeScript include / Vitest coverage.include / Vitest test discovery) 모집단 매트릭스 박제 — 정상화 task 트리거 부재.
#           (b) developer writer 영역 밖 산출물 — 4축 모두 `eslint.config.js` / `tsconfig.json` / `vite.config.js` / `package.json` 루트 config. RULE-01 writer 매트릭스 (developer 는 `src/`) 충돌.
#           (c) §동작 4 (신규 루트 `*.test.*` 추가) / §동작 5 (vitest 메이저 bump default 글로브 변경) 미래 활성 게이트 — 현 시점 baseline 위반 0 (루트 1 hit `vite.config.test.js`, vitest 5.x). path-alias 5th 격리 사유 (c) 동일 패턴.
#           (d) 환경 회귀 미회복 — 19th tick 재실측 위와 동일. precondition (`devbin-install-integrity` REQ-064 격리) 해소 의존.
#           (e) RULE-05 정식 복귀 경로만 해소.
#       (B) eslint-linter-options-default-override → 50.blocked/spec/foundation/eslint-linter-options-default-override.md + _reason.md. 사유 박제 (18th 행동지침 (2) 가이드 정합):
#           (a) 수단 중립 메타 spec 패턴 — `tooling.md` (REQ-028/053/058) 의 `rules` 키 의미 박제와 동일 카테고리. spec 본문은 `eslint.config.js:20` `{ linterOptions: { reportUnusedDisableDirectives: 'off' } }` 단일 블록 명시 박제 — 현 시점 baseline 위반 0.
#           (b) developer writer 영역 밖 산출물 — `eslint.config.js` (루트 config). RULE-01 충돌.
#           (c) 환경 회귀 미회복 — `npm run lint` ERR_MODULE_NOT_FOUND `@eslint/js`. RULE-06 grep dry-run 측정 baseline 비활성. 본 spec §동작 6 `npm run lint` unused directive 출력 0 hit 실측 precondition 미충족.
#           (d) ID 충돌 분기 잔존 — REQ-068 = `eslint-linter-options-default-override` + `log-island-convergence` 동시 박제 (차기 discovery 재배정 신호).
#           (e) RULE-05 정식 복귀 경로만 해소.
#     - carve 0건: 잔존 3 green spec (log-island / monitor-island / root-entry-island) 모두 carve 보류. 보류 사유 별 spec 1줄:
#       (i) log-island-convergence — 산출물 `src/Log/` (developer writer 영역 내부 — 16th 박제 (3) carve 가능 카테고리) + baseline 위반 ≥1 (find src/Log .jsx/.js 24 hit / PropTypes 21 hit 19th 재실측 동일) 이나 **island-proptypes-removal chain RULE-02 재시도 금지 분기 잔존** (TSK-20260517-{01,02,03} 환경 회귀 chain / 9th-13th 격리 시계열 / 2e8a731 revive) + **typecheck FR-02 precondition 미충족** (TS6046+2688+5070 3 hit tsconfig 경로) + **pre-commit hook chain 미회복** (npm run lint @eslint/js resolve 실패 → developer carve 발행 후 src/ commit 시 hook 차단 = RULE-02 fail-fast 발동). carve 시 50.blocked/task/ 재진입 멱등 위반. 정체 1회차 누적 (20th tick 정체 2회차 도달 시 격리 후보).
#       (ii) monitor-island-convergence — log-island 와 동일 chain (산출물 `src/Monitor/` 17 hit + PropTypes 20 hit 동일, 동일 typecheck precondition + pre-commit hook chain 미회복 + RULE-02 재시도 금지). 정체 1회차 누적.
#       (iii) root-entry-island-convergence — 산출물 src/ 직계 7 파일 (App.jsx / App.test.jsx / index.jsx / reportWebVitals.js / reportWebVitals.test.js / setupTests.js / setupTests.timer-idiom.test.jsx) + 동일 typecheck/lint precondition 미충족 + RULE-02 재시도 금지 분기 (`src-typescript-migration` 의 island 정의 마지막 잔존 카테고리). 정체 1회차 누적.
#     - 승격 0건: 잔존 3 green spec 모두 §테스트 현황 + §수용 기준 unchecked `- [ ]` 잔존 (활성 fail 신호 도달 전 추정). 격리 2 spec 도 동일 unchecked 잔존 (격리 분기 처리).
#   다음 세션 (20th tick) 행동지침:
#     (1) log-island-convergence hash 가 본 tick 박제 (SHA-1 9d8e1b43) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 20th tick 진입 시 정체 2회차 도달 → 격리 후보 도래 (vite-jsx 18th + test-discovery/eslint-linter-options 19th 동일 패턴). 격리 결정 시 _reason.md 박제 사유: (a) RULE-02 재시도 금지 chain (island-proptypes-removal 9th-13th 시계열) 미해소, (b) 환경 회귀 미회복 (typecheck + lint pre-commit hook), (c) §island-proptypes-removal chain TSK-20260517-{01,02,03} 영구 박제 — ID 재발행 분기 차단, (d) RULE-05 정식 복귀 경로만 해소. hash 변경 또는 환경 회귀 회복 시 카운트 리셋.
#     (2) monitor-island-convergence hash 가 본 tick 박제 (SHA-1 01acbefd) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 20th tick 진입 시 정체 2회차 도달 → 격리 후보 도래. 격리 결정 시 _reason.md 박제 사유: log-island 와 동일 chain (산출물 `src/Monitor/` 17 hit + PropTypes 20 hit, 동일 typecheck + lint precondition 미충족 + RULE-02 재시도 금지). hash 변경 또는 환경 회귀 회복 시 카운트 리셋.
#     (3) root-entry-island-convergence hash 가 본 tick 박제 (SHA-1 389ef8ee) 와 동일 + 환경 회귀 미회복 + inspector 미터치 시 → 20th tick 진입 시 정체 2회차 도달 → 격리 후보 도래. 격리 결정 시 _reason.md 박제 사유: 산출물 src/ 직계 7 파일 (App.jsx / App.test.jsx / index.jsx / reportWebVitals.js / reportWebVitals.test.js / setupTests.js / setupTests.timer-idiom.test.jsx) + 동일 typecheck/lint precondition 미충족 + RULE-02 재시도 금지 분기 (src-typescript-migration island 정의 마지막 잔존 카테고리). hash 변경 또는 환경 회귀 회복 시 카운트 리셋.
#     (4) 50.blocked/spec/foundation/ 의 격리 3 spec (vite-jsx 18th + test-discovery / eslint-linter-options 19th) 의 /revisit 처리 신호 검출 → revive (10.followups/) 또는 close (60.done/2026/05/17/revisit/) 분기. 본 분기는 RULE-05 사람 운영자 영역 — planner 미접촉.
#     (5) 10.followups/ devbin from-blocked 1건 (17th-19th 동일 잔존) discovery 흡수 신호 검출 → 20.req/ 발행 후 inspector → green spec 재진입 흐름 가능. devbin 후속은 수단 중립 메타 spec 패턴 재발 가능성 — carve 시점 RULE-01 writer 매트릭스 재검토 의무.
#     (6) Task ID footprint: TSK-20260517-{01,02,03} 사용 중 (60.done followups + revive 박제). -04+ 부터 채번 가능. RULE-01 표준 grep 전수 (40.task + 60.done + 50.blocked + 10.followups + 20.req) 매 carve 직전 의무. 영구 박제 -01,-02,-03 재사용 금지.
#     (7) **carve 진입 회복 조건 명시 (3 island 공통)** — **(i) 환경 회귀 회복**: `npx tsc --noEmit` exit 0 (TS6046/2688/5070 3 hit 해소) + `npm run lint` exit 0 (`@eslint/js` ESM resolve 성공) + `node_modules/{vite,vitest,eslint,@eslint/js,typescript}` 모두 존재. **(ii) RULE-02 재시도 금지 chain 해소**: island-proptypes-removal 9th-13th 격리 시계열 + TSK-20260517-{01,02,03} 환경 회귀 chain followup 처리 완료 신호 (60.done revive 후 discovery 재흡수 → 신규 task 발행 시 동일 환경 회귀 미재발 검증). **(iii) precondition spec 해소**: `devbin-install-integrity` (REQ-064) + `path-alias-resolver-coherence` (REQ-065) /revisit 처리 완료. (i)+(ii)+(iii) 동시 충족 시 3 island carve 진입 가능.
#     (8) 본 tick 격리 시계열 패턴 누적 정합 (18th 박제 (9) 갱신): toolchain-version-coherence (5th-7th 격리, /revisit followup 완료) → island-proptypes-removal (9th-13th 격리, 2e8a731 revive 완료) → runtime-dep-version-coherence (11th-13th 격리, 2e8a731 revive 완료) → devbin-install-integrity (16th 격리, 17th 33c71a8 revive followup) → path-alias-resolver-coherence (16th 격리, 17th 33c71a8 close) → vite-jsx-transform-channel-coherence (18th 격리) → **test-discovery-population-coherence (19th 격리, 본 tick)** → **eslint-linter-options-default-override (19th 격리, 본 tick)**. 8 spec 모두 수단 중립 메타 spec + developer writer 영역 밖 산출물 + hash 정체 2~5회차 + 환경 회귀 미회복 시계열. 3 island (log/monitor/root-entry) 20th tick 격리 후보 도래 시 11번째까지 누적 가능 — **환경 회귀 회복 신호** 가 시계열 단절 트리거.
#     (9) RULE-07 정합 검증: 본 tick 격리 2 spec (test-discovery + eslint-linter-options) 모두 §동작 평서형·반복 검증 가능·시점 비의존·incident 비귀속·수단 중립 (§역할 line 10 / line 11 박제) → RULE-07 정합. 격리 사유는 RULE-07 위반 아님 — RULE-01 writer 매트릭스 + RULE-02 산출물 경계 충돌 + RULE-06 grep dry-run 환경 의존 + 정체 2회차 (hash 동일) 분기. 잔존 3 green spec 도 RULE-07 정합 유지 (§역할 박제 수단 중립).
#     (10) 본 tick 박제 시점 baseline (HEAD=0e961fc) 와 20th tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) 3 green spec hash 동일성 재실측, (b) 10.followups + 20.req 새 항목 검출, (c) 50.blocked 진입/이탈 항목 검출, (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (`node_modules/vite` 존재 / `npm run lint` exit 0 / `npx tsc --noEmit` exit 0) 검출. 격차 항목 별 행동지침 (1)~(7) 분기 적용.
# 2026-05-17 planner session (20th tick @HEAD=7477189) — 3 island spec (log/monitor/root-entry) hash 18th/19th 동일 (9d8e1b43 / 01acbefd / 389ef8ee) + 환경 회귀 미회복 (`npx tsc --noEmit` TS6046+2688+5070 3 hit 잔존 / `npm run lint` ERR_MODULE_NOT_FOUND `@eslint/js` 잔존 / `node_modules/{vite,vitest,@eslint/js}` ABSENT — 19th tick 대비 `@eslint/js` 도 사라짐 신규 악화) + inspector 미터치 (a9529185 이후 변경 0) → 정체 2회차 도달 (19th 행동지침 (1)(2)(3) 정합) → 50.blocked/spec/foundation/ 격리 + _reason.md 박제 (총 6 파일 신규 — 3 spec mv + 3 _reason.md). carve 0건, 승격 0건, 격리 3건.
#   본 tick 입력 상태:
#     - 30.spec/green/foundation/: 3 spec (log-island / monitor-island / root-entry-island). 본 tick 처리 후 0 spec (디렉터리 비어있음).
#     - 30.spec/green/components/ + common/: 비어있음 (18th-19th 동일).
#     - 30.spec/blue/: foundation/ 5 spec + components/ 10 spec + common/ 1 spec (18th-19th 동일).
#     - 50.blocked/spec/foundation/: 본 tick 진입 시 3 spec + _reason.md (vite-jsx 18th + test-discovery + eslint-linter-options 19th). 본 tick 처리 후 +3 spec (log/monitor/root-entry) + _reason.md = 12 파일.
#     - 50.blocked/{task,req}/: 비어있음.
#     - 10.followups/: 비어있음 (19th tick 의 devbin from-blocked 1건 discovery 흡수 또는 처리 완료 신호 — planner writer 영역 밖).
#     - 20.req/: 비어있음 (19th tick 동일).
#     - 40.task/: 비어있음 (TASK_READY_MAX=10 미달 정합 — 본 tick carve 0건이라 임계치 무관).
#     - 60.done/2026/05/17/: req 8건 누적 (18th-19th 동일). task 디렉터리 부재 (완료 task 0건). followups 디렉터리 (devbin from-blocked 처리 완료 추정).
#     - 환경 회귀: 20th tick 실측 — `npx tsc --noEmit` TS6046 (`moduleResolution Bundler` 미인식, tsconfig.json:6) + TS2688 (`vitest/globals` 미해석) + TS5070 (`resolveJsonModule` + node 외, tsconfig.json:15) 3 hit (tsconfig 경로, src/** 0 hit). `npm run lint` ERR_MODULE_NOT_FOUND `@eslint/js`. `node_modules/{vite,vitest,@eslint/js}` ABSENT (19th 대비 `@eslint/js` 추가 손실), `node_modules/{typescript,eslint}` EXIST.
#     - Task ID footprint 전수 (RULE-01 표준 grep `specs/{40.task,60.done,50.blocked,50.blocked/task,10.followups,20.req}/`): `TSK-20260517-{01,02,03}` 점유 흔적 (60.done/2026/05/17/followups/ + revive 박제 — 18th-19th 동일). `TSK-20260517-04~99` 미사용 — 본 tick 신규 carve 0건이라 ID 발행 없음.
#   본 세션 처리:
#     - 격리 3건:
#       (A) log-island-convergence → 50.blocked/spec/foundation/log-island-convergence.md + _reason.md. 사유: (a) RULE-02 재시도 금지 chain (island-proptypes-removal 9th-13th 시계열 + TSK-20260517-{01,02,03} 영구 박제), (b) 환경 회귀 미회복 (TS6046+2688+5070 3 hit + `@eslint/js` ERR_MODULE_NOT_FOUND + pre-commit hook chain 차단), (c) RULE-05 정식 복귀 경로만 해소. 산출물 `src/Log/` developer writer 영역 내부 정합 (carve 가능 카테고리) 이나 chain 으로 멱등 위반.
#       (B) monitor-island-convergence → 50.blocked/spec/foundation/monitor-island-convergence.md + _reason.md. 사유: log-island 와 동일 chain (산출물 `src/Monitor/` 17 hit + PropTypes 20 hit, 동일 typecheck + lint precondition 미충족 + RULE-02 재시도 금지).
#       (C) root-entry-island-convergence → 50.blocked/spec/foundation/root-entry-island-convergence.md + _reason.md. 사유: 산출물 src/ 직계 7 파일 (App.jsx / App.test.jsx / index.jsx / reportWebVitals.js / reportWebVitals.test.js / setupTests.js / setupTests.timer-idiom.test.jsx) + 동일 typecheck/lint precondition 미충족 + RULE-02 재시도 금지 (src-typescript-migration island 정의 마지막 잔존 카테고리) + 빌드 채널 전체 영향 (root entry — App.jsx / index.jsx 변경 시 vite 빌드 / vitest 검증 / eslint lint 3 채널 동시 영향).
#     - carve 0건: green 큐 비어있음 (본 tick 처리 후). 잔존 처리 대상 없음.
#     - 승격 0건: 격리 3 spec 모두 unchecked `- [ ]` 잔존 (log 20 + monitor 20 + root-entry 24 — `grep -c "^- \[ \]"` 실측). 박제 자체 정합 (`[WIP]` 0 + `^#+ .*To-Be` 0 + 변경 이력 inspector 박제 1 hit) 이나 unchecked == 0 위반으로 승격 4조건 불충족 — 격리 분기로 처리.
#   다음 세션 (21st tick) 행동지침:
#     (1) green 큐 비어있음 (`30.spec/green/foundation/` + `components/` + `common/` 모두 0 spec) → RULE-03 (2) 입력 큐 비면 no-op. 단, discovery → inspector 흐름이 새 green spec 박제 시 21st 또는 이후 tick 진입.
#     (2) 50.blocked/spec/foundation/ 9 파일 (vite-jsx 18th + test-discovery + eslint-linter-options 19th + log + monitor + root-entry 20th 각 .md + _reason.md) 의 /revisit 처리 신호 검출 → revive (10.followups/) 또는 close (60.done/2026/05/17/revisit/) 분기. 본 분기는 RULE-05 사람 운영자 영역 — planner 미접촉. 6 spec 모두 환경 회귀 회복 + RULE-02 재시도 금지 chain 해소 동시 충족 시 revive 대상 (수단 중립 메타 spec 박제 정합 + 산출물 chain 의존).
#     (3) **환경 회귀 회복 신호 검출** = 시계열 단절 트리거 (19th 행동지침 (8) 정합). `node_modules/vite` 존재 + `node_modules/vitest` 존재 + `node_modules/@eslint/js` 존재 + `npx tsc --noEmit` exit 0 + `npm run lint` exit 0 = 4 조건 동시 충족 시 6 격리 spec (foundation/ 전체) revive 후보 도래. 사람 운영자 /revisit 수동 트리거 의존 — planner 자동 재시도 금지 (RULE-02 멱등).
#     (4) Task ID footprint: TSK-20260517-{01,02,03} 사용 중. -04+ 부터 채번 가능. RULE-01 표준 grep 전수 매 carve 직전 의무. 영구 박제 -01,-02,-03 재사용 금지.
#     (5) 본 tick 박제 시점 baseline (HEAD=7477189) 와 21st tick 진입 시점 baseline 격차 분석: HEAD 변경 시 (a) green 큐 새 spec 검출 (inspector 흡수 신호), (b) 10.followups + 20.req 새 항목 검출, (c) 50.blocked 진입/이탈 항목 검출 (/revisit 처리 신호 — 본 tick 6 spec 신규 격리 + 기존 잔존), (d) Task ID 전수 grep 재실행, (e) 환경 회귀 회복 신호 (`node_modules/vite` 존재 / `npm run lint` exit 0 / `npx tsc --noEmit` exit 0 / `node_modules/@eslint/js` 존재) 검출. 격차 항목 별 행동지침 (1)~(3) 분기 적용.
#     (6) RULE-07 정합 검증: 본 tick 격리 3 spec (log + monitor + root-entry) 모두 §역할 line 10 박제 "수렴 수단 ... 본 spec 비박제 — task 계층 위임" 수단 중립 메타 spec → RULE-07 정합. §동작 평서형·반복 검증 가능·시점 비의존·incident 비귀속. 격리 사유는 RULE-07 위반 아님 — RULE-02 재시도 금지 chain + 환경 회귀 의존 + 정체 2회차 (hash 동일) 분기.
#     (7) 본 tick 격리 시계열 패턴 누적 정합 (19th 박제 (8) 갱신): toolchain-version-coherence (5th-7th, /revisit followup 완료) → island-proptypes-removal (9th-13th, 2e8a731 revive 완료) → runtime-dep-version-coherence (11th-13th, 2e8a731 revive 완료) → devbin-install-integrity (16th, 17th 33c71a8 revive followup) → path-alias-resolver-coherence (16th, 17th 33c71a8 close) → vite-jsx-transform-channel-coherence (18th) → test-discovery-population-coherence (19th) → eslint-linter-options-default-override (19th) → **log-island-convergence (20th, 본 tick)** → **monitor-island-convergence (20th, 본 tick)** → **root-entry-island-convergence (20th, 본 tick)**. 11 spec 모두 수단 중립 메타 spec + developer writer 영역 밖 또는 chain 의존 + hash 정체 2~5회차 + 환경 회귀 미회복 시계열. **환경 회귀 회복 신호** 가 시계열 단절 트리거 — 21st tick 진입 시 회복 신호 검출 가능 시 일괄 revive 후보 도래.
src-spec-reference-coherence 3ebe603e1bdc5d10319d0d7cabda8dab763bad5f66347d7ffa1530a8f99ef12a
# 2026-05-17 planner session (24th tick @HEAD=e6cd28b) — inspector REQ-20260517-071 흡수 (foundation/src-spec-reference-coherence.md 신규 박제) → green 큐 1차 노출 + carve 2건 발행.
#   본 tick 입력 상태:
#     - 30.spec/green/foundation/: 1 spec (src-spec-reference-coherence.md, hash 3ebe603e — 본 tick 최초 노출). components/ + common/ 부재.
#     - 30.spec/blue/foundation/: 5 spec (ci + coverage-determinism + dependency-bump-gate + regression-gate + tooling). 동명 spec 부재 — 신규 carve 정합.
#     - 50.blocked/spec/foundation/: 9 파일 잔존 (vite-jsx + test-discovery + eslint-linter-options + log + monitor + root-entry 각 .md + _reason.md). 본 tick 미접촉 — RULE-05 /revisit 영역.
#     - 40.task/: 비어있음 → 본 tick carve 2건 발행 후 2건. TASK_READY_MAX=10 여유.
#     - 60.done/2026/05/17/req/: 누적 8건 (직전 tick 동일). 본 tick 신규 0건 (inspector 영역).
#     - 환경 회귀: 24th tick 실측 — `node_modules/vite` ABSENT + `node_modules/vitest` ABSENT + `node_modules/@eslint/js` ABSENT + `node_modules/typescript` EXIST + `node_modules/eslint` EXIST. 20th tick 대비 동일 — 회복 신호 미검출. 단, 본 tick carve 대상 (src-spec-reference-coherence) 는 grep / test -e POSIX 도구만 요구 (§의존성 line 29 "외부: 없음") → 환경 회귀와 무관하게 carve 가능 (환경 회귀 시계열 단절 트리거 무관).
#     - Task ID footprint 전수 (RULE-01 표준 grep): TSK-20260420-{13,26,33} + TSK-20260517-{01,02,03,04} + carve 접미사 (TSK-20260517-01-island-proptypes-removal-toaster + -from-blocked). -05 / -06 미점유 — 본 tick 채번 정합.
#   본 세션 처리:
#     - carve 2건 (depends_on chain):
#       (A) TSK-20260517-05-src-spec-reference-dead-link-purge: spec §동작 G1 (suffix 정합 grep 0 hit) + §G2 (test -e 0 MISSING) 회수. 11 hit / 11 file scope 박제 (declared == actual 정합 검증 완료). expansion 불허 — scope 밖 src 파일 0 hit 확인. RULE-06 grep-baseline 실측 박제 (HEAD=e6cd28b 시점) + dry-run G1=11/11 hit + G2=7/7 MISSING + file uniqueness 11/11 = **3/3 match**.
#       (B) TSK-20260517-06-src-spec-reference-coherence-ci-gate: spec §동작 G3 (시점 비의존) + §수용 기준 Should (CI lint step / pre-commit hook / npm run lint 부속 스텝) 박제. depends_on [TSK-20260517-05]. 수단 선정 (scripts/ + package.json + husky/lefthook + 선택 ESLint) 본 task 위임. expansion N/A (grep 게이트 구현체 박제).
#     - 승격 0건: green spec unchecked 8건 잔존 (§테스트 현황 3 + §수용 기준 5 unchecked) — RULE-01 승격 4조건 (`[WIP]` 0 ✓ + `^#+ .*To-Be` 0 ✓ + 변경 이력 inspector 박제 ✓ + unchecked 0 ✗) 중 unchecked 0 미충족. 박제 자체 정합 — carve 발행 후 developer 수행 + DoD 완료 후 inspector 차후 tick 에서 unchecked → checked 갱신 시 승격 후보 도래.
#     - 격리 0건: 본 tick 신규 노출 spec 1건 (3ebe603e hash, 정체 카운트 0회차) → 격리 분기 비대상. RULE-07 정합 (§역할 시스템 횡단 게이트 + §동작 평서형 + §G3 시점 비의존 + 반복 검증 가능 grep / test -e + incident 비귀속).
#     - 환경 회귀 처리: carve 대상 spec 의 §의존성 외부 0 정합 → 환경 회귀 미회복 상태에서도 carve 가능 (POSIX grep/test 만 요구). TSK-05 발행 task DoD 의 `npm run lint` / `npm test` / `npm run build` 는 developer 수행 시점에 환경 회복 가정 — 환경 미회복 시 developer 가 50.blocked/task/ 격리 후 followup 분기 (planner 영역 밖).
#   다음 세션 (25th tick) 행동지침:
#     (1) **green 큐 상태 분기**:
#       - (a) green 비어있음 (developer 가 TSK-05/06 수행 후 inspector 가 unchecked → checked 갱신 + green 자체 미터치) → RULE-03 (2) no-op. 단 inspector 가 신규 spec 박제 시 진입.
#       - (b) green 동일 hash 3ebe603e 유지 + inspector 미터치 → carve 재실행 금지 (TSK-05/06 이미 박제, 멱등). hash 동일 + unchecked 0 == 0 도달 시 (developer 가 §G1/§G2 회복 후 inspector 가 §수용 기준 checked 갱신) **승격 분기 도래** — 4조건 전수 충족 시 mv green/foundation/F → blue/foundation/F.
#       - (c) green hash 변경 (3ebe603e ≠) → inspector 가 spec 재편집. 재검증 후 carve 재발행 또는 RULE-07 격리 분기.
#     (2) **40.task/ 백프레셔**: 본 tick 발행 2건 → 25th tick 진입 시 40.task/ 2건 유지 시 임계치 8건 여유. developer 가 TSK-05 수행 완료 (40.task → 60.done/task/) 시 1건 감소. depends_on chain 정합 — TSK-06 은 TSK-05 완료 후 developer 진입 (developer 자율 chain 점검).
#     (3) **승격 판정 멱등 게이트**: 25th tick 진입 시 green 동일 hash + unchecked 0 도달이면 mv green→blue. RULE-04 notes `promote: <slug>` 토큰 박제. unchecked >0 잔존 시 carve 재발행 없이 박제 보존 (TSK-05/06 가 §G1/§G2 회복 책임 — 측정 결과 inspector 가 spec §수용 기준 checked 갱신).
#     (4) **TSK-05 / TSK-06 발행 후 후속 분기**: 본 tick 박제 2 task 가 40.task/ 잔존 시 developer 자율 수행 — planner 미접촉 (RULE-01 writer 매트릭스). carve 재발행 금지 (멱등). depends_on chain 위반 (TSK-06 이 TSK-05 미완료 시 진입) 은 developer 영역.
#     (5) **환경 회귀 분기 (24th 동일)**: 본 tick 발행 task 는 grep / test -e POSIX 만 요구 (TSK-05) + scripts/ + package.json + husky/lefthook 인프라 박제 (TSK-06). TSK-05 는 환경 미회복 상태에서도 수행 가능. TSK-06 의 DoD `npm run lint` / `npm test` / `npm run build` 는 환경 회복 가정 — 발행 시점에 명시 제약 박제. 환경 회귀 시계열 단절 트리거 미충족 = `node_modules/{vite,vitest,@eslint/js}` ABSENT 잔존 시 developer 수행 시점에 50.blocked/task/ 격리 + followup 분기.
#     (6) **Task ID footprint**: 본 tick 채번 TSK-20260517-{05,06} 영구 박제. 25th tick 신규 carve 시 -07+ 채번. 영구 박제 -05,-06 재사용 금지.
#     (7) **본 tick 박제 시점 baseline (HEAD=e6cd28b) 와 25th tick 진입 시점 baseline 격차 분석**: HEAD 변경 시 (a) green spec hash 변경 검출 (inspector 재편집 또는 mv green→blue 시 디스크 이동), (b) 40.task/ 진입/이탈 검출 (developer 수행 완료 신호), (c) 50.blocked 진입/이탈 검출 (developer 격리 또는 /revisit 처리), (d) 환경 회귀 회복 신호 (`node_modules/{vite,vitest,@eslint/js}` 존재 + `npx tsc --noEmit` exit 0 + `npm run lint` exit 0) 검출. 격차 항목 별 행동지침 (1)~(3) 분기 적용.
#     (8) **RULE-07 정합 검증**: src-spec-reference-coherence §역할 (`src/**` ↔ `specs/30.spec/**` 간 참조 정합 상시 시스템 계약) + §동작 G1~G4 평서형·반복 검증 가능·시점 비의존·incident 비귀속. RULE-07 양성 정합. carve 발행 task 는 incident patch 가 아닌 spec 박제 게이트 회복 (§G1/§G2 0 hit / 0 MISSING) — 1회성 진단 아님.
# 2026-05-17 planner session (25th tick @HEAD=7154b8e) — green spec hash 동일 (3ebe603e) + inspector 미터치 + 40.task/ 2건 잔존 (TSK-05/06) → 직전 tick 행동지침 (1)(b) 분기 정합 (carve 재실행 금지 멱등 + unchecked 8 잔존 승격 보류) + 정체 1회차 누적.
#   본 tick 입력 상태:
#     - 30.spec/green/foundation/: 1 spec (src-spec-reference-coherence.md, hash 3ebe603e — 24th 박제 동일, inspector 미터치). components/ + common/ 부재.
#     - 30.spec/blue/foundation/: 5 spec (ci + coverage-determinism + dependency-bump-gate + regression-gate + tooling). 동명 spec 부재 (승격 충돌 없음).
#     - 50.blocked/spec/foundation/: 12 파일 잔존 (vite-jsx + test-discovery + eslint-linter-options + log + monitor + root-entry 각 .md + _reason.md). 본 tick 미접촉 — RULE-05 /revisit 영역.
#     - 40.task/: 2건 잔존 (TSK-20260517-05-src-spec-reference-dead-link-purge + TSK-20260517-06-src-spec-reference-coherence-ci-gate) — 24th 박제 유지, developer 미진입. TASK_READY_MAX=10 임계치 2/10 (8건 여유).
#     - 50.blocked/task/: 비어있음 (developer 격리 신호 0).
#     - 60.done/2026/05/17/req/: 14건 누적 (24th 8건 + 6건 신규 — log/monitor/root-entry/test-discovery/eslint-linter-options/vite-jsx/path-alias/devbin 등 inspector 흡수 영역). 본 tick 신규 0건 (planner 영역 밖).
#     - 60.done/2026/05/17/task/: 디렉터리 부재 (완료 task 0건 — TSK-05/06 미수행).
#     - 10.followups/: 비어있음 (24th 동일).
#     - 20.req/: 2건 (20260517-node-modules-extraneous-coherence.md + 20260517-vite-env-boundary-typing.md) — inspector 흡수 대기 (planner 영역 밖).
#     - 환경 회귀: 25th tick 실측 — `npx tsc --noEmit` TS6046 + TS2688 + TS5070 3 hit (tsconfig 경로, src/** 0 hit, 24th 동일). `npm run lint` ERR_MODULE_NOT_FOUND `@eslint/js` (24th 동일). `node_modules/{vite,vitest,@eslint/js}` ABSENT + `{typescript,eslint}` EXIST (24th 동일). 회복 신호 미검출 — 시계열 단절 트리거 미충족.
#     - Task ID footprint 전수 (RULE-01 표준 grep `specs/{40.task,60.done,50.blocked,50.blocked/task,10.followups,20.req}/` + 카브 접미사 -a,-b,-from-blocked 포함): TSK-20260420-{13,26,33} + TSK-20260517-{01,02,03,04,05,06}. -07 미점유 (본 tick 채번 0건).
#   본 세션 처리:
#     - carve 0건: green spec hash 3ebe603e 동일 (24th 박제) + 24th 발행 TSK-05/06 가 본 spec §G1/§G2 회수 책임 박제 완료 — carve 재실행은 RULE-02 멱등 위반. 직전 tick 행동지침 (1)(b) "hash 동일 + 미터치 → carve 재실행 금지 (TSK-05/06 이미 박제, 멱등)" 정합.
#     - 승격 0건: green spec unchecked 8건 잔존 (§테스트 현황 3 + §수용 기준 5 unchecked, 24th 동일) — RULE-01 승격 4조건 (`[WIP]` 0 ✓ + `^#+ .*To-Be` 0 ✓ + 변경 이력 inspector 박제 ✓ line 64 + unchecked 0 ✗) 중 unchecked 0 미충족. 박제 자체 정합 — TSK-05/06 developer 수행 + DoD 완료 후 inspector 차후 tick 에서 §G1/§G2/§수용 기준 unchecked → checked 갱신 시 승격 후보 도래.
#     - 격리 0건: 본 spec 정체 1회차 (24th 첫 노출 → 25th 동일 hash) — RULE-01 .planner-seen 기준 "같은 diff-hash 2회 이상 정체 → 50.blocked/spec/" 분기 미충족 (1회차). 또한 본 spec RULE-07 양성 정합 (§역할 시스템 계약 + §동작 평서형 + §G1~G4 시점 비의존 + 반복 검증 가능 grep/test -e + incident 비귀속). 정체 사유는 developer 미수행 + inspector unchecked 미갱신 — 박제 자체 결함 아님. carve task (TSK-05/06) 가 40.task/ 잔존 — chain 진행 중 분기.
#     - 환경 회귀 처리: 24th 동일 잔존 — 본 spec 의 §의존성 외부 0 정합으로 환경 무관. TSK-05 (POSIX 도구) 는 환경 미회복 상태에서도 수행 가능 — developer 미진입은 환경 정체 아닌 트리거 주기 의존. TSK-06 (`npm run lint` 부속) 은 환경 회복 가정 — 환경 미회복 시 developer 50.blocked/task/ 격리 후 followup 분기.
#     - 20.req/ 2건 (node-modules-extraneous-coherence + vite-env-boundary-typing) 본 tick 미접촉 — inspector 영역 (RULE-01 writer 매트릭스). 25th-26th tick 진입 시 inspector 가 green 박제 시 차후 tick 에서 carve 검토.
#   다음 세션 (26th tick) 행동지침:
#     (1) **green 큐 상태 분기**:
#       - (a) green 비어있음 (TSK-05/06 수행 완료 후 inspector 가 unchecked → checked 갱신하며 본 tick 25th 와 동일 행동지침 (1)(b) 충족 시 mv green→blue 또는 inspector 가 green 자체 mv → blue 처리는 planner 의무 — inspector 미접촉 시 planner 가 promote 수행) → RULE-03 (2) no-op. 단 inspector 가 신규 spec 박제 시 진입.
#       - (b) green 동일 hash 3ebe603e 유지 + inspector 미터치 + unchecked 8 잔존 → **정체 2회차 도달** (24th 1차 + 25th 1회차 정체 + 26th 2회차 정체). RULE-01 ".planner-seen 기준 같은 diff-hash 2회 이상 정체 → 50.blocked/spec/" 분기 충족 검토 필요. 단, 본 spec 의 정체 사유는 (i) developer 미수행 (TSK-05/06 잔존) + (ii) inspector 미갱신 (unchecked → checked) — spec 박제 자체 결함 아님. **격리 vs 박제 보존 분기 결정**: TSK-05/06 가 40.task/ 잔존 = developer 진척 중 = 격리 부적합 (chain 정상). TSK-05/06 가 60.done/2026/05/17/task/ 이동 완료 + green hash 동일 + unchecked 잔존 = inspector 갱신 누락 = inspector 의 unchecked 갱신 의무 위반 = 격리 후보 도래. **분기 판정 게이트**: 26th tick 진입 시 `ls /Users/park108/Dev/log/specs/60.done/2026/05/17/task/` 결과 + 40.task/ 잔존 검사 + green unchecked 카운트 동시 측정 후 (i) TSK-05/06 미완료 = 박제 보존 (chain 진행 중) (ii) TSK-05/06 완료 + unchecked 잔존 = inspector 의무 위반 → 격리 (iii) TSK-05/06 완료 + unchecked == 0 = 승격 진행.
#       - (c) green hash 변경 (3ebe603e ≠) → inspector 가 spec 재편집. 재검증 후 carve 재발행 또는 RULE-07 격리 분기. 정체 카운트 리셋.
#       - (d) green 자체 mv → blue (planner 가 본 tick 또는 26th 에서 승격 수행) → carve 진행 중 task 박제는 60.done/task/ 진척 시점에 박제 유효 (blue 가 carve 박제 위임 task 의 게이트 회수 대상).
#     (2) **40.task/ 백프레셔**: 본 tick 2건 유지 (TSK-05/06) → 26th tick 진입 시 2건 동일이면 임계치 8건 여유 (developer 미진입). developer 수행 완료 시 1건씩 감소 (TSK-05 → 60.done/task/ 후 TSK-06 진입). carve 재발행 금지 (멱등).
#     (3) **승격 판정 멱등 게이트**: 26th tick 진입 시 green 동일 hash + unchecked 0 도달이면 mv green→blue. RULE-04 notes `promote: src-spec-reference-coherence` 토큰 박제. unchecked >0 잔존 시 carve 재발행 없이 박제 보존 + 정체 카운트 분기.
#     (4) **TSK-05 / TSK-06 발행 후 후속 분기 (24th 동일)**: 본 tick 박제 2 task 가 40.task/ 잔존 시 developer 자율 수행 — planner 미접촉. carve 재발행 금지. depends_on chain 위반은 developer 영역. 본 tick 미수행 = developer 트리거 미진입 (planner 영역 밖) 또는 환경 회귀 의존 (TSK-06 의 npm run lint).
#     (5) **환경 회귀 분기 (24th 동일)**: 회복 신호 미검출 잔존. TSK-05 는 환경 무관, TSK-06 은 환경 회복 가정. 환경 미회복 잔존 시 TSK-06 developer 수행 시점에 50.blocked/task/ 격리 + followup 분기.
#     (6) **20.req/ 신규 2건 처리**: node-modules-extraneous-coherence + vite-env-boundary-typing — inspector 흡수 대기. inspector 가 green 박제 + planner 인지 시점 본 tick 박제 패턴 (24th carve 분기) 재적용. 환경 회귀 시계열 단절 트리거 (node_modules 회복) 신호 박제 후보 — 20.req/ node-modules-extraneous-coherence 가 환경 회귀 본질 spec 박제일 수 있음 (inspector 처리 결과 의존).
#     (7) **Task ID footprint**: 본 tick 채번 0건. -07+ 부터 채번 가능. 영구 박제 -01,-02,-03,-04,-05,-06 재사용 금지.
#     (8) **본 tick 박제 시점 baseline (HEAD=7154b8e) 와 26th tick 진입 시점 baseline 격차 분석**: HEAD 변경 시 (a) green spec hash 변경 검출 (inspector 재편집 또는 unchecked → checked 갱신), (b) 40.task/ 진입/이탈 검출 (developer 수행 완료 신호), (c) 60.done/2026/05/17/task/ 디렉터리 신설 검출 (developer 첫 완료 신호), (d) 50.blocked/task/ 진입 검출 (developer 격리 신호 — 환경 회귀 의존), (e) 20.req/ 변동 검출 (inspector 흡수 신호), (f) 환경 회귀 회복 신호 (`node_modules/{vite,vitest,@eslint/js}` 존재 + `npx tsc --noEmit` exit 0 + `npm run lint` exit 0) 검출. 격차 항목 별 행동지침 (1)~(3) 분기 적용.
#     (9) **정체 카운트 게이트**: 26th tick 진입 시 hash 3ebe603e 유지 시 정체 2회차 도달 — RULE-01 격리 분기 검토 (단 (1)(b) 의 분기 판정 게이트 적용: TSK-05/06 chain 진행 중이면 격리 부적합 = 박제 보존, chain 완료 후 unchecked 잔존이면 inspector 의무 위반 = 격리). 본 분기는 일반 격리 정체 (수단 중립 메타 spec + 환경 회귀) 와 다른 chain 의존 정체 — 격리 시점은 developer 완료 신호 검출 후 판정.
