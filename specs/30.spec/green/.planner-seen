# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T(37th cycle) — carve TSK-20260421-90 (husky v8→v9 hook 진입점 전환).
# Session summary:
#   HEAD (세션 시작) = 42cf572 (a11y green 편집 관련 수동 커밋 후 working tree clean).
#   직전 ledger HEAD=4dc9aea. 그 사이 커밋 체인:
#     4dc9aea → 3284490 (planner 36th cycle no-op) → d1a97a1 (inspector Phase 1 reconcile 0/0 ack 3회 연속 no-op)
#     → (inspector 가 REQ-20260422-044 흡수 → Phase 3 green 생성: foundation/husky-hook-entrypoint.md)
#     → 42cf572 (a11y 수동 작업 커밋, planner 영역 밖).
#   본 세션 입력 (green, HEAD=42cf572 기준): **1 spec file 신규 유입**.
#     - `specs/30.spec/green/foundation/husky-hook-entrypoint.md` (REQ-20260422-044, sha256=6c384fc4...).
#     - `specs/30.spec/green/common/`: 비어있음.
#     - `specs/30.spec/green/components/`: 비어있음.
#
#   RULE-03 선결 점검 (순서 고정):
#     (1) pause lock: `.claude/locks/` 부재 — 통과.
#     (2) 입력 큐: **1건 (husky-hook-entrypoint.md)** — 처리 진행.
#     (3) 하류 임계치 TASK_READY_MAX=10 : `40.task/` 잔존 0건 → 통과 (여유 10).
#     (4) 처리.
#
#   승격 판정 (foundation/husky-hook-entrypoint.md):
#     - `[WIP]` == 0 → 통과.
#     - unchecked `- [ ]` == **9건** (§테스트 현황 2 + §수용 기준 7 ─ FR-01~06 + 스코프). → 실패.
#     - `^#+ .*To-Be` == 0 → 통과.
#     - §변경 이력 TSK/커밋 = HEAD=d1a97a1 박제됨 → 통과.
#     → **승격 불가** (unchecked 9건 잔존). Carve 분기.
#
#   Carve 분석:
#     - spec grep-baseline (a)~(f) 6개 게이트 중 (a)(b)(c)(d)(e) 5개 위반 상태, (f) 관찰 계약 baseline 1개.
#     - FR-01 (.husky/* shim 제거) / FR-02 (prepare 전환) / FR-03 (husky dep ≥9) / FR-06 (.husky/_ 제거) 는 **불가분 원자 단위**:
#       - `npm install` 이 lockfile + node_modules/husky 를 v8→v9 로 한 번에 교체하므로 분할 시 중간 상태에서 hook broken.
#       - hook 파일 (pre-commit/pre-push) 재작성은 husky dep 버전 교체와 동시 수행해야 FR-04 관찰 계약 보존.
#     - 따라서 **TSK 1건 (원자)** 으로 carve. 독립 `git revert` 1회로 롤백 가능.
#     - depends_on: [] (선행 task 부재).
#
#   TSK ID 중복 검증:
#     - `grep -rn "TSK-20260421-..." specs/{40.task,60.done,50.blocked}/` → 최대 NN=89 사용.
#     - TSK-20260421-90 → 미사용 확정. 할당.
#
#   grep dry-run (baseline 6 gate, HEAD=42cf572 실측):
#     (a) `grep -nE "\"husky\":\s*\"\^?8" package.json` → 1/1 hit (`package.json:55`). spec 박제 일치.
#     (b) `grep -nE "\"prepare\":\s*\"husky install\"" package.json` → 1/1 hit (`package.json:25`). 일치.
#     (c) `grep -rn "_/husky.sh" .husky/` → 2/2 hits (`.husky/pre-commit:2`, `.husky/pre-push:2`). 일치.
#     (d) `ls -d .husky/_` → 1/1 (존재, husky.sh 포함). 일치.
#     (e) `node -p "require('./node_modules/husky/package.json').version"` → `8.0.2`. 일치.
#     (f) `grep -nE "npx lint-staged|npm test" .husky/pre-commit .husky/pre-push` → 2/2 hits
#         (`.husky/pre-commit:4`, `.husky/pre-push:4`). 일치.
#     → **grep dry-run: 6/6 match** (drift 없음, spec baseline 과 실측 완전 일치).
#
#   발행:
#     - `specs/40.task/TSK-20260421-90-husky-v8-to-v9-hook-entrypoint-migration.md` (신규).
#       · §스코프 규칙 섹션 포함 (grep 게이트 DoD 있음 — RULE-06 필수 조건 충족).
#       · expansion: 불허 (수정 파일 = package.json + .husky/pre-commit + .husky/pre-push + .husky/_ 제거 + package-lock.json 한정).
#       · baseline 6개 grep 게이트 실측 수치 박제.
#       · DoD = FR-01~04 + FR-06 에 대응하는 grep 후조건 + lint/test/수동 관찰 계약.
#
#   승격 mv: **0건** (승격 불가 확정).
#   blocked mv: **0건** (신규 유입, stale=0).
#   carve mv: **0건** (green 은 carve 후에도 원위치 유지 — carve 이벤트는 task 발행이며 green mv 아님).
#
#   RULE-01 점검:
#     - 본 세션 변경 = `.planner-seen` ledger edit (planner writer 영역) + `40.task/TSK-...` 발행 (planner writer 영역).
#     - `40.task/**` 는 .gitignore 로 git 추적 제외 (기존 파이프라인 규약).
#     - green 본문 미터치 (승격 불가 판정 후 원위치 유지).
#   RULE-02 점검:
#     - staged 자기 영역 밖 없음 — unstage 불요.
#     - 커밋 scope = `task(planner): carve TSK-20260421-90 (REQ-044 husky v8→v9 hook 진입점 전환)`.
#     - push 금지 (developer 전용).
#   RULE-04 점검:
#     - 보고 notes: `carve: 0 -> TSK-20260421-90 (1 신규 원자 task)` + `grep dry-run: 6/6 match` + `promote: 0` + `blocked: 0`.
#   RULE-06 점검:
#     - TSK-90 에 §스코프 규칙 섹션 박제 (grep 게이트 DoD 有 → 필수 조건 충족).
#     - baseline = spec grep-baseline 6개 실측 수치 그대로 전사.
#     - expansion=불허 rationale 박제.
#   RULE-07 점검:
#     - spec 콘텐츠 미터치.
#
#   **운영 관찰**:
#     - 36th cycle 까지 이어진 no-op 체인이 REQ-044 유입으로 종료됨.
#     - 37th cycle 에서 단일 원자 task carve 발행. TASK_READY_MAX 여유 10→9.
#     - green 잔존 1건 (husky-hook-entrypoint.md) 는 task DoD 체크박스 충족 및 §변경 이력 커밋 해시 박제 시 차기 이후 cycle 에서 승격 후보.
#
#   **파이프라인 상태 (37th cycle 종료 시)**:
#     - 30.spec/green/**: 1건 (foundation/husky-hook-entrypoint.md, stale=0, sha256=6c384fc4...).
#     - 30.spec/blue/**: 15 files (변경 없음).
#     - 40.task/: **1건** (TSK-20260421-90).
#     - 50.blocked/spec/common/: 2건 (a11y + error-reporter-channel) — 변화 없음.
#
# ledger entries (잔존 green):
# - specs/30.spec/green/foundation/husky-hook-entrypoint.md 6c384fc4140de8e11f41877c693d705f2a22189d9d5bb669ef3ac57daff4101b 0
#
# promoted (→ 30.spec/blue/): 본 cycle 0건.
# blocked (→ 50.blocked/spec/): 본 cycle 0건.
# carves (→ 40.task/):
# - 37th cycle: TSK-20260421-90 (husky v8→v9 hook 진입점 전환, REQ-20260422-044, depends_on=[], supersedes=없음).
#
# ready 큐: count=1. 신규 유입 직후.
# next cycle (38th+):
#   - **developer (15m)**: TSK-20260421-90 수행 → 성공 시 `60.done/YYYY/MM/DD/task/` 이동, spec §변경 이력 에 TSK 슬러그·커밋 해시 박제 요청.
#   - **planner (38th+)**: developer 완료 후 green 의 unchecked 9건이 해소되면 승격 판정 재수행. 미해소면 stale=1.
#   - **inspector (30m)**: Phase 1 reconcile (매 세션 필수 `reconcile: N/M ack`).
#   - **discovery (1h)**: 10.followups / /revisit 경로.
