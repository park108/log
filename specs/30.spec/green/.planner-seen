# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T(session+1)Z
# Session: 2 carves (no promotions). 5 green all unchecked > 0 → 승격 0.
#   신규 green 2건 최초 인지:
#     - common/geturl-render-path-test-audit-spec.md (hash b3137923, REQ-20260421-003) → TSK-20260421-44 발행 (App.test.jsx stubMode 가드 1건 — TSK-37 이디엄 승계, FR-01 감사 표 + FR-02 가드 단일 책임).
#     - common/test-isolation-shuffle-safety-spec.md (hash bd27261f, REQ-20260421-004) → TSK-20260421-45 발행 (3축 shuffle flake 제거 — LogItem restoreAllMocks / msw.test describe.sequential / File.test QueryClient race; depends_on TSK-20260421-42 선행 명시).
#   기존 3 pending green stale_cycles 증가:
#     - deps/msw-test-idioms-unification-spec.md (hash 불변 2c4b96bc, TSK-20260420-41 ready): unchecked 16 → 미승격, stale 2 → 3 (≥3cycle 경계 진입; 다음 세션에서도 hash 불변·미승격 지속 시 RULE-01 상 50.blocked/spec/ 이동 후보 재평가 — 단, TSK-41 ready 큐 생존 중이므로 본 세션은 유지).
#     - deps/react-19-findby-timing-stabilization-spec.md (hash 불변 b5d9e93e, TSK-20260421-43 ready): unchecked 15 → 미승격, stale 0 → 1.
#     - deps/fake-timer-teardown-residual-cleanup-spec.md (hash 불변 97cedb50, TSK-20260421-42 ready): unchecked 14 → 미승격, stale 0 → 1.
# ready 큐: TSK-20260420-41, TSK-20260421-42, TSK-20260421-43, TSK-20260421-44, TSK-20260421-45 (count=5, TASK_READY_MAX=10 여유).
# carve 계보:
#   - TSK-20260421-44 (신규, supersedes 없음).
#   - TSK-20260421-45 (신규, supersedes 없음, depends_on TSK-20260421-42).
# grep dry-run (carve 별 실측 대조, HEAD=515d9b3):
#   - TSK-44 (geturl-render-path-audit): `grep -rn "getUrl()" src --include="*.{js,jsx}"` → 9/9 match (spec baseline 런타임 6 + test 2 + setupTests 주석 1 일치). `grep -rn "userAgentParser(" src` → 13 실측 vs spec 14 (내부 정의 1건 차이, drift fixed in TSK §grep-baseline). `grep -n "stubMode" src/App.test.jsx` → 0/0 match (FR-02 대상). `grep -nE "findByText.*park108" src/App.test.jsx` → 3/3 match at :39, :317, :376 (spec :36 → 실측 :39 라인 drift, carve 수치 불변 3건).
#   - TSK-45 (test-isolation-shuffle-safety): `grep -n "vi\.spyOn" src/Log/LogItem.test.jsx` → 12/12 match at :53,:54,:113,:114,:180,:181,:235,:236,:290,:291,:351,:352. `grep -n "vi\.restoreAllMocks" src/Log/LogItem.test.jsx` → 0/0 match (FR-01 대상). `grep -rn "vi\.restoreAllMocks" src --include="*.test.*"` → 10 실측 in 6 files vs spec 9 in 5 files (LogItemInfo.test.jsx:58 신규 편입, drift fixed). `grep -nE "toHaveBeenCalledTimes|sibling|cycle" src/test-utils/msw.test.js` → 12 hits (sibling-it 2 블록 :28-37, :52-57 확인). `grep -n "no data on prod server|queryClient|QueryClient" src/File/File.test.jsx` → 2 hits at :34, :37 (describe + test; spec §Baseline :55 → 실측 :34 라인 drift). `src/test-utils/timing.js` → absent (TSK-43 미완료, 옵션 2 선택 시 TSK-43 선행 필요; 옵션 1 기본 권장).
# 총 grep dry-run 9/9 match (carve 별 게이트 전부 실측 대조 완료, 2건 drift — spec baseline 은 afe109e 스냅샷, 현 HEAD=515d9b3 로 소폭 drift 는 TSK 문서 §grep-baseline 에 실측값 박제로 정합).
30.spec/green/deps/msw-test-idioms-unification-spec.md 2c4b96bcd5a280056138d8546569e9e280fee667154259577763a073255719cf 3
30.spec/green/deps/react-19-findby-timing-stabilization-spec.md b5d9e93ed288177673e17ba1012d54fc77f26e20722a1071ac1b43d5c4b133a4 1
30.spec/green/deps/fake-timer-teardown-residual-cleanup-spec.md 97cedb50c645b512b563f2bb33ed251b2e67ce6830611f548dc5f8b5266345a9 1
30.spec/green/common/geturl-render-path-test-audit-spec.md b31379230e2b28e677c5497537f64efa203878e3a9e568817b06ca7150fefc4a 0
30.spec/green/common/test-isolation-shuffle-safety-spec.md bd27261fdbd3c24b4ad4db3ac13e37413841ebfdad575f31138773dcfd06b4bc 0
