# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (16th cycle) — 3 new task carves + 1 promotion + 0 new blocked.
# Session summary:
#   HEAD=51fe201 (`spec(inspector): Phase 1 reconcile 1/6 ack — common.md isAdmin matrix [x]`).
#   본 세션 입력: green spec 5개 (`common/test-idioms.md`, `components/common.md`, `components/log.md`, `foundation/ci.md`, `foundation/tooling.md`).
#
#   RULE-03 선결 점검:
#     (1) pause lock 없음 (`.claude/locks/` 부재). `.claude/pipeline.json` 없음 → `TASK_READY_MAX=10` 기본.
#     (2) 입력 큐 5 spec files (15차 ledger 의 0 → 5 증가).
#     (3) 하류 임계치: `40.task/` = 0 → +3 = 3, TASK_READY_MAX=10 여유 7 (OK).
#     (4) 처리 진입.
#
#   각 spec 분석 (승격 4조건: WIP=0, unchecked `[ ]`=0, To-Be=0, 변경 이력 박제):
#     - common/test-idioms.md (`hash=ce0bef..` 추정, stale=0): unchecked=0 / WIP=0 / To-Be=0 / 변경 이력 inspector/29d9da0 박제 → **승격**.
#     - components/common.md (`hash=...`, stale=0): unchecked=2 (`auth() SameSite 회귀 방어 테스트`, `(Must, REQ-025 FR-02 실현)`) → 미승격. TSK-62 carve.
#     - components/log.md (stale=0): unchecked=1 (LogItem DELETE shuffle) → 미승격. TSK-63 carve.
#     - foundation/ci.md (stale=0): unchecked=1 (수동 Node LTS 호환성, 별 task 대상) → 미승격. carve 보류 — "별 task 대상" 의 deferred 성격, 운영자 판단 시점 의존이므로 본 사이클 task 박제 보류 (다음 사이클 또는 명시 req 발행 대기).
#     - foundation/tooling.md (stale=0): unchecked=1 (lint-staged `.d.ts` 보강) → 미승격. TSK-64 carve.
#
#   carve 발생 (3건): TSK-20260421-62 (auth SameSite), TSK-20260421-63 (LogItem DELETE shuffle), TSK-20260421-64 (lint-staged d.ts). 신규 carve 이므로 `supersedes:` 메타 없음 (분할 없음 — 각 spec § 단일 unchecked → 단일 task).
#
#   grep dry-run 결과 (RULE-04 토큰):
#     - TSK-62: 4 baselines 박제, 4/4 match (sameSite 2 / SameSite=valid 0 / SameSite=invalid 0 / sameSite:site 2).
#     - TSK-63: 2 baselines 박제, 2/2 match (describe DELETE 4 / useMockServer 4).
#     - TSK-64: 3 baselines 박제, 3/3 match (lint-staged 2 / src pattern 1 / d.ts 0).
#     - 합계: **9/9 match**.
#
#   Task ID 중복 검증: `grep -rn "TSK-20260421-(62|63|64)" specs/` → 본인 파일 3 hits 만 → 충돌 없음.
#
#   승격 mv (1건):
#     - `30.spec/green/common/test-idioms.md` → `30.spec/blue/common/test-idioms.md`. (mkdir -p blue/common 선행 후 mv 원자.)
#     - green/common/ 디렉터리는 빈 채로 잔존 (planner 영역 외 — inspector 가 차후 새 spec 추가 시 활용 가능).
#
#   blocked 신규: 0. stale ≥ 3 cycle 없음 (전 spec 신규 등록 또는 carry-over).
#
#   RULE-06 스코프 규칙 섹션: 신규 task 3건 모두 grep 게이트 1건+ 포함 → 전건 § 스코프 규칙 섹션 신설 + grep-baseline 실측 박제.
#   RULE-07 정합 점검: green spec 5건 전부 불변식 한정 (시점·incident 귀속 plan 없음) → 50.blocked/spec/ 격리 0건.
#
# ledger entries:
# components/common.md   <hash>  0
# components/log.md      <hash>  0
# foundation/ci.md       <hash>  0
# foundation/tooling.md  <hash>  0
#
# ready 큐 (본 사이클 +3): count=3, TASK_READY_MAX=10 여유 7.
# next cycle (17th):
#   - **inspector (30m)**: 4건 잔존 green spec 의 unchecked 항목 진행도에 따라 추가 grep gate / baseline 갱신. ci.md 의 "별 task 대상" 항목은 운영자/discovery 판단 영역.
#   - **planner (17th+)**: 4건 green 의 unchecked 가 [x] 로 전환되면 (developer 가 TSK-62/63/64 완료 후 inspector reconcile 거쳐) 승격 진입.
#   - **본 사이클 writer 영역 변경**: `40.task/` +3 (TSK-62/63/64), `30.spec/green/` -1 (test-idioms.md mv out), `30.spec/blue/` +1 (test-idioms.md mv in), `.planner-seen` 갱신 1. 커밋 scope = `task(planner):` (carve 가 본 세션 핵심 산출).
