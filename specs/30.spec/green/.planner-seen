# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (21st cycle) — 2 promotions (ci.md, dependency-bump-gate.md) + 0 new carves + 0 blocked.
# Session summary:
#   HEAD=28bbf26 (`feat: a11y 패턴 B 정상화 — M9 UserLogin login/logout`).
#   본 세션 입력 (green): 3 spec files —
#     (1) `common/a11y.md` — 20th cycle carve 개시 후 inspector 가 TSK-69 @28bbf26 결과 반영 ([x] 플립 + §drift 표 M9 행 취소선 + §스코프 규칙 gate (a) 8→9 갱신).
#     (2) `foundation/ci.md` — **신규** (inspector REQ-20260421-034 재발행, 원 blocked 판본 `626e1330..` 대체 재발행).
#     (3) `foundation/dependency-bump-gate.md` — **신규** (inspector REQ-20260421-035 흡수, blocked req `react-19-runtime-bump-reattempt` followup (b) 갈래 spec-grade 추출).
#
#   RULE-03 선결 점검:
#     (1) pause lock 없음 (`.claude/locks/` 부재).
#     (2) `.claude/pipeline.json` 부재 → `TASK_READY_MAX=10` 기본.
#     (3) 입력 큐: 3 green spec files.
#     (4) 하류 임계치: `40.task/` = 3 (TSK-70/71/72 잔존, TSK-69 는 60.done 이동 완료 @28bbf26) → 본 세션 +0 = 3, 여유 7 (OK).
#     (5) 처리 진입.
#
#   세션 시작 시 git 상태 점검 (RULE-02): modified `specs/30.spec/green/common/a11y.md` (inspector 영역) + untracked `specs/30.spec/green/foundation/` (inspector 영역 2 신규 파일). 양쪽 모두 planner writer 영역 밖 → **staged 항목 0, reset 불필요**. 승격 mv 로 자기 영역 (blue) 으로 이동 후 커밋.
#
#   각 spec 승격 판정 (4조건: unchecked=0, WIP=0, To-Be=0, 변경 이력 TSK/커밋 박제):
#     - `common/a11y.md` (hash=bd32ed2b16f8d3a1c5b8b1e34e87743d8d6d91808c9374d2ce0b41075f36db53, 이전 7dd9830f.. 대비 변경됨 — inspector 편집 반영 → stale=0 유지):
#         unchecked=13 (Must, REQ-033 FR-03 M1~M8 테스트 [ ] 4종 + FR-05/06/08 [ ] 3종 + FR-02 M1~M8 정상화 [ ] 1종 + FR-03/05/06 수용 기준 [ ] 3종 + FR-07 상호참조 [ ] 1종 + FR-08 audit 수용 기준 [ ] 1종),
#         WIP=0, To-Be=0,
#         변경 이력 = 최초 등록 (`75498693`) + M9 ack (`28bbf26`, TSK-20260421-69) 2행 ✓
#         → **미승격**. 20th cycle ledger 지침대로 TSK-70/71/72 실현 후 inspector 재진입 → marker 플립 → 22nd+ cycle 에서 FR-05/06/08 결정 분기 carve.
#
#     - `foundation/ci.md` (신규 등장 → 이전 ledger 부재 → stale=0):
#         unchecked=0 ✓ (§테스트 현황 [x]×3, §수용 기준 [x]×7 전부 플립),
#         WIP=0 ✓, To-Be=0 ✓,
#         변경 이력 = inspector 재발행 1행 (`28bbf26`, REQ-20260421-034) ✓
#         → **승격 O**. `mv green/foundation/ci.md → blue/foundation/ci.md`. blue/foundation/ 기존재 (tooling.md 공존).
#
#     - `foundation/dependency-bump-gate.md` (신규 등장 → 이전 ledger 부재 → stale=0):
#         unchecked=0 ✓ (§테스트 현황 [x]×4, §수용 기준 [x]×6),
#         WIP=0 ✓, To-Be=0 ✓,
#         변경 이력 = inspector 흡수 1행 (`28bbf26`, REQ-20260421-035) ✓
#         → **승격 O**. `mv green/foundation/dependency-bump-gate.md → blue/foundation/dependency-bump-gate.md`.
#
#   승격 전 grep-baseline dry-run 검증:
#     [ci.md §스코프 규칙 3 gate]
#       (a) `grep -nE "uses:\s*actions/checkout@" .github/workflows/*.yml` → 1 hit (`ci.yml:16` `actions/checkout@v6`) ✓ 일치
#       (b) `grep -nE "uses:\s*actions/setup-node@" .github/workflows/*.yml` → 1 hit (`ci.yml:19` `actions/setup-node@v6`) ✓ 일치
#       (c) `grep -nE "uses:\s*actions/(checkout|setup-node)@v[0-9]+\.[0-9]+\.[0-9]+" .github/workflows/*.yml` → 0 hit (exit 1) ✓ 일치
#     [dependency-bump-gate.md §스코프 규칙 3 gate]
#       (a) `grep -nE "\"(dependencies|devDependencies)\":" package.json` → 2 hits (`package.json:6` dependencies, `package.json:42` devDependencies) ✓ 일치
#       (b) `grep -rnE "findDOMNode|\"ref\":\s*['\"][^'\"]+['\"]|defaultProps\s*=\s*\{" src --include="*.js" --include="*.jsx" --exclude="*.test.*"` → 0 hit (exit 1) ✓ 일치
#       (c) `grep -nE "console\.(warn|error)" src/setupTests.js` → 0 hit (exit 1) ✓ 일치
#   총 6/6 gate 정합 (positive 4/4 hit + negative 2/2 pass) — 두 spec 의 §스코프 규칙 grep-baseline 박제 수치와 완전 일치. 승격 검증 통과.
#
#   carve 발생: **0 건**. 이유:
#     - a11y.md 잔존 drift 는 20th cycle 에서 TSK-70/71/72 carve 완료 (M1/M2, M3/M6, M7/M8). 본 사이클 신규 carve 대상 없음. FR-05/06/08 결정 분기 carve 는 TSK-70/71/72 실현 후 자연 순서 유지 (22nd+ cycle).
#     - ci.md, dependency-bump-gate.md 는 두 spec 모두 신규 등장 즉시 4조건 전원 충족 → carve 대상 아님, 즉시 승격.
#
#   Task ID 중복 검증: 본 세션 신규 task 발행 0건 → 검증 생략 (기존 TSK-20260421-{42..72} 중 결번 TSK-20260421-54 확인 — 타 agent 영역, 무관).
#
#   승격 mv: **2건** (foundation/ci.md, foundation/dependency-bump-gate.md).
#   blocked mv: **0건**.
#
#   RULE-04/RULE-06 점검:
#     - 본 세션 신규 발행 task = 0 → RULE-06 §스코프 규칙 섹션 박제 의무 없음.
#     - 기존 큐 task (TSK-70/71/72) 는 20th cycle 에서 §스코프 규칙 완비 상태 발행 — 본 세션 rewrite 금지 (RULE-01) 정상 준수.
#     - grep dry-run = 6/6 match (두 spec 승격 검증 gate — RULE-04 notes 박제).
#   RULE-07 정합:
#     - a11y.md: 패턴 B 4 속성 동시 부여 시스템 불변식 박제 지속.
#     - ci.md: §기능 불변식 3건 (action 사용 + LTS 최신 + 메이저 floating) + §스코프 규칙 3 gate 평서형 선언 한정. 1회성 진단·incident patch 부재.
#     - dependency-bump-gate.md: §기능 불변식 2건 (FR-01 회귀 0 + FR-02 런타임 deprecated API 경고 0) 평서형 선언. grep-time 정적 차원 vs 런타임 경고 차원 분리 명시. 특정 React 런타임 버전 귀속 없음.
#   본 세션 spec 콘텐츠 수정 0 (planner 는 mv 만).
#
# ledger entries (잔존 green):
# common/a11y.md  bd32ed2b16f8d3a1c5b8b1e34e87743d8d6d91808c9374d2ce0b41075f36db53  0  # 21st cycle — inspector M9 ack 반영 버전. TSK-70/71/72 실현 대기 (developer 큐). 차기 carve 후보: FR-05 (M5 실측), FR-06 (M10 결정 A/B), FR-08 (audit 테스트 Should). FR-07 blue 상호참조 갱신은 a11y.md green→blue promote 단계 (planner).
#
# promoted (→ 30.spec/blue/):
# - foundation/ci.md  (21st cycle — REQ-20260421-034 재발행 즉시 4조건 충족)
# - foundation/dependency-bump-gate.md  (21st cycle — REQ-20260421-035 흡수 즉시 4조건 충족)
#
# blocked (→ 50.blocked/spec/):
# - (없음)
#
# ready 큐 (본 사이클 +0): count=3 (TSK-70/71/72), TASK_READY_MAX=10 여유 7.
# next cycle (22nd+):
#   - **developer (15m)**: TSK-70/71/72 동시 소비 가능 (depends_on=[] 서로 다른 파일 — Writer.jsx / LogItemInfo.jsx / Comment,CommentItem.jsx). 병합 순서 무관.
#   - **inspector (30m)**: 발행된 task 결과 반영하여 a11y.md green §테스트 현황 + §수용 기준 의 M1/M2 (TSK-70), M3/M6 (TSK-71), M7/M8 (TSK-72) marker [ ] → [x] 플립. §drift 표 해당 행 취소선. §스코프 규칙 gate (a) hits 수 업데이트 (9 → 15 예상).
#   - **planner (22nd+)**:
#       (i) TSK-70/71/72 전원 60.done 이동 + inspector marker 플립 완료 확인 후,
#       (ii) FR-05 (M5 `Link<span>` 실측 task) carve — 단일 task, grep-baseline 포함.
#       (iii) FR-06 (M10 `<img listitem>` 옵션 A/B 결정 + 구현 task) carve — 결정 후 spec §예외 갱신 필요 시 inspector 재진입 선행.
#       (iv) FR-08 (audit 테스트 도입 Should) carve.
#       (v) FR-07 blue 상호참조 갱신은 a11y.md 의 Must 항목 전수 [x] 후 promote 단계.
#   - **본 사이클 writer 영역 변경**:
#       - `30.spec/green/foundation/ci.md` → `30.spec/blue/foundation/ci.md` (mv 승격)
#       - `30.spec/green/foundation/dependency-bump-gate.md` → `30.spec/blue/foundation/dependency-bump-gate.md` (mv 승격)
#       - `30.spec/green/foundation/` rmdir (빈 디렉터리 정리)
#       - `.planner-seen` 갱신.
#     커밋 scope = `spec(planner):` (promotion 2건 단일 산출 — carve 부재).
