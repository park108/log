# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (22nd cycle) — 0 promotions + 1 carve (TSK-20260421-73) + 0 blocked.
# Session summary:
#   HEAD=1414eae (`feat: a11y 패턴 B 정상화 — M7/M8 Comment comment-toggle / reply-toggle`).
#   본 세션 입력 (green): 2 spec files —
#     (1) `common/a11y.md` (hash=171caada..; 이전 bd32ed2b.. → 변경됨 — inspector TSK-71 ack 편집분 반영). unchecked=11 잔존 (LogItemInfo.test M4/M5 row, Comment.test M7 row, CommentItem.test M8 row, FR-05 M5 실측 row, FR-06 M10 결정 row, FR-08 audit row, FR-02/03/05/06/07/08 수용 기준 [ ] 7종).
#     (2) `common/test-idioms.md` (hash=50eb6310..; 신규 등장 — untracked). blue(7447ef35..) 대비 **진화** — REQ-20260421-036 FR-05 console spy 비파괴 이디엄 (9) 신규 흡수 (+91 줄 진화, +1 변경이력 row @0f03547).
#
#   RULE-03 선결 점검:
#     (1) pause lock 없음 (`.claude/locks/` 부재).
#     (2) `.claude/pipeline.json` 부재 → `TASK_READY_MAX=10` 기본.
#     (3) 입력 큐: 2 green spec files.
#     (4) 하류 임계치: `40.task/` = 0 (TSK-69/70/71/72 모두 60.done 이동 완료 @1414eae) → 본 세션 +1 = 1, 여유 9 (OK).
#     (5) 처리 진입.
#
#   세션 시작 시 git 상태 점검 (RULE-02): modified `specs/30.spec/green/common/a11y.md` (inspector 영역; 이전 inspector 세션 편집) + untracked `specs/30.spec/green/common/test-idioms.md` (inspector 영역 재개방). 양쪽 모두 planner writer 영역 밖 → **staged 항목 0, reset 불필요**.
#
#   각 spec 승격 판정 (4조건: unchecked=0, WIP=0, To-Be=0, 변경 이력 TSK/커밋 박제):
#     - `common/a11y.md` (hash=171caada1c52a..., stale=0):
#         unchecked=11 (§테스트 현황 row 4건 [ ] + §수용 기준 row 7건 [ ]),
#         WIP=0, To-Be=0,
#         변경 이력 = 최초 등록 (`75498693`) + M9 ack (`28bbf26`) + M1/M2/M3/M6 ack (`0f03547`) 3행 ✓
#         → **미승격**. inspector 가 TSK-72 (M7/M8 @1414eae) ack 편집을 아직 반영하지 않음 — 다음 inspector 사이클에서 Comment/CommentItem row [x] 플립 + §drift 표 M7/M8 취소선 + gate (a)/(b) hits 수 업데이트 예상. 본 planner 세션은 carve 유보 (21st cycle ledger "TSK-70/71/72 전원 60.done 이동 + inspector marker 플립 완료 확인 후" 조건 중 marker 플립 조건 미충족).
#
#     - `common/test-idioms.md` (hash=50eb6310a2a06964c0eb5492f9c63386a2070e4a127286c27e338abc8fc0b526, 신규 등장 → 이전 ledger 부재 → stale=0):
#         unchecked=1 (§테스트 현황 row 9 "이디엄 (9) console spy 비파괴 보존 (REQ-036): ... 마이그레이션 별 task (planner 영역 carve 대상)"),
#         WIP=0, To-Be=0,
#         변경 이력 = REQ-021 최초 (`29d9da0`) + REQ-027 흡수 + REQ-029 흡수 + REQ-036 흡수 (`0f03547`) 4행 ✓
#         → **미승격**. unchecked 1건이 planner carve 지시 (spec 본문 §테스트 현황 (9) row 가 "마이그레이션 별 task (planner 영역 carve 대상)" 직접 명시) — 본 세션에서 carve 발행 → developer 실현 → inspector ack → marker 플립 → 차기 planner 사이클에서 승격 가능.
#
#   carve 발생: **1 건** — TSK-20260421-73-console-spy-nondestructive-migration.md.
#     - 출처 spec: `30.spec/green/common/test-idioms.md` §동작 이디엄 (9) (REQ-036 FR-05).
#     - 범위: `src/setupTests.js` 전역 afterEach 에 `vi.restoreAllMocks()` 1행 추가 + 21 test 파일의 module-level `console.X = vi.fn()` 43 hits 제거, 각 파일 `beforeEach(vi.spyOn(console, 'X').mockImplementation(() => {}))` 이관.
#     - 1 PR 크기 수용 근거: 동형 기계적 편집 (21 파일 × 2~3 행), 롤백 단위 `git revert <sha>` 단일, 의존성 0 (depends_on=[]), 회귀 영향 pass count 불변 (403 기준선 이상).
#     - 차기 inspector ack 경로: grep 게이트 (negative 본 0 + negative 보조 0 + positive 28+ + `vi.restoreAllMocks` 1) 통과 시 test-idioms.md §테스트 현황 (9) [ ] → [x] 플립 + §스코프 규칙 (9) baseline 갱신 (43/21 → 0/0).
#
#   Task ID 중복 검증: `grep -rn "TSK-20260421-73" specs/{40.task,60.done,50.blocked}/` → 0 hit @planner 발행 직전 → TSK-20260421-73 가용. 발행 후 1 hit (본 task) 예상.
#
#   grep dry-run (본 task §스코프 규칙 grep-baseline 박제 수치 검증):
#     (1) `grep -rnE "^console\.(log|warn|error|debug|info)\s*=\s*vi\.fn" src --include="*.test.js" --include="*.test.jsx"` (via Grep tool `^console\.(log|warn|error|debug|info)\s*=\s*vi\.fn` on `src/**/*.test.{js,jsx}`):
#       → **43 occurrences across 21 files** (App=2, WebVitalsItem=2, File/File=2, ImageSelector=2, ContentItem=2, common/common.test.js=2, WebVitalsMon=2, SearchInput=2, Monitor=2, LogSingle=3, FileUpload=2, VisitorMon=2, FileItem=2, Comment=2, Search=2, ApiCallItem=2, FileDrop=2, Navigation=2, Writer=2, Log=2, LogItem=2) ✓ 일치 (task §스코프 규칙 grep-baseline 박제 수치 `43 hits in 21 files` 와 완전 정합).
#     (2) `grep -rnE "^console\.(log|warn|error|debug|info)\s*=\s*(\(\s*\)|function)" src --include="*.test.js" --include="*.test.jsx"` → **0 hits** ✓ (task 박제 `0 hits` 정합).
#     (3) `grep -rnE "vi\.spyOn\s*\(\s*console\s*," src --include="*.test.js" --include="*.test.jsx"` → **7 hits in 6 files**: App.test.jsx:349/363, ErrorBoundary.test.jsx:25, Search.test.jsx:161/249, common.test.js:85, errorReporter.test.js:5 ✓ 일치 (task 박제 `7 hits in 6 files` 정합).
#     (4) `grep -n "vi\.restoreAllMocks" src/setupTests.js` → **0 hits** (전역 정책 미박제 현재) ✓ (task 박제 `0 hits` 정합).
#     총 4/4 gate 정합.
#
#   승격 mv: **0건**.
#   blocked mv: **0건**.
#
#   RULE-04/RULE-06 점검:
#     - 본 세션 신규 발행 task TSK-73 = `## 검증/DoD` 에 grep 게이트 4건 박제 (1건 negative 본, 1건 negative 보조, 1건 positive, 1건 전역 정책) → RULE-06 `## 스코프 규칙` 섹션 필수 조건 충족, 본 task 내 박제 완료 (expansion=불허, grep-baseline 실측 박제, rationale 2줄).
#     - grep dry-run = 4/4 match (RULE-04 notes 박제).
#   RULE-07 정합:
#     - a11y.md: 시스템 불변식 + §drift enumeration + §예외 baseline 한정 유지 (본 세션 수정 0, planner 는 mv 만).
#     - test-idioms.md: 이디엄 (9) 신설은 inspector 소관 완료. 9 이디엄 계약 평서형 선언 유지. TSK-73 carve 는 "기계적 이디엄 마이그레이션 1회성 배치" 가 아니라 "negative baseline 43 → 0 수렴 + 전역 정책 도입" 으로, 본 task 내부 §범위 밖 절에 "1회성 배치 리팩터 지시 지양" 원칙 명시 — 본 spec (green) 자체는 불변식만 박제 지속.
#
# ledger entries (잔존 green):
# common/a11y.md  171caada1c52a02473d09a04f5dd05d1af77f0faa0728f85fa41c9ce1fa1b3c0  0  # 22nd cycle — inspector M1/M2/M3/M6 ack 반영 버전 (TSK-72 M7/M8 ack 미반영). marker 플립 대기. 차기 carve 후보: FR-05 (M5 `Link<span>` 실측), FR-06 (M10 `<img listitem>` 옵션 A/B 결정), FR-08 (audit 테스트 Should). FR-07 blue 상호참조 갱신은 a11y.md 전수 [x] 후 promote 단계.
# common/test-idioms.md  50eb6310a2a06964c0eb5492f9c63386a2070e4a127286c27e338abc8fc0b526  0  # 22nd cycle — inspector REQ-036 흡수 버전 (이디엄 9 신설, diff vs blue: +91 줄). §테스트 현황 (9) [ ] 1건 잔존 → TSK-73 carve 발행 완료. developer 실현 + inspector ack 후 승격 가능 (차기 23rd+ cycle).
#
# promoted (→ 30.spec/blue/):
# - (없음)
#
# blocked (→ 50.blocked/spec/):
# - (없음)
#
# ready 큐 (본 사이클 +1): count=1 (TSK-73), TASK_READY_MAX=10 여유 9.
# next cycle (23rd+):
#   - **developer (15m)**: TSK-73 실현. 21 test 파일 + setupTests.js 1 파일 변환. `npm run lint/test/build` 통과 + grep 게이트 4/4 통과 + pass count 현 기준선 이상.
#   - **inspector (30m)**:
#       (a) TSK-72 (M7/M8 @1414eae) 결과 반영하여 a11y.md green §테스트 현황 M7/M8 row [ ] → [x] 플립. §drift 표 M7/M8 행 취소선. §스코프 규칙 gate (a) hits 수 업데이트 (13 → 15~16 예상). gate (b) hits 수도 업데이트 (20 → 22 예상).
#       (b) TSK-73 (console spy @차기 커밋) 결과 반영하여 test-idioms.md green §테스트 현황 (9) row [ ] → [x] 플립. §스코프 규칙 (9) baseline 갱신 (negative 본 43/21 → 0/0).
#   - **planner (23rd+)**:
#       (i) inspector marker 플립 완료 (TSK-72/73 각각) 확인 후,
#       (ii) test-idioms.md 4조건 (unchecked=0 포함) 전원 충족 → **승격 O** (mv green→blue).
#       (iii) a11y.md 는 FR-05/06/08 결정 분기 carve 진입 (별 task 3건 예상 — 각각 1 PR 크기 단일 task 또는 decision task + impl task 2단 분할 후보).
#   - **본 사이클 writer 영역 변경**:
#       - `40.task/TSK-20260421-73-console-spy-nondestructive-migration.md` (신규 carve)
#       - `.planner-seen` 갱신.
#     커밋 scope = `task(planner):` (carve 1건 단일 산출 — promotion/blocked 부재).
