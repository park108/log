# planner ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T13:38Z (31st cycle) — 0 promotion + 0 carve + 1 blocked (common/a11y.md stale=3 → 50.blocked/spec/common/).
# Session summary:
#   HEAD=c5eed40 (`feat: errorReporter 채널 단일화 — D6 FileUpload.jsx` — TSK-85).
#   직전 ledger HEAD=5190466. 그 사이 커밋 체인:
#     5190466 (inspector revert FileDrop 스윕) → 602a7bc (dev TSK-84 D5 FileDrop `refactor:`) → 5689925 (planner 30th — TSK-87 carve)
#       → c5eed40 (dev TSK-85 D6 FileUpload `feat:`) → 본 세션 시작.
#   30th 이후 진척: dev TSK-84/85 (D5/D6) 2건 ack (inspector `.inspector-seen` 에 박제), TSK-86 40.task/ 대기 (미소비), TSK-87 40.task/ 대기.
#   `40.task/` 잔존 (본 세션 시작 시): TSK-86 D7 Comment + TSK-87 coverage 진단 (2건).
#   working tree (본 세션 시작 시, 참고): M inspector-seen / M erc.md (inspector 편집) / M Comment.jsx / M Comment.test.jsx (developer 영역, TSK-86 진행중 추정) — 전부 planner 영역 밖, 터치 금지.
#   본 세션 입력 (green, HEAD=c5eed40 기준): 3 spec files:
#     (1) `common/a11y.md` — HEAD hash=e1d6de4806b2dd89fa50794c3b71be094207d489a4b1f662f02781a522d7b78a.
#         직전 ledger hash=e1d6de48.. — **불변** (D5/D6 커밋 + inspector ack 모두 a11y.md 미편집). stale 2→**3**.
#     (2) `common/error-reporter-channel.md` — HEAD hash=cfe4badd3be20bfb4927ac53a0e3957125b3d3e062d325f32a7178bfd31032bd.
#         직전 ledger hash=2ac91c37.. — **변경** (c5eed40 inspector Phase 1 ack 으로 D5/D6 `[x]` 플립 박제 + §drift 정상화 표 D5/D6 취소선 + §스코프 규칙 baseline (a) 4 hits / 1 file / (b) 29 hits 재박제). stale 1→0 리셋→**1**.
#     (3) `foundation/coverage-determinism.md` — HEAD hash=47062dd81da542c931603a31c5baf424f03409e9c4aa607b844e7018d88dbab6.
#         직전 ledger hash=47062dd8.. — **불변**. stale 0→**1**.
#
#   RULE-03 선결 점검 (순서 고정):
#     (1) pause lock: `.claude/locks/` 부재 — 통과.
#     (2) `.claude/pipeline.json` 부재 → `TASK_READY_MAX=10` 기본.
#     (3) 입력 큐: 3 HEAD green specs — 통과.
#     (4) 하류 임계치: `40.task/` = 2 (TSK-86/TSK-87). `TASK_READY_MAX=10` 여유 8 — 통과 (신규 carve 발행 여유 충분, 단 본 세션은 carve 0).
#     (5) 처리 진입.
#
#   세션 시작 시 git 상태 점검 (RULE-02):
#     working tree: `M .inspector-seen`, `M common/error-reporter-channel.md`, `M Comment.jsx`, `M Comment.test.jsx` — 전부 planner 영역 밖.
#     staged: 0. 자기 영역 밖 staged 항목 없음 → unstage 불요.
#
#   각 green spec 승격 판정 (4조건: unchecked=0, WIP=0, To-Be=0, 변경 이력 TSK/커밋 박제; HEAD=c5eed40):
#     - `common/a11y.md` (HEAD hash=e1d6de48.., stale 2→3):
#         unchecked: **1** (L180 수용 기준 FR-07 blue 상호참조).
#         WIP: 0. To-Be: 0. 변경 이력: 박제 완료.
#         → **미승격** (unchecked 1 > 0). 구조적 blocker (FR-07 가 blue 편집 요구 — planner/inspector 어느 쪽도 독단 수행 불가, writer 매트릭스 순환).
#         → **stale=3 도달 + 구조적 blocker** → RULE-03 "같은 diff-hash 2회 이상 정체" 기준 `50.blocked/spec/common/a11y.md` 격리 + `_reason.md` 작성. RULE-05 `/revisit` 경로 진입.
#         → 신규 carve 0건.
#     - `common/error-reporter-channel.md` (HEAD hash=cfe4badd.., stale 1 — c5eed40 로 hash 변경 리셋):
#         unchecked: **5** (L197 D7 Comment.test FR-03, L198 grep 게이트 FR-02, L208 FR-02 수렴, L209 FR-03 수렴, L212 FR-06 blue 상호참조).
#         WIP: 0. To-Be: 0. 변경 이력: 박제 완료 (직전 30th 에서 관측한 unchecked 7 → c5eed40 inspector ack 반영 후 5로 감소 — D5/D6 테스트 2건 [x] 플립).
#         → **미승격** (unchecked 5 > 0). D7 (TSK-86 기발행, developer working tree 진행중) + FR-02/FR-03 수렴 대기 + FR-06 구조적 blocker.
#         → 신규 carve 0건 (TSK-86 기발행, FR-06 는 blue 편집 구조 blocker — D7 완료 후 FR-06 단독 잔존 시 재평가).
#     - `foundation/coverage-determinism.md` (HEAD hash=47062dd8.., stale 0→1):
#         unchecked: **2** (§테스트 현황 L85 FR-01 반복실행 coverage JSON diff 0 / L86 FR-02 수단 택1 이상 적용). 둘 다 명시 `planner 수렴 task 대기`.
#         WIP: 0. To-Be: 0. 변경 이력: 1건 (inspector 신설 @690aa74).
#         → **미승격** (unchecked 2 > 0).
#         잔존 [ ] carve 대상 분석:
#           - FR-02 수단 택1 이상 적용 은 FR-05 (수단 중립성) 강제 — planner 가 (a~f) 중 하나 독단 선택 불가.
#           - TSK-87 (coverage 진단 task, 40.task/ 대기) 가 수단 선택 근거를 제공하는 **선행 task** — 진단 완결 전 수렴 task carve 불가.
#           → 신규 carve 0건 (TSK-87 완결 → discovery 가 followup→req 승격 후 다음 planner cycle 에서 수렴 task carve).
#
#   carve 발생: **0건**.
#
#   grep dry-run: 본 세션 신규 carve 0 → grep dry-run N/A. notes 에 `grep dry-run: 0/0 match` 박제.
#
#   승격 mv: **0건**.
#   blocked mv: **1건** — `specs/30.spec/green/common/a11y.md` → `specs/50.blocked/spec/common/a11y.md` + `specs/50.blocked/spec/common/a11y_reason.md` 신규 작성.
#
#   RULE-01 점검:
#     - 본 세션 변경 = `specs/50.blocked/spec/common/a11y.md` (mv-in) + `specs/50.blocked/spec/common/a11y_reason.md` (create) + `specs/30.spec/green/.planner-seen` (edit).
#     - mv: `30.spec/green/** → 50.blocked/spec/**` planner writer matrix 허용.
#   RULE-02 점검:
#     - staged 자기 영역 밖 없음 — unstage 불요.
#     - 커밋 scope = `spec(planner):` (blocked 격리).
#   RULE-05 점검:
#     - blocked 격리 시 `_reason.md` 작성 및 `/revisit` 경로 진입 준비. `10.followups/` revive 는 운영자 판정.
#   RULE-06 점검:
#     - 신규 carve 0 → RULE-06 적용 대상 없음.
#   RULE-07 점검:
#     - 격리 대상 `a11y.md` 본문은 RULE-07 정합 (시스템 불변식 + 계약). 격리 사유는 writer 매트릭스 순환 (구조적 blocker) 이며 spec 콘텐츠 결함 아님. `_reason.md` 에 해소 경로 (운영자 revive/close) 명시.
#
#   **운영 관찰**:
#     - a11y.md 격리 성립 — FR-07 구조적 blocker 는 operator `/revisit` 로만 해소 가능. discovery 가 "blue components §접근성 상호참조 추가 req" 신설 → inspector 가 blue→green 복사·편집 후 재승격 경로.
#     - error-reporter-channel.md 는 FR-06 동일 구조적 blocker 존재하나 D7 완결 전 stale 누적 없음 (hash 변경 유지). D7 + FR-02/03 수렴 후 FR-06 단독 잔존 시점 재평가.
#     - coverage-determinism.md 는 FR-05 수단 중립성 보호 하에 TSK-87 진단 → followup → req → 수렴 task 의 2단계 경로. 현재 1단계 (TSK-87 발행됨) 진행 중.
#
# ledger entries (잔존 green; a11y.md 는 blocked 이동으로 제거):
specs/30.spec/green/common/error-reporter-channel.md cfe4badd3be20bfb4927ac53a0e3957125b3d3e062d325f32a7178bfd31032bd 1  # 31st cycle — HEAD hash 변경 (c5eed40 inspector Phase 1 ack TSK-84/85 로 D5/D6 [x] 플립 + §drift 표 취소선 + §스코프 규칙 baseline 갱신). stale 1→0 리셋→1. 잔존 unchecked 5건 (L197 D7 test + L198 grep 게이트 + L208 FR-02 수렴 + L209 FR-03 수렴 + L212 FR-06 blue 상호참조). TSK-86 (D7) 기발행·developer working tree 진행중. TSK-85 완결 이후 D7 완료 대기.
specs/30.spec/green/foundation/coverage-determinism.md 47062dd81da542c931603a31c5baf424f03409e9c4aa607b844e7018d88dbab6 1  # 31st cycle — HEAD hash 불변 (REQ-041 흡수 이후 편집 없음). stale 0→1. unchecked 2건 (§테스트 현황 FR-01 반복실행 0 lines diff / FR-02 수단 택1 이상). TSK-87 (진단 전용) 40.task/ 대기. FR-05 수단 중립성 보호 2단계 carve 경로.
#
# promoted (→ 30.spec/blue/):
# - (없음)
#
# blocked (→ 50.blocked/spec/):
# - specs/30.spec/green/common/a11y.md → specs/50.blocked/spec/common/a11y.md (stale=3 도달 + FR-07 구조적 blocker; reason: writer 매트릭스 순환 — planner/inspector 어느 쪽도 blue 편집 단독 수행 불가; RULE-05 /revisit 경로 진입).
#
# ready 큐: count=2 (TSK-86 / TSK-87), TASK_READY_MAX=10 여유 8.
# next cycle (32nd+):
#   - **developer (15m)**: TSK-86 D7 Comment working tree 진행중. TSK-87 진단 독립 병렬 가능.
#   - **inspector (30m)**:
#       (a) TSK-86 완결 시 Phase 1 reconcile ack + erc.md §drift 정상화 표 D7 취소선 + §테스트 현황 D7 [x] 플립 + §스코프 규칙 baseline (a) 0 hit 수렴 시 §수용 기준 FR-02/FR-03 수렴 `[x]` 플립.
#       (b) TSK-87 완결 시 followup 기반으로 coverage-determinism.md §테스트 현황 주범 관측 기록 (FR-05 중립성 유지).
#       (c) a11y.md blocked 이동 후 inspector `.inspector-seen` ledger 에서 해당 entry 제거 (다음 inspector cycle 에서 자율 갱신).
#   - **discovery (1h)**:
#       (i) TSK-87 산출 followup 을 req 로 승격 또는 close 판정 (coverage 진단 결과 기반).
#       (ii) a11y.md `_reason.md` 가 `/revisit` 으로 `10.followups/` revive 되면 "blue components §접근성 상호참조 추가 req" 신설 (운영자 판정 필요).
#   - **planner (32nd+)**:
#       (i) erc.md 는 D7 완결 + FR-02/03 수렴 후 FR-06 단독 잔존 시 재평가 — 동일 구조적 blocker 격리 고려.
#       (ii) coverage-determinism.md 는 TSK-87 followup 소비 req 발행 시 수렴 task carve 진입.
#       (iii) 신규 a11y-related req (operator revive 시) 가 inspector 에서 green spec 으로 흡수되면 다음 planner cycle 에서 promote 경로 재진입.
