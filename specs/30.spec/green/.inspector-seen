# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-05-17 @ b96bb5e → THIS — reconcile 0/0 ack (0 stale ≥3cycle). 본 세션 시작 직전 working tree 실측: `30.spec/green/foundation/` 디렉터리에 디스크 파일 0건 (이전 세션 추적 대상이던 devbin-install-integrity.md + path-alias-resolver-coherence.md 두 파일이 unstaged delete 상태로 git index 에서 검출 + 동일 슬러그 파일이 `50.blocked/spec/foundation/` 에 untracked 상태로 존재 — planner 13th tick (commit 267c449, island/runtime 격리) 이후 추가로 일어난 in-progress mv 행위로 추정되나 `b96bb5e` commit 본문은 devbin/path-alias 격리 부재 + carve 0건 박제). RULE-01 inspector writer 경계: `50.blocked/spec/`, `50.blocked/spec/foundation/*_reason.md`, devbin/path-alias 의 unstaged delete 는 모두 planner writer 영역 — inspector 본 세션 커밋에서 ABSENT (`git add` 파일 명시 외부 — `30.spec/green/foundation/vite-jsx-transform-channel-coherence.md` create + `.inspector-seen` 갱신 + `20.req/20260517-vite-jsx-transform-channel-coherence.md → 60.done/2026/05/17/req/` mv 의 3 file 만 staging). Phase 1 분모: 본 세션 디스크 실측 분모 = 0 (devbin / path-alias 가 30.spec/green/foundation/ 에 디스크 부재 → 추적 대상 외부, vite-jsx-transform 은 본 세션 신규 create 로 차기 세션부터 추적). ack = 0. RULE-04 토큰: `reconcile: 0/0 ack`.
# 직전 (35ba9d4, 2026-05-17 — REQ-065 흡수) 세션의 추적 분모 1 (devbin 1st cycle) + REQ-065 흡수로 path-alias 신설 후 분모 2 (devbin 2nd + path-alias 1st 예상) → 본 세션 진입 시점 working tree 가 두 파일을 disk 부재 + 50.blocked 위치 untracked 로 표시. 본 세션은 RULE-02 writer 경계 정합으로 두 파일의 mv 행위 확정/원복 어느 쪽도 수행하지 않음 — 정식 복귀 경로 (RULE-05 /revisit) 또는 planner 후속 tick 의 명시 격리 커밋 (50.blocked staging + _reason.md staging + green delete staging 단일 commit) 이 행해질 때까지 inspector 추적 대상 외부. devbin / path-alias 의 stale_cycles 추적은 다음 두 시나리오 분기: (a) planner 가 50.blocked staging 으로 격리 commit 확정 → 추적 분모 외부 영구 (RULE-05 /revisit 의존) / (b) 누군가 mv 를 원복하고 30.spec/green/foundation/ 로 복귀 → 본 spec 추적 분모 재진입 (stale_cycles 는 cycle 단절 — 신규 1st 로 재출발).
# Phase 2 처리: 입력 큐 REQ-20260517-066 (vite-jsx-transform-channel-coherence) 1건 흡수. RULE-07 자기검증 통과 — (i) 평서형 효능 (채널 등록 수 + 책임 분담 + 이중 변환 부재 + runtime 모드 단일성 + 단계 분리 + build/test 동일성 + bump 후 갱신 의무 + 선언 단일성/산출 동등성 직교 동시 성립) / (ii) 시점 비의존 (특정 채널 등록 수·메이저 숫자는 §스코프 규칙 baseline 에만 박제) / (iii) 반복 검증 가능 (`grep -nE` + `npm run build` + bundle grep 1-line) / (iv) incident 귀속 부재 / (v) 수단 중립 (§ 동작 9 자기 검증 — `기본값|권장|우선|default|best|root cause|가장 효과적` 매치 모두 § 동작 9 카테고리 (i)~(iv) 내부). 신규 green spec `30.spec/green/foundation/vite-jsx-transform-channel-coherence.md` create (9개 불변식 § 동작 1~9 박제). req mv `20.req/20260517-vite-jsx-transform-channel-coherence.md → 60.done/2026/05/17/req/`. GREEN_PENDING_MAX=20 — 본 세션 후 green spec disk 상 총 1건 (1 << 20 임계치 미달, planner WIP mv 가 commit 으로 확정될 경우 추후 1~3건 범위 변동 가능).
# 입력 큐 잔여: `20.req/20260517-test-discovery-population-coherence.md` 1건 (본 세션 시작 후 가시화된 신규 req — 차기 inspector tick 의 Phase 2 흡수 대상).
# 차기 inspector tick 우선순위: (1) planner 의 devbin / path-alias mv 행위 확정 시점에 본 seen-state 분기 재정합 / (2) vite-jsx-transform-channel-coherence.md 1st cycle 추적 + baseline 재실측 (§ 동작 3 production build 측정 채널은 REQ-064 충족 후 활성) / (3) test-discovery-population-coherence 신규 req 흡수.
# 본 세션 commit diff = `specs/30.spec/green/foundation/vite-jsx-transform-channel-coherence.md` create + `specs/30.spec/green/.inspector-seen` 갱신 + `specs/20.req/20260517-vite-jsx-transform-channel-coherence.md → specs/60.done/2026/05/17/req/` mv (3 file). 작업 트리 잔존 (devbin/path-alias unstaged delete + 50.blocked/ untracked) 은 RULE-02 writer 경계 정합으로 staging 제외.
#
# == seen-state ==
specs/30.spec/green/foundation/vite-jsx-transform-channel-coherence.md	NEWLY-CREATED-b96bb5e-baseline	0
