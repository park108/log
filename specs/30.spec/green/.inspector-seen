# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-04-22 @ ab88167 — no completion-ready WIP markers in green.
# src-typescript-migration.md: 6 [ ] markers (FR-01~06 게이트·수용기준). 실측 HEAD=ab88167 — `.jsx` 59 / `.js` 런타임 32 / `.test.jsx` 27 / `.test.js` 15. FR-01/02 수렴 조건 (전원 0) 미달 — 정상 WIP. 파생 task: TSK-20260422-01~04 (3건 done, 1건 in-queue). stale_cycles=0.
# tsconfig-test-ambient-globals.md: 6 [ ] markers (FR-01~06). 실측 HEAD=ab88167 — `npm run typecheck 2>&1 | grep -cE "TS2304"` → 118, 대상 `.test.t(sx)` 5건. FR-01 수렴 조건 (0 hit) 미달 — 정상 WIP. 파생 task: TSK-20260422-05 in-queue. stale_cycles=0.
# tooling.md (new in green this session): REQ-028 이력 행 전원 [x] 유지 + REQ-053 FR-01~06 · NFR-03 체크 7건 [ ] 신규 박제 (§동작 5 rule swap). HEAD=ab88167 baseline: vanilla `no-unused-vars` 2 hit @`eslint.config.js:70,74` / `@typescript-eslint/no-unused-vars` 0 hit / disable 주석 2 hit @`src/test-utils/msw.ts:41`,`src/Image/ImageItem.tsx:6` / `typescript-eslint@^8.58.2` 기 설치 / `npm run lint` exit 0. stale_cycles=0.
# reconcile this session: 0/3 ack (flip 후보 0, drift 없음).
