# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-05-17 @ 9e5f00a → THIS — reconcile 3/3 ack (2 spec PASS marker flip + 1 spec cycle 추적).
#
# 본 세션 디스크 실측 (tick 시작 시점):
#  - HEAD = 9e5f00a (planner 26차 tick — TSK-07/08 발행).
#  - specs/30.spec/green/ 직계: marker 파일 2개 (.planner-seen, .inspector-seen) 만 잔존.
#  - specs/30.spec/green/foundation/ : 3 spec (src-spec-reference-coherence + vite-env-boundary-typing + node-modules-extraneous-coherence).
#  - specs/30.spec/green/components/ : 본 세션 carry-over 신규 1 spec (common.md, REQ-074 흡수용).
#  - specs/30.spec/green/foundation/tooling.md : 본 세션 carry-over 신규 (REQ-075 흡수용).
#  - working tree: src/types/env.d.ts + src/{File,Image}/api{,.mock}.ts 5건 미커밋 변경 (developer 영역 — TSK-07 진행 중 추정. RULE-02 자기영역 밖, unstage 불요 — staged 0).
#  - specs/20.req/ : 1건 (REQ-076 deleted-spec-restore-batch-2-7) — 본 세션 보류 (6 토픽 묶음 — 차기 tick 분할 흡수).
#  - specs/40.task/ : 2건 (TSK-20260517-07 vite-env-boundary-typing-cast-purge + TSK-20260517-08 vite-env-boundary-typing-ci-gate, planner 26차 tick 발행, developer 진행 중).
#  - specs/50.blocked/spec/foundation/ : 6 spec + 6 reason (직전 누적 동일 — vite-jsx + test-discovery + eslint-linter + log + monitor + root-entry).
#  - specs/50.blocked/req/ : 디스크 부재.
#  - .claude/locks/ : 부재 (pause 0).
#
# Phase 1 추적 (분모 3):
#  - foundation/src-spec-reference-coherence.md (cycle 1):
#    * G1 grep gate: 실측 0 hit (HEAD=9e5f00a, `grep -rnE 'specs/30\.spec/[^"`\ ]*-spec\.md' src` → exit 1 / 0 lines). 직전 ack 유지.
#    * G2 disk exist gate: 실측 0 path 추출 (참조 자체 삭제로 추출 unique paths 비어짐) / 0 MISSING. 직전 ack 유지.
#    * G3 CI/pre-commit 게이트 박제: TSK-20260517-06 회수 @`1cfa78e` (HEAD 조상). `.husky/pre-commit` conditional staged 매칭 `bash scripts/check-spec-coherence.sh` 호출 + `package.json scripts.lint:spec-coherence` 노출 + `scripts/check-spec-coherence.sh` POSIX shell (G1 -spec suffix 0 hit + G2 disk-existence test -e PASS 통합, 위반 시 stderr 분기 exit 1). 객관 grep 게이트 PASS + commit 메시지 명확 (`feat: src→spec reference coherence CI/pre-commit gate (TSK-20260517-06)`) → marker 플립 정당.
#    * 플립 대상: 테스트 현황 G3 + 수용 기준 Should (CI 게이트 자동 실행) 2 marker.
#    * 유보: Must (1 PR 동시 충족) — 단일 사례 박제로 marker 유보, 차기 이벤트 시 재검증.
#    * ack 발생 → stale_cycles 0 으로 리셋.
#  - foundation/vite-env-boundary-typing.md (cycle 1):
#    * G1 grep gate: HEAD=9e5f00a 실측 8 hits 유지 (위반). working tree 변경 (`as string` 제거 + S_type 확장 4 file + env.d.ts) 미커밋 — TSK-07 developer 영역. marker 플립 보류 (HEAD 기준).
#    * G2 동치 게이트: HEAD=9e5f00a 실측 `S_src=10 / S_type=0` 위반 유지. working tree 변경으로 `S_type=10` 일치 — 미커밋.
#    * G3 typecheck 게이트: 미실측 (REQ-061 수렴 의존).
#    * G4 CI 게이트: TSK-20260517-08 발행 (planner 26차 tick), developer 미시행 — `[ ]` 유지.
#    * hook-ack: 미발생 — marker 0 플립. stale_cycles += 1 (0 → 1).
#  - foundation/node-modules-extraneous-coherence.md (cycle 1):
#    * G1 grep gate: HEAD=9e5f00a 실측 `npm ls --depth=0 | grep -cE " extraneous$"` → 0. baseline 837 → 0 회수.
#    * G2 등식: HEAD=9e5f00a 실측 `N=29 / M=29 / diff=0`. baseline `N=29 / M=866` 회수.
#    * `package.json` 변경 0, working tree 0 — 운영자가 외부 수단 (`npm install` / `npm prune` 등) 으로 환경 회복 (node_modules 는 untracked). 게이트는 결과 측정이므로 marker 플립 정당.
#    * 회귀 0 hook-ack: `npm run build` exit 0 (311ms) / `npx vitest run` 48 files / 439 tests PASS / `npm run lint` exit 0 — 본 세션 실측 PASS.
#    * 플립 대상: 테스트 현황 G1·G2 + 수용 기준 Must G1·Must G2·Must 회귀 0·Must 범위 제한 6 marker.
#    * 유보: G3·G4 / Should 수단 박제 / Should 자동 게이트 / Must 시점 비의존 (단일 사례) — 차기 이벤트 시 재검증.
#    * ack 발생 → stale_cycles 0 으로 리셋.
#
# Phase 2: 입력 큐 2건 (본 세션 시작 직후 디스크 갱신 — discovery 다른 세션 발행) 중 1건 흡수 + 1건 보류.
#  - REQ-20260517-074 error-fallback-skeleton-contract (세션 시작 시 큐에 존재):
#    * RULE-07 양성: 분기 enum / variant enum / 단일 채널 / a11y 속성 모두 평서문 선언 + grep 게이트 + 테스트 박제로 반복 검증 가능 + 시점 비의존. PASS.
#    * 박제 경로: blue `components/common.md` → green 으로 carry-over 후 §에러 처리 / §UI 셸 / §ErrorFallback 분기 계약 (신설) / §Skeleton variant 계약 (신설) / §스코프 규칙 grep-baseline (d-1/d-2/e-1/e-2/e-3 5 gate) / §테스트 현황 5 marker / §수용 기준 9 marker / §변경 이력 1행 박제. inspector 영역 (green) → planner promote 시 blue 흡수.
#    * baseline 실측 (HEAD=9e5f00a): `console.error` 직접 호출 0 lines (errorReporter.ts + *.test.* 제외) / `reportError` 호출 50 hits / ErrorFallback 분기 메시지 2 hits @:31,32 / VALID_VARIANTS 3 hits / role="alert" 1 hit / role="status" + aria-label 2 hits.
#  - REQ-20260517-075 lint-staged-untracked-preservation (본 세션 중 신규 등장 @12:41):
#    * RULE-07 양성: lint-staged 사이클 untracked 보존 / glob 한정 / 훅 본문 사이드이펙트 0 — 시점 비의존 평서문 + grep 0 hit 게이트 + `git status --short` 재현 가능. PASS.
#    * 박제 경로: blue `foundation/tooling.md` → green 으로 carry-over 후 §동작 7 신설 (7.1~7.5) + §스코프 규칙 grep-baseline (m)(n)(o)(p) 4 gate + §테스트 현황 3 marker + §수용 기준 8 marker (FR-01/02/04 + NFR-01/03 즉시 [x], FR-03/05 + NFR-02 task/이벤트 대기 [ ]) + §변경 이력 1행 박제.
#    * baseline 실측 (HEAD=9e5f00a): `grep -nE '"specs' package.json | grep -E "lint-staged|jsx|tsx"` → 0 hit / `grep -nE "git add -A|git stash" .husky/pre-commit` → 0 hit / `lint-staged@^16.4.0` 박제.
#  - REQ-20260517-076 deleted-spec-restore-batch-2-7 (본 세션 중 신규 등장 @12:43):
#    * RULE-07 양성: 6 토픽 (env / sanitizeHtml / markdownParser / accessibility / css-modules / search-abort) 모두 시점 비의존 계약 PASS.
#    * 본 세션 보류: 6 토픽 분할 흡수 부담 — 차기 tick 분할 처리. 큐 잔존 (no-op 위반 없음, RULE-02 보류 정상).
#  - req mv: specs/20.req/20260517-error-fallback-skeleton-contract.md + 20260517-lint-staged-untracked-preservation.md → specs/60.done/2026/05/17/req/.
#
# GREEN_PENDING_MAX=20 (override 부재) — 본 tick 후 green spec disk 실측 5건 << 20 임계치 미달.
#
# 본 세션 commit diff:
#  - specs/30.spec/green/foundation/src-spec-reference-coherence.md (G3 + Should marker flip + 변경 이력 1행)
#  - specs/30.spec/green/foundation/node-modules-extraneous-coherence.md (G1·G2·Must 4 marker flip + 변경 이력 1행)
#  - specs/30.spec/green/components/common.md (신규 — blue carry-over + REQ-074 흡수)
#  - specs/30.spec/green/foundation/tooling.md (신규 — blue carry-over + REQ-075 §동작 7 흡수)
#  - specs/30.spec/green/.inspector-seen (갱신)
#  - specs/20.req/20260517-error-fallback-skeleton-contract.md → specs/60.done/2026/05/17/req/ (mv)
#  - specs/20.req/20260517-lint-staged-untracked-preservation.md → specs/60.done/2026/05/17/req/ (mv)
# RULE-02 destructive 금지 정합 (50.blocked/spec/foundation/ 6 spec 복원 시도 0 — RULE-05 /revisit 경로 의존).
# RULE-02 working tree (src 영역) 회피 정합 — modified 5건 잔존 (TSK-07 developer 진행 중), unstage·git checkout -- 둘다 미수행 (staged 없음으로 unstage 불요).
#
# 입력 큐 잔여: 1건 (REQ-076 — 차기 tick 분할 흡수).
# 차기 inspector tick 우선순위:
#  (1) REQ-20260517-076 deleted-spec-restore-batch-2-7 6 토픽 분할 흡수 (env / sanitizeHtml / markdownParser / accessibility / css-modules / search-abort).
#  (2) foundation/vite-env-boundary-typing.md G1·G2 ack (TSK-07 developer 회수 commit 후) — working tree 8 hit → 0 + S_type ∅ → 10 회수 commit 시 marker 플립.
#  (3) foundation/vite-env-boundary-typing.md G3·G4 ack (TSK-08 developer 회수 후) — CI 게이트 박제 시 marker 플립.
#  (4) foundation/node-modules-extraneous-coherence.md Should/G3·G4 ack (수단 박제 + 자동 게이트 task 발행/회수 후).
#  (5) components/common.md REQ-074 박제 후 planner promote 검토 (green → blue).
#  (6) foundation/tooling.md REQ-075 FR-03 hook-ack (task 발행/회수 후) + FR-05/NFR-02 (회귀/업그레이드 이벤트 후).
#  (7) 격리 6 spec _reason.md RULE-05 /revisit → followups 승격 (운영자 영역).
#
# == seen-state ==
specs/30.spec/green/foundation/src-spec-reference-coherence.md	ack-9e5f00a	0
specs/30.spec/green/foundation/vite-env-boundary-typing.md	baseline-9e5f00a	1
specs/30.spec/green/foundation/node-modules-extraneous-coherence.md	ack-9e5f00a	0
specs/30.spec/green/components/common.md	baseline-9e5f00a	0
specs/30.spec/green/foundation/tooling.md	baseline-9e5f00a	0
