# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (HEAD=b005644, pre-commit)
# Phase 1 ack this session: 0/2 ack (no new dev commits since prior inspector session HEAD=b005644; input queue empty; all 4 green sha256 unchanged).
#
# Phase 1 historical context (reference only):
#   - 이전 inspector 세션 HEAD=b005644 → 현 HEAD=b005644 (동일, dev/planner 신규 커밋 없음).
#   - 직전 ack: node-env-helper-migration (Must 6/6 + Should 1/1 via TSK-20260420-37/a95f1bb), typescript-bootstrap (Must 6/6 + Should 1/1 + NFR 1/3 via TSK-20260420-32/ceb9c60), msw-sibling-it 신규 carve (REQ-20260421-011 → green).
#   - 50.blocked/** 현 상태: blocked/task 0건 (TSK-20260421-52 이전 해제). blocked/req 0건. blocked/spec 0건.
#   - 40.task/ready/ 0건 (이전 세션 TSK-20260421-53 이미 소비되었거나 아직 planner 큐). 확인: `ls 40.task/ready` → empty.
#   - 20.req/ready/ 0건 (Phase 2/3 no-op).
#
# Phase 1 detail this session (drift reconcile, HEAD=b005644 재검증):
#
#   [1] common/test-isolation-shuffle-safety-cold-start-spec.md:
#       * HEAD 무변동, sha256 21543b77... 동일. WIP (`- [ ]`) 다수 존재 (FR-02/06/07/08/11/12/13 + Must FR-01/05/06/07/08/11/12/13/NFR-02 등).
#       * 대응 dep TSK-20260421-49 (Layer 2) 여전히 미완료 (done/task/21/ 내 TSK-49 폴더 없음). Layer 2 race 미해소.
#       * 재실측 생략 (WIP 불변, src 무변동, 대응 task 미완료 — ack 근거 없음).
#       * stale_cycles: 0 → **1** (WIP 불변 + ack 0건).
#
#   [2] common/node-env-helper-migration-spec.md:
#       * HEAD 무변동, sha256 69e99590... 동일. Must 6/6 + Should 1/1 모두 `[x]` (이전 세션 flip 완료). 잔존 unchecked 는 NFR gzip 1건 `[deferred]` 태깅.
#       * ack-pending 항목 없음 — planner promote 대기 상태.
#       * stale_cycles: **0 유지** (WIP-less; NFR deferred 는 stale 카운팅 대상 아님).
#
#   [3] foundation/typescript-bootstrap-spec.md:
#       * HEAD 무변동, sha256 5a86ebc5... 동일. Must 6/6 + Should 1/1 + NFR 1/3 모두 `[x]` (이전 세션 flip 완료). 잔존 unchecked 는 NFR 2건 `[deferred]` 태깅.
#       * ack-pending 항목 없음 — planner promote 대기 상태.
#       * stale_cycles: **0 유지** (WIP-less; NFR deferred 는 stale 카운팅 대상 아님).
#
#   [4] common/msw-test-sibling-it-shuffle-race-dedicated-spec.md:
#       * HEAD 무변동, sha256 e956a40a... 동일. Must 5/5 + Should 2/2 + NFR 6/6 모두 `- [ ]` (이전 세션 신규 carve, planner Task 발행 대기).
#       * ack 근거 없음 (대응 Task 미발행, src 무변동).
#       * stale_cycles: 0 → **1** (WIP 불변 + ack 0건; planner carve 대기 상태 계측).
#
# Phase 2 this session: 변화 없음.
#   - deferred 태깅 신규 전환 없음.
#   - 300줄 초과 spec 없음 (shuffle-safety ~120줄, node-env ~75줄, typescript-bootstrap ~75줄, msw-sibling ~70줄).
#
# Phase 3 this session: no-op.
#   - input queue (20.req/ready/): 0건 → 0건.
#   - downstream threshold GREEN_PENDING_MAX=20 (.claude/pipeline.json 없음). 현 green 4 < 20.
#   - 처리된 req: 0. blocked: 0. 신규 green: 0. 갱신 green: 0.
#
# 현 ledger:
specs/30.spec/green/common/test-isolation-shuffle-safety-cold-start-spec.md 21543b775c3e0f64b5f75aed161bf0722caf2dc4136efbf7996299baf60a92c3 1
specs/30.spec/green/common/node-env-helper-migration-spec.md 69e99590e31f62e726b4bd78d0753b34b1fc96c4545a7aaf275328ab19483223 0
specs/30.spec/green/foundation/typescript-bootstrap-spec.md 5a86ebc542eba28915e16dbeeb6a4e5b48722427b6ef81a166ca3009c541878b 0
specs/30.spec/green/common/msw-test-sibling-it-shuffle-race-dedicated-spec.md e956a40a36f46c10d7b49c543a49868c8041ba7c2d99970d9c80bed25efb3fb4 1
#
# Stale 경고:
#   - shuffle-safety-cold-start stale=1 (< 3 임계, 경고 없음). TSK-49 Layer 2 해소 대기 상태 — planner/operator 검토 지속.
#   - msw-sibling-it-shuffle-race-dedicated stale=1 (< 3 임계, 경고 없음). planner 의 신규 Task 발행 대기.
#   - node-env-helper-migration / typescript-bootstrap stale=0 (WIP-less, NFR deferred). planner promote green→blue 대기.
#
# 다음 세션 유의:
#   - node-env-helper-migration / typescript-bootstrap 는 Must 전원 ack — planner 가 promote 하면 REQ-20260420-002 / -003 마감 가능. promote 전까지 green 에 위치, stale 카운팅 대상 아님.
#   - shuffle-safety-cold-start 는 Layer 2 TSK-49 (blocked 해제됨? 실측 이후 미재생) 현황 — 50.blocked/task 비어있음. TSK-20260421-53 (B3 msw listen beforeAll) 은 이전 세션 planner carve 후 40.task/ready 에서 소비됨 (현 ready 큐 empty; done/task/21/ 에도 TSK-53 폴더 미존재 — developer 실행 대기 또는 다른 경로). 다음 사이클 developer 실행 결과 박제 필요.
#   - msw-sibling-it-shuffle-race-dedicated green 은 planner 의 Task 발행 대기. 발행 시 원본 shuffle-safety spec §FR-06 태깅 전환 (planner 담당).
#   - 3cycle stale 초과 시 reconcile 보고에 `reconcile: N/M ack (K stale ≥3cycle)` 토큰 추가 (RULE-04).
