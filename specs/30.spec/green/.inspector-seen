# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T13:00:00Z (HEAD=01e5d42)
# Phase 1 ack this session: 0/1.
#   - common/test-isolation-shuffle-safety-spec.md (TSK-20260421-45): hash 불변.
#       TSK-45 는 `50.blocked/task/` 격리 상태 (`_reason.md` 잔존, RULE-05 수동 개입 대기).
#       커밋 無 → hook-ack 불가, DoD 재실행 의미 無. marker 유지 (stale 3 → 4).
#       **≥3cycle 경고 연속** — 해제 경로는 RULE-05 §Blocked 해제: (A) LogItem:210 등
#       `getByTestId` → `findByTestId` 교체 또는 (B) `beforeEach(vi.useFakeTimers({ shouldAdvanceTime: true }))`
#       를 LogItem.test.jsx 파일-level 로 도입 — 둘 다 spec §FR-01 범위 확장 요함.
# Phase 1 context (historical, 파일 이동 반영):
#   - deps/react-19-findby-timing-stabilization-spec.md → `50.blocked/spec/` (01e5d42 이전 planner mv).
#     REQ-20260421-005 (α 폐기 + β 재설계) 로 대체 — 본 session Phase 3 신규 green 발행.
#   - deps/fake-timer-teardown-residual-cleanup-spec.md → `50.blocked/spec/` (01e5d42 이전 planner mv).
#     TSK-20260421-42 (c18132d) 는 완료 · 60.done 아카이브. marker-유지 결정 유효.
#     RULE-05 §Blocked 해제 시 inspector 가 spec 을 "marker 유지" 버전으로 재작성 가능.
#   - common/geturl-render-path-test-audit-spec.md → `30.spec/blue/` (01e5d42 이전 planner promote).
#     TSK-44 flip 완료 (직전 세션).
# Phase 2/3 this session:
#   - input queue (20.req/ready/): 2 건 (REQ-005, REQ-006) 처리 완료 → 60.done/req/.
#   - downstream threshold GREEN_PENDING_MAX=20 (기본, pipeline.json 없음). green 현 3 < 20, 여유.
#   - Phase 2 deferred: 신규 없음 (기존 tag 유지).
#   - 처리된 req: 2 / blocked: 0 / 신규 green: 2 / 갱신 green: 0.
#     · REQ-20260421-005 → deps/react-19-findby-option-beta-idiom-spec.md (β + 하이브리드).
#     · REQ-20260421-006 → foundation/test-infra-conventions-grep-gate-lifo-spec.md (RULE-06 보강 + LIFO 박제).
# Stale >=3 경고: 1건 (test-isolation-shuffle-safety=4).
#   - shuffle-safety: TSK-45 blocked 지속, 수동 개입 필요 (RULE-05). inspector writer 영역 밖.
#
30.spec/green/common/test-isolation-shuffle-safety-spec.md bd27261fdbd3c24b4ad4db3ac13e37413841ebfdad575f31138773dcfd06b4bc 4
30.spec/green/deps/react-19-findby-option-beta-idiom-spec.md 18ead1c35ecceee29a4cd46cda798de5885bf648f8aee08f43464463731835a3 0
30.spec/green/foundation/test-infra-conventions-grep-gate-lifo-spec.md 54f35c1425ae04a122de9356f075a8d7ce4cae667a5c18ac54b39b467aa78ed5 0
