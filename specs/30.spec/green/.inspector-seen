# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T23:05:00Z (HEAD=73bb995 at session start; Phase 3 promoted REQ-049).
# Session outcome: Phase 1 reconcile 0/4 ack (3 stale >=3cycle — dep-set-int 7→8, husky 5→6, rr-guard 3→4; useeffect-tc 1→2) + Phase 3 REQ-049 흡수 (green 신규 `foundation/react-hooks-lint-gate.md`).
#
# 세션 시작 상태 (HEAD=73bb995):
#   - `git rev-parse HEAD` = `73bb995` (직전 ledger HEAD=057770b 이후 inspector 7aab715 + planner 73bb995 2 커밋 누적).
#   - Working tree: `M package.json` (unstaged) — developer in-flight (TSK-20260421-92 husky v9 migration) 유지. 세션 시작 시 staged 비어 있음, inspector writer 영역 밖 변동 없음. `git reset HEAD -- package.json` 불요 (이미 unstaged).
#   - `specs/20.req/` 1 건 (신규 유입): `20260422-eslint-plugin-react-hooks-lint-gate-adoption.md` (REQ-20260422-049).
#   - `specs/40.task/` 3 건: TSK-20260421-92 (husky v9 migration, developer in-flight), TSK-20260421-93 (writer redirect guard, developer 대기), TSK-20260421-94 (FileUpload cleanup return, developer 대기).
#   - `specs/50.blocked/task/` TSK-20260421-91 격리 유지 (`_reason.md` 동반). 운영자 `/revisit` 경로 대기.
#   - `specs/50.blocked/spec/foundation/husky-hook-entrypoint.md` 잔존 (수동 개입 대상).
#   - `specs/30.spec/green/**` 유효 집합 4 spec (세션 시작): `common/router-redirect-reentry-guard.md`, `foundation/dependency-set-integrity.md`, `foundation/husky-hook-entrypoint.md`, `foundation/useeffect-timer-cleanup.md`.
#   - `.claude/locks/` 부재 → 정상 진행. `.claude/pipeline.json` 부재 → GREEN_PENDING_MAX 기본 20.
#
# Phase 1 drift marker 스캔 (HEAD=73bb995 실측 게이트 재실행, 세션 시작 집합 4건):
#   - `foundation/dependency-set-integrity.md` (sha 1ba2d103..., 134L, 불변):
#     · markers: `- [ ]` 7. 대응 task TSK-20260421-91 `50.blocked/task/` 격리 유지 (result.md 부재).
#     · 실측 HEAD=73bb995 게이트 재실행:
#       - `grep -c '"history"' package.json` → 1 hit (line 54 `"history": "^5.3.0"`) — FR-01 (a) 위반 유지.
#       - `grep -rnE "from 'history'|createMemoryHistory" src` → 2 hits in Writer.test.jsx (line 2, 51) — FR-01 (b)(c) 위반 유지.
#     · ACK 가능 = 0. stale_cycles: 7 → **8** (경고 지속 — `/revisit` 대기).
#   - `foundation/husky-hook-entrypoint.md` (sha a4b38749..., 119L, 불변):
#     · markers: `- [ ]` 7 (FR-06 이미 [x]). 대응 task TSK-20260421-92 `40.task/` in-flight.
#     · 실측 HEAD=73bb995 게이트 재실행 (HEAD committed 기준):
#       - `.husky/pre-commit` HEAD → `. "$(dirname "$0")/_/husky.sh"` source 포함 — FR-01 위반 유지.
#       - `.husky/pre-push` HEAD → 동일 husky.sh source 포함 — FR-01 위반 유지.
#       - `git show HEAD:package.json` scripts.prepare (25L) → `"husky install"` — FR-02 위반 유지.
#       - `git show HEAD:package.json` devDependencies.husky (55L) → `"^8.0.1"` — FR-03 위반 유지.
#       - Working tree `package.json` unstaged 에서 v9 전환 in-flight (developer 대기), HEAD 는 여전히 v8.
#       - `git ls-files .husky/_` → 0 lines (FR-06 기 충족 유지).
#     · ACK 가능 = 0. stale_cycles: 5 → **6** (경고 지속). TSK-92 working tree 해결 경로 유지, 다음 세션 flip 기대 연장.
#   - `common/router-redirect-reentry-guard.md` (sha f0d87b31..., 134L, 불변):
#     · markers: `- [ ]` 10. 대응 task TSK-20260421-93 `40.task/` developer 대기.
#     · 실측 HEAD=73bb995 게이트 재실행:
#       - `sed -n '45,70p' src/Log/Writer.jsx` → deps `[location]` 유지 + 본문 `navigate(redirectPage); return;` 순서 (pre-guard `if (location.pathname === redirectPage) return;` 부재) — FR-01 위반 유지.
#       - `grep -rn "navigate(redirectPage)" src --include="*.jsx"` → 3 hits (Monitor.jsx:49, File.jsx:34, Writer.jsx:53) — baseline 불변.
#       - `grep -n "from 'history'\|createMemoryHistory" src/Log/Writer.test.jsx` → 2 hits (2, 51) — FR-02 이관 대기.
#     · ACK 가능 = 0. stale_cycles: 3 → **4** (경고 지속).
#   - `foundation/useeffect-timer-cleanup.md` (sha c3984dc7..., 148L, 불변):
#     · markers: `- [ ]` 9 (§테스트 현황 2건 + §수용 기준 Must 5건 + Should 2건 + 스코프 1건 + 경계 보존 1건). §테스트 현황 (d)~(h), §수용 기준 비교군 이미 [x]. 대응 task TSK-20260421-94 `40.task/` developer 대기 (result.md 부재).
#     · 실측 HEAD=73bb995 게이트 재실행:
#       - `grep -rnE "setTimeout\s*\(|setInterval\s*\(" src --include="*.jsx" --include="*.js"` 테스트 제외 후 → 8 hits / 6 files: Toaster.jsx(2 FR-01 충족), FileUpload.jsx:101/115 (**2 FR-01/FR-02 위반**), FileDrop.jsx:82 (FR-01 충족), FileItem.jsx:29 (범위 밖), Search.jsx:60 (`setInterval` 충족), useHoverPopup.js:46 (FR-01 충족).
#       - `grep -nE "clearTimeout\(|clearInterval\(" src/File/FileUpload.jsx` → 0 hit. FR-02 Must (`clearTimeout` 최소 1 hit) 위반 유지.
#       - `grep -nE "retain on unmount" src` → 0 hit. FR-03 예외 주석 baseline 빈 집합 유지 (FR-03 [x] 상태 정합).
#     · ACK 가능 = 0. stale_cycles: 1 → **2** (정상 — 임계 미도달).
#   - 종합 ack: N=0, M=4. 토큰 = `reconcile: 0/4 ack (3 stale >=3cycle — dep-set-int 8, husky 6, rr-guard 4)`.
#
# Phase 2 (deferred/split):
#   - 라인 수 (HEAD=73bb995): dependency-set-integrity 134L / husky-hook-entrypoint 119L / router-redirect-reentry-guard 134L / useeffect-timer-cleanup 148L / react-hooks-lint-gate 126L (본 세션 신규) — 모두 < 300, split 불요.
#   - 신규 `[deferred: ...]` 태깅 0. 기존 태깅 부재 유지.
#
# Phase 3 (신규 반영):
#   - 입력 `20260422-eslint-plugin-react-hooks-lint-gate-adoption.md` (REQ-20260422-049) RULE-07 콘텐츠 자기검증:
#     · FR-01 (devDependencies 엔트리) = 시스템 상태 불변식, grep 재현 가능.
#     · FR-02 (flat config 활성화 3조건) = 구성 계약, grep 재현 가능.
#     · FR-03 (회귀 가시성 — rule id 인식) = 수단 독립 관측 가능 (dry-run fixture 재현).
#     · FR-04 (severity 선택 정책) = baseline 수치 기반 결정론적 정책 — 시점·incident 귀속 아님.
#     · NFR-01~04 = 측정 기준·호환성 불변식.
#     · 1회성 진단·incident patch 요소 부재. 시점 비의존. → RULE-07 양성.
#   - 중복·매핑 점검:
#     · 기존 blue/green 중 `eslint-plugin-react-hooks` 또는 `react-hooks/*` rule 불변식 박제 spec 0. `foundation/tooling.md` (blue) 는 대상 블록 불변식, `foundation/regression-gate.md` (blue) 는 `npm test`/`lint` 진입점 — 본 건 (rule 층) 과 차원 직교.
#     · `useeffect-timer-cleanup.md` §FR-05 / `router-redirect-reentry-guard.md` §FR-04 의 "수단 예시" 언급은 본 게이트의 자매 결합점 — 본 세션에서는 신규 spec §FR-05 로 상호참조 박제 (자매 spec 본문 편집 0).
#   - green create: `specs/30.spec/green/foundation/react-hooks-lint-gate.md` (126L, sha 46c49636...). 스코프 규칙 grep-baseline 5 gate 실측 박제 ((a)~(e)):
#     · (a) `eslint-plugin-react-hooks` in package.json → 0 hits.
#     · (b) react-hooks 참조 in eslint.config.js → 0 hits.
#     · (c) scripts.lint eslint 진입점 → 1 hit (package.json:21).
#     · (d) useEffect( 면적 in src → 56 hits.
#     · (e) eslint-plugin-react flat.recommended → 1 hit (eslint.config.js:26).
#   - req 이동: `specs/20.req/20260422-eslint-plugin-react-hooks-lint-gate-adoption.md` → `specs/60.done/2026/04/22/req/20260422-eslint-plugin-react-hooks-lint-gate-adoption.md`.
#
# 하류 임계치 (GREEN_PENDING_MAX=20 default; `.claude/pipeline.json` 부재):
#   - 세션 종료 시점 green 유효 집합 5 < 20. 여유 15. backpressure none.
#
# Stale 경고 (>= 3 cycle): **3 건** (rr-guard 임계 초과 지속).
#   - `foundation/dependency-set-integrity.md` (stale_cycles=8). 대응 task TSK-20260421-91 `50.blocked/task/` 격리. 운영자 RULE-05 `/revisit` 경로 필요.
#   - `foundation/husky-hook-entrypoint.md` (stale_cycles=6). 대응 task TSK-20260421-92 `40.task/` in-flight. working tree 에 FR-02/03 해결 경로 반영 (v9 migration). developer 커밋 후 flip 기대 지속.
#   - `common/router-redirect-reentry-guard.md` (stale_cycles=4). 대응 task TSK-20260421-93 `40.task/` developer 대기. Writer.jsx guard 도입 · Writer.test.jsx history 이관 후 flip 가능.
#
# 변동 spec sha (세션 종료 시점, inspector 관할 green 집합):
specs/30.spec/green/foundation/dependency-set-integrity.md 1ba2d103adf37b3e0fe5477622935402b4ebb7fb5703f5e01242ddfc5c2a821a 8
specs/30.spec/green/foundation/husky-hook-entrypoint.md a4b387491cb7b3efb23f1e02184011b6b10bd7ff7d76883f490833bffa160f47 6
specs/30.spec/green/common/router-redirect-reentry-guard.md f0d87b3112c93a6c41dfc399cda0fa5ae3b29012bfdf2f79c2b4ee60e3f2fa74 4
specs/30.spec/green/foundation/useeffect-timer-cleanup.md c3984dc7c0275cb49cff0165f6655e71758bada7e1ce7e0d9aea0cdfa7ff1da0 2
specs/30.spec/green/foundation/react-hooks-lint-gate.md 46c49636fa094aed5651c840f891a3f9e3d74606b145db6a9ea13a17baa7bfac 0
#
# 다음 세션 유의:
#   - 신규 green `foundation/react-hooks-lint-gate.md` (REQ-049) — planner 가 task 로 carve 대기 (FR-01 의존성 추가 + FR-02 flat config 활성화 + FR-03 회귀 가시성 fixture dry-run).
#   - 기존 4 green spec sha256 불변 유지 (본 세션 편집 0).
#   - dep-set-int stale=8 (지속 경고). `/revisit` TSK-91 판정 전 stale 증가 지속 (다음 9).
#   - husky-hook-entrypoint stale=6 (지속 경고). TSK-92 working tree 해결 경로 반영, developer 커밋 후 flip 기대.
#   - rr-guard stale=4 (임계 초과 지속). TSK-93 developer 대기.
#   - useeffect-timer-cleanup stale=2 (정상 임계 미도달). TSK-94 developer 대기.
#   - react-hooks-lint-gate stale=0 (신규 등록). planner carve 후 FR-01~03 ack 후보.
#   - `20.req/` 빈 큐 복귀 → 다음 세션 Phase 3 trigger 대기 (discovery 1h 주기 산출물 대기).
#   - Phase 1 은 pause lock 외 항상 수행. `reconcile: N/M ack` 토큰 RULE-04 notes 박제 필수.
#   - unstaged `package.json` developer in-flight 유지 (inspector writer 경계 밖 — git reset HEAD 불요, 이미 unstaged).
