# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-04-22 @ a1fedbc — reconcile 0/41 ack (no flip this session). HEAD 이동 구간 57b6459..a1fedbc = [bfdd4f4 spec(inspector) / a1fedbc task(planner) carve TSK-13..17] — developer src 커밋 0건. WIP 불변.
# 이번 세션부터 집계를 §동작 게이트 + §수용 기준 항목 분리 카운트로 정규화 (이전 세션 27 집계는 §수용 기준 일부 합산 누락). 실제 marker 총량 41 — 증분이 아니라 카운트 일원화.
# 집계 내역 (HEAD=a1fedbc):
#   src-typescript-migration.md: 12 WIP (§동작 6 + §수용 기준 6). 실측 `.jsx` 42 / `.js` non-test-non-d.ts 22 / `.test.jsx` 19 / `.test.js` 14 / `.ts` non-d.ts 20 / `.tsx` 24. FR-01/02 게이트 전원 0 미달. stale_cycles=2.
#   tooling.md: 7 WIP (REQ-053 FR-01~06 + NFR-03). 실측 vanilla `no-unused-vars` 1 hit @eslint.config.js / `@typescript-eslint/no-unused-vars` 0 hit / disable 주석 8 hit (test-utils/msw.ts:41, Comment/api.mock.ts:9,11,13, Comment/CommentItem.tsx:28,30, Comment/CommentForm.tsx:19, Image/ImageItem.tsx:6). FR-01 rule swap 미적용. stale_cycles=2.
#   tsconfig-test-ambient-globals.md: 6 WIP (§동작 3 [FR-02/03/06] + §수용 기준 3 동일). 실측 TS2304 0 hit 유지 (FR-01 이미 [x]). FR-02/03/06 해석·메타 게이트로 수치 ack 불가. stale_cycles=2.
#   typecheck-exit-zero.md: 16 WIP (§동작 6 + §수용 기준 10). 실측 `npm run typecheck` rc=2 / `grep -cE "error TS"` **84 hit** (이전 세션 103 → 84, Δ-19 감소 — 작업 트리 unstaged `src/Toaster/Toaster.test.tsx` 영향 가능 · 커밋된 src 변경 0건이므로 측정 환경 변동 가능). FR-01 rc=0 + 0 hit 수렴 미달. stale_cycles=2.
# reconcile this session: 0/41 ack (4 specs 41 WIP 전원 유지 · 0 flip).
# 처리: REQ-20260422-055 (branch coverage threshold drift 94 convergence) 를 RULE-07 반려 — 50.blocked/req/ + _reason.md. 반려 근거: FR-01/02/03 특정 커밋 구간 bisect·N회 재실측 (1회성 진단), FR-04 기존 regression-gate.md 재박제 금지 (req NFR-03 자인), FR-05/06 inspector 쓰기 영역 밖, FR-07/NFR-04 박제 불가 자인. 반려 시그널 5건 중첩.
# 작업 트리: src/Toaster/Toaster.test.tsx unstaged (inspector 영역 밖, RULE-02 준수 — 건드리지 않음 + staging 도 하지 않음).
