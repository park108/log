# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (HEAD=1eb4a53, Phase 1 reconcile 세션)
# Phase 1 ack this session: 1/6 ack.
#
# drift marker 스캔 (5 spec) — 총 6 marker 발견:
#   1. `foundation/tooling.md:57` — lint-staged `.d.ts` 포함 보강 (향후 task) → 대응 task 미수행 → [ ] 유지.
#   2. `foundation/ci.md:48`       — (수동) Node LTS 상향 시점 engine 확인 → 수동 점검 반복 계약 → [ ] 유지.
#   3. `components/common.md:137`  — isAdmin() 6 매트릭스 회귀 테스트 감사 → ACK (572009f / TSK-20260421-61 PASS; `common.test.js:670` describe('isAdmin matrix (REQ-20260421-017)') 6 케이스 박제, 381 tests passed, 3 isAdmin describe 블록) → [x] 플립 + 변경 이력 1행 박제.
#   4. `components/common.md:138`  — auth() SameSite 회귀 방어 테스트 → 현 `common.js:138,144` 가 `sameSite: site` (도메인 문자열) 로 RFC 6265bis 유효값 미준수 baseline (0 hit). task 미수행 → [ ] 유지.
#   5. `components/common.md:155`  — (Must, REQ-025 FR-02 실현) positive/negative 어설션 실제 추가 (향후 task) → 위와 동일 미준수 → [ ] 유지.
#   6. `components/log.md:59`      — LogItem DELETE shuffle 안정성 (향후 task 로 실현; spec 은 계약 박제만) → task 미수행 → [ ] 유지.
#
# Phase 2 (deferred/split): 해당 없음. 300줄 초과 spec 없음.
#
# Phase 3 (신규 req 반영): 20.req 비어있음 → Phase 3 no-op.
#
# 이동: 없음 (20.req 비어있음).
# blocked: 없음 (반려 대상 없음).
# 입력 큐: 0건. 하류 임계치 GREEN_PENDING_MAX=20, 현 green 파일 5 (spec) < 20. 여유 15.
#
# hook-ack 보조 확인:
#   - ack 대상 (3) 의 `572009f` (2026-04-21 14:07) 은 HEAD=1eb4a53 조상. `572009f` 메시지 "isAdmin 매트릭스 회귀 테스트" + result.md DoD "npm test -- --run 47 files / 381 tests passed" 박제. Must 주관 혼재 없음 → ack 보조 확정.
#
# 현 ledger:
specs/30.spec/green/common/test-idioms.md 7447ef35bbb4c6659d5b737b79ad487bf527458a650921d6aa86b7918a1a9c6a 0
specs/30.spec/green/components/common.md a96a73c3855747448c17a13563069f16da48969e96619f2bc39bc96d4bad6acc 0
specs/30.spec/green/components/log.md cdd2ba754bf67416be93feb9825f7023f2b960c13f6b1d354c8ed64844e3b82a 0
specs/30.spec/green/foundation/ci.md 626e1330c6535a7b2fe3b493e377298b39ae3d78ad5140850d3c8de5ea7ea74f 0
specs/30.spec/green/foundation/tooling.md e8d809041b276e689d35f624cdf86d111d3e93fa059067eef667ea2fee04edfd 0
#
# Stale 경고: 0 cycle (marker 단위 stale tracking 은 ledger schema 상 path 단위이므로 본 session 범위 밖).
#
# 승격 가능성 (planner 관할, 참조용):
#   - `components/common.md` 잔여 [ ] 2건 (line 138, 155) — SameSite 정상화 task carve 후 해소 가능. 우선 순위: 운영 쿠키 속성 실측 회귀 위험.
#   - `components/log.md:59` — LogItem DELETE shuffle 결정성 실현 task carve 대상.
#   - `foundation/tooling.md:57` — lint-staged `.d.ts` glob 보강 task (1 line 수정) carve 대상.
#   - `foundation/ci.md:48` — 수동 점검 반복 계약. task 불요 (운영자 동선).
#
# 다음 세션 유의:
#   - 20.req 가 다시 찰 경우 Phase 3 재개.
#   - Phase 1 ack 추가 후보: SameSite 정상화 task 완료 시 common.md line 138/155 ack 가능.
