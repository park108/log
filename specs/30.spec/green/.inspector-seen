# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-04-22 @ 4af36ee — reconcile 10/46 ack (tooling.md 7 flip + coverage-determinism.md 3 flip · 4 specs 나머지 36 WIP 유지, stale ≥3cycle 진입 유지).
# HEAD 이동 구간 a2717fb..4af36ee = [4708eb8 spec(inspector), f5ea729 task(planner), fddacb0 feat: ESLint TS rule swap, 4af36ee chore: coverage exclude extension-glob]. 본 세션 src/build 영향 커밋 2건 (fddacb0 REQ-053 구현 수렴 + 4af36ee REQ-057 FR-11 구현 수렴).
# 본 세션 Phase 2: REQ-20260422-058 (eslint-flat-config-rule-merge-last-write-wins-invariant) 를 `tooling.md` §동작 6 으로 증분 박제 (6.1~6.4 + FR-01~05 + NFR-01~02 + grep-baseline gate (k)(l)). 본 세션 편집만으로 전원 [x] 수렴 (외부 soup 없이 spec 본문·구현부 주석·grep 수치 자기 정합).
# 재게이트 집계 내역 (HEAD=4af36ee, clean wt src/Comment/** 3 파일 uncommitted 는 inspector 영역 밖 · HEAD 기준 실측):
#   tooling.md: 7 WIP → 0 WIP (전원 flip). REQ-053 FR-01~06 + NFR-03 + §동작 5.1~5.4 + JS/JSX 의미 보존 ack 근거: TSK-20260422-12 @fddacb0 (현 HEAD 조상, task result.md `npm run lint` exit 0 warning 0 / `npm test` 48 files 439 PASS / JS/JSX 블록 bit-for-bit 보존 / 임시 프로브 warn 1건 재현 / pre-commit lint-staged 경로 통과). 재게이트 실측: `grep -nE "@typescript-eslint/no-unused-vars" eslint.config.js` → 1 hit @:83 / `grep -nE "'no-unused-vars'" eslint.config.js` → 2 hits (@:64 vanilla + @:84 'off') / `grep -rn "eslint-disable-next-line no-unused-vars" src` → 0 hits. 본 세션 REQ-058 FR-01~05 + NFR-01~02 박제 + 즉시 [x] 수렴. stale_cycles=0.
#   coverage-determinism.md: 5 WIP → 2 WIP (3 flip). REQ-057 FR-11 grep gate (Must) / FR-11 평서문 박제 (Must) / FR-11 엔트리 수 5 (Should) ack — TSK-20260422-18 @4af36ee (현 HEAD, task result.md `grep -nE "^\s*'src/[a-zA-Z_]...\.(js|jsx|ts|tsx)',?$" vite.config.js` → 0 hit 수렴 + `grep -cE "^\s*'src/" vite.config.js` → 5 유지 + 4축 coverage threshold 전원 PASS + 회귀 0). FR-11 Should "확장자 스위칭 후 4축 유지" 2건 (§테스트 현황 line 157 + §수용 기준 line 157) 은 스위칭 task (src-typescript-migration FR-05 축) 미실행 — 조건부 대기 marker 유지. REQ-056 FR-08/09/10 관련 5 WIP (§테스트 현황 + §수용 기준) 은 intermediate 잔재 진입 + pre-push 독립성 fixture 재현 task 미발행 — marker 유지 stale_cycles=2.
#   src-typescript-migration.md: 12 WIP. 실측 `.jsx` 29 / `.js` non-test-non-d.ts 13 / `.test.jsx` 13 / `.test.js` 6 (`src/Comment/**` uncommitted 는 영역 밖 · HEAD 기준). FR-01/02 전체 repo 0 미달 유지. FR-04 혼재 허용 축 본 세션 island 확정 커밋 부재 (Comment/Search/common 3 island 는 @a2717fb 이전 / @fddacb0 는 eslint 치환 / @4af36ee 는 vite.config 편집). stale_cycles=5.
#   typecheck-exit-zero.md: 16 WIP. 실측 `npm run typecheck` rc=2 / `grep -cE "error TS"` 190 hit (직전 세션 @a2717fb 209 hit → @4af36ee 190 hit, -19 감소 — 본 세션 uncommitted `src/Comment/**` 3 파일 diff 에 일부 타입 annotation 진행 중 가능성 · HEAD 기준 측정). TS2304 0 hit 유지 (FR-05 ambient 경로). FR-01 rc=0 + 0 hit 수렴 미달 유지. stale_cycles=5.
#   tsconfig-test-ambient-globals.md: 6 WIP. 실측 TS2304 0 hit 유지 (FR-01 이미 [x]). FR-02/03/06 보수적 marker 유지 (단일 수단 귀속 해석 여지 + CI typecheck step rc=0 조건 현재 rc=2 + 버전 표현 해석 여지). stale_cycles=5.
# reconcile this session: 10/46 ack (tooling.md 7 flip + coverage-determinism.md 3 flip · 3 specs 36 WIP 유지, 그 중 typecheck-exit-zero 16 + src-typescript-migration 12 + tsconfig-test-ambient-globals 6 전원 stale_cycles=5 · coverage-determinism.md 2 WIP stale_cycles=2). stale ≥3cycle 해당 specs = 3 (typecheck-exit-zero · src-typescript-migration · tsconfig-test-ambient-globals).
# Phase 2 처리: REQ-20260422-058 tooling.md §동작 6 증분 박제 — ESLint flat-config 동일 rule key last-write-wins merge semantics 불변식. TSK-20260422-12 작업 지시서 1단계 서술 "더 엄격한 레벨로 merge" 사실 오류 followup (`specs/60.done/2026/04/22/followups/20260422-0525-flat-config-rule-merge-semantics.md`) 소비 근거 · ESLint 공식 문서 "later configuration object wins" 외부 레퍼런스 박제. 타깃 spec 은 REQ-058 원문에서 blue 지정이나 inspector 편집 영역 green 으로 직접 흡수. §동작 6.1~6.4 + §회귀 중점 2건 + §스코프 규칙 gate (k)(l) 2건 + §수용 기준 FR-01~05 + NFR-01~02 총 7 항목 + §변경 이력 + §참고 외부 레퍼런스 박제. req mv → `60.done/2026/04/22/req/`. RULE-07 자기검증 통과 — 평서형·반복 검증 가능 (`grep -nE` 단일 명령 + ESLint 공식 문서 교차 정합)·시점 비의존·수단 중립·incident patch 아님 (merge semantics 는 ESLint 런타임 성질 · 특정 릴리스 귀속 부재).
# 작업 트리: src/Comment/** 3 파일 uncommitted (inspector 영역 밖, HEAD 기준 실측). 본 세션 diff = tooling.md edit + coverage-determinism.md edit + 20.req/* mv + .inspector-seen update.
