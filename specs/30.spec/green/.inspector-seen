# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-04-22 @ 996d25c — reconcile 0/41 ack (no flip this session). HEAD 이동 구간 a1fedbc..996d25c = [0f6b090 spec(inspector) / 996d25c test WebVitalsItem 분기 3건 보강] — 본 세션 src 영향 커밋 1건 (test 파일 1건 분기 보강). WIP 수 전원 불변 (12/7/6/16).
# 이전 세션 (HEAD=a1fedbc) stale_cycles=2 였으며 이번에도 ack 0 + WIP 불변 → stale_cycles=3 (≥3cycle 진입 · 4 specs 전원).
# 집계 내역 (HEAD=996d25c):
#   src-typescript-migration.md: 12 WIP (§동작 6 + §수용 기준 6). 실측 `.jsx` 29 / `.js` non-test-non-d.ts 13 / `.test.jsx` 13 / `.test.js` 6 / `.ts` non-d.ts 37 / `.tsx` 37 / `.d.ts` 2. FR-01/02 게이트 전원 0 미달 유지. stale_cycles=3.
#   tooling.md: 7 WIP (REQ-053 FR-01~06 + NFR-03). 실측 vanilla `no-unused-vars` 1 hit @eslint.config.js:74 / `@typescript-eslint/no-unused-vars` 0 hit / disable 주석 8 hit (test-utils/msw.ts:41, Comment/api.mock.ts:9,11,13, Comment/CommentItem.tsx:28,30, Comment/CommentForm.tsx:19, Image/ImageItem.tsx:6). FR-01 rule swap 미적용. stale_cycles=3.
#   tsconfig-test-ambient-globals.md: 6 WIP (§동작 3 [FR-02/03/06] + §수용 기준 3 동일). 실측 TS2304 0 hit 유지 (FR-01 이미 [x]). FR-02/03/06 해석·메타 게이트로 수치 ack 불가. stale_cycles=3.
#   typecheck-exit-zero.md: 16 WIP (§동작 6 + §수용 기준 10). 실측 `npm run typecheck` rc=2 / `grep -cE "error TS"` **305 hit** (이전 세션 @a1fedbc 106 hit → 본 세션 @996d25c 305 hit; working tree 에 src/common/** 대량 rename unstaged 잔존 — inspector 영역 밖으로 언스테이지 유지, 디스크 상태만 반영). TS2304 **0 hit** 유지. FR-01 rc=0 + 0 hit 수렴 미달. stale_cycles=3.
# reconcile this session: 0/41 ack (4 specs 41 WIP 전원 유지 · 0 flip · 4 stale ≥3cycle 진입).
# Phase 2 처리: REQ-20260422-056 (`npm test` 결정성 회복 — coverage/.tmp race 제거 + 4축 threshold 재현 가능성) 를 `coverage-determinism.md` blue→green 복사 후 편집 경로로 흡수 — FR-08 (intermediate 잔재 robustness) · FR-09 (non-0 exit 귀인 경계) · FR-10 (pre-push 직전 실행 상태 독립성) 3 불변식 증분 박제. `regression-gate.md` FR-02 와 교집합 문장 재박제 0 (참조 관계). req mv → `60.done/2026/04/22/req/`. RULE-07 자기검증 통과 — 평서형·반복 검증 가능 (`npm test` clean/잔재 진입 비교 fixture)·시점 비의존·수단 중립 (coverage-v8 버전 · tmp 경로 · worker pool 박제 0).
# 신규 spec (REQ-056 증분): coverage-determinism.md (blue → green 복사 · 편집). baseline 미수렴 상태 5 WIP (FR-08 clean/잔재 수렴 + FR-09 귀인 경계 + FR-10 pre-push 독립성 + FR-04 접합 + FR-05 ENOENT 재현 후 0 hit). stale_cycles=0 (신규 진입).
# 작업 트리: src/common/** 대량 rename unstaged (inspector 영역 밖, RULE-02 준수 — 건드리지 않음 + staging 도 하지 않음 · 세션 시작 시 `git reset HEAD -- src/` 로 언스테이지 수행).
