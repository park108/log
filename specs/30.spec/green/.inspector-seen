# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-05-17 @ 0305fb1 → THIS — reconcile 0/2 ack (0 stale ≥3cycle). 본 세션 평가 대상 green spec 2 (island-proptypes-removal.md 2nd cycle 추적 + runtime-dep-version-coherence.md 1st cycle 신설 후 추적). 미 flip 정상: 두 spec 모두 효능 도입 task 발행 대기 상태 — island-proptypes-removal 의 14 checkbox (PropTypes 0 hit 게이트 a/b/c — 46/11/11 hit) 와 runtime-dep-version-coherence 의 17 checkbox (3축 메이저 격차 0 — react/react-dom blue↔declared 격차 1, react-router-dom declared↔installed 격차 1, @testing-library/react 메이저 13 < 14) 자연 미수렴. flip 시점은 task 발행 (planner) + 완료 (developer) 후 result.md 커밋 해시 + DoD 게이트 재실행 통과 이후. hook-ack 보조: 본 세션 직전 commit 0305fb1 (spec(inspector) REQ-063 흡수) 은 spec 박제 commit — src/test/lint/build "회귀 0" 문구 부재 → hook-ack 0 (spec 박제 commit 군은 ack 자동 0). 본 세션 commit 또한 spec 박제 commit + REQ-064 흡수 — hook-ack 0. RULE-04 토큰 `reconcile: 0/2 ack` (분모 2 — 양 green spec / ack 0 — 양 spec 1st-2nd cycle 자연 미수렴, src/package.json 변경 0).
# 본 세션 baseline 재실측 (HEAD=0305fb1, 07eaf10 → 0305fb1 src 영향 0):
#   island-proptypes-removal.md: (a) PropTypes 46 hits in 11 files (변동 0) / (b) import 11 / 11 (변동 0) / (c) selector 등록 11 / 11 (변동 0). 14 미수렴 checkbox 자연 유지. stale_cycles 1→2 (2nd cycle).
#   runtime-dep-version-coherence.md: (a) declared react 18 / react-dom 18 / react-router-dom 7 (변동 0) / (b) declared @testing-library/react 13 (변동 0) / (c) installed react 18.2.0 / react-dom 18.2.0 / react-router-dom 6.4.3 / @testing-library/react 13.4.0 (변동 0) / (d) blue 박제 react ^19.2.x, react-dom ^19.2.x, react-router-dom ^7.14.1 (app.md:39 변동 0). 격차 baseline 불변. 17 미수렴 checkbox 자연 유지. stale_cycles 0→1 (1st cycle, 신설 직후 첫 추적).
# Phase 2 처리: 입력 큐 REQ-20260517-064 (devbin-install-integrity) 1건 흡수. RULE-07 자기검증 통과 — (i) 평서형 효능 (devbin 존재 + binary resolve + 메이저 격차 0 동시 성립) / (ii) 시점 비의존 (특정 패키지 이름·메이저 숫자는 §스코프 규칙 baseline 에만 박제, NFR-01 명시) / (iii) 반복 검증 가능 (`ls` + `node -p` + `command -v` 1-line) / (iv) incident 귀속 부재 (req §개요 "1회성 npm ci 운영 task 가 아니라 반복 검증 가능한 상시 불변식" 박제) / (v) 수단 중립 (FR-06 자기 검증 — `기본값|권장|우선|default|best|root cause|가장 효과적` 9 hit 매치 모두 § 동작 6 카테고리 (i)~(iv) 내부: (i) § 동작 6 정의 본문 1 hit (line 68), (ii) 자기 검증 게이트 본문 6 hit (line 48 § 동작 3.3 "그 req 우선" 분기 정의 / line 70 § 동작 6.1 자기 검증 / line 138 §스코프 규칙 baseline (h) / line 151 테스트 현황 § 동작 3.3 / line 158 테스트 현황 § 동작 6.1 / line 168 수용 기준 FR-06), (iii) 외부 라이브러리 인용 0 hit 독립 본문 (npm ci default 동작 인용은 § 동작 6.1 정의 본문에 포함되어 카테고리 (i) 와 중첩), (iv) 템플릿 메타 1 hit (line 7 "식별자 우선"), 추가 1 hit (line 180 변경 이력 본문 — 카테고리 (ii) 변경 이력 정책 명시)). 신규 green spec `30.spec/green/foundation/devbin-install-integrity.md` create (7개 불변식 § 동작 1~7 박제). req mv `20.req/20260517-devbin-install-integrity.md → 60.done/2026/05/17/req/`. `GREEN_PENDING_MAX=20` — 본 세션 후 green spec 총 3건 (island-proptypes-removal + runtime-dep-version-coherence + devbin-install-integrity, 3 << 20 임계치 미달).
# 본 세션 후 입력 큐 = 빈 큐. 차기 inspector tick 은 Phase 1 단일 책임 (3 green spec 추적) + 입력 큐 모니터링.
# 작업 트리 정리: 세션 시작 시 staged 0 / unstaged 0 (`?? specs/30.spec/green/foundation/devbin-install-integrity.md` 본 세션 신규 create 만). 본 inspector 세션 commit diff = `specs/30.spec/green/foundation/devbin-install-integrity.md` create + `specs/30.spec/green/.inspector-seen` 갱신 + `specs/20.req/20260517-devbin-install-integrity.md → specs/60.done/2026/05/17/req/` mv (3 file).
# 직전 (0305fb1, 2026-05-17 — REQ-063 흡수 세션) — reconcile 0/1 ack (0 stale ≥3cycle). 평가 대상 green 1 (island-proptypes-removal 1st cycle). Phase 2 REQ-063 흡수 1건 (runtime-dep-version-coherence 신설). 본 세션은 그 두 spec 의 +1 cycle 추적 + REQ-064 흡수.
#
# == seen-state ==
specs/30.spec/green/foundation/island-proptypes-removal.md	BASELINE-46/11/11-HEAD-0305fb1-unchanged	2
specs/30.spec/green/foundation/runtime-dep-version-coherence.md	BASELINE-3AXIS-GAPS-HEAD-0305fb1-unchanged	1
specs/30.spec/green/foundation/devbin-install-integrity.md	NEWLY-CREATED-0305fb1-baseline	0
