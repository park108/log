# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T08:15:00Z (HEAD=ea9d30c, pre-commit)
# Phase 1 ack this session: 0/3 ack.
# Phase 1 historical context (reference only):
#   - 이전 세션 HEAD=3822147 → 현 HEAD=ea9d30c 사이:
#       * 0225335 spec(inspector): flip toaster-helper DoD (1/3 ack via TSK-48); stale=1 shuffle+file-split
#         (toaster spec Must 6/7 + Should 2/2 + NFR 2/2 flip; FR-09 만 unchecked 잔존)
#       * ea9d30c spec(planner): rename TSK-49/50 prefix; 0 promote; TSK-49/50 → 50.blocked/task/ 이관 관측
#   - 50.blocked/task/ 잔여 (HEAD=ea9d30c): TSK-20260421-43, TSK-20260421-49, TSK-20260421-50 — RULE-05 관할.
#   - 50.blocked/spec/ 5건 불변.
#   - 40.task/ready/ 0건 (직전 세션의 TSK-49/50 가 blocked 이관).
#
# Phase 1 detail this session (drift reconcile):
#   - components/toaster-conditional-mount-or-helper-spec.md (REQ-20260421-009):
#       * 대응 커밋 a8eee5e (TSK-20260421-48 done, 0225335 세션에서 Must 6/7 + Should 2/2 + NFR 2/2 flip 완료).
#       * 현 HEAD=ea9d30c 재검증:
#           - grep "waitForToasterHidden|waitForToasterVisible|getToasterElement" src → 25 hits in 3 files (FR-02 ack 유지)
#           - grep "document.querySelector.*data-type=" src --include="*.test.*" → 0 hits (FR-05 ack 유지)
#           - grep "<Toaster " src → 8 hits in 8 files (FR-04 ack 유지)
#       * 유일한 unchecked FR-09 (shuffle seed 0 fail) 은 TSK-20260421-49 blocked (Layer 2 mutation flush race) 때문에 경로 차단.
#       * 이번 세션 추가 ack: 0건. stale=0 유지 (직전 세션 flip 으로 인해 hash drift 정지 상태).
#   - common/test-isolation-shuffle-safety-cold-start-spec.md (REQ-20260421-007):
#       * 대응 task TSK-20260421-49 가 50.blocked/task/ 로 이관 (ea9d30c 시점). ack 불가.
#       * developer blocked_reason: 옵션 A+B+C 조합 + `it` timeout 10s + `act(async)` + `vi.runAllTimersAsync` 전부 시도 후에도 shuffle seed=1 에서 `LogItem DELETE network-error` 1건 `findByText("Deleting log network error.")` 5067ms timeout 잔존. `window.confirm` spy 호출 / 직접 fetch 200 ok 확인됨 → mutation fetch 경로에 한해 first-in-file 실행 시 resolution hang. 별도 Layer 2 race (mutation + msw listen 타이밍) 식별.
#       * 이번 세션 drift reconcile: FR-02/06/07/08 에 [deferred: TSK-49 blocked] 태깅 + 변경 이력 1행 박제. ack 0건. stale=1→2.
#   - components/file-test-multi-scenario-split-spec.md (REQ-20260421-008):
#       * 대응 task TSK-20260421-50 가 50.blocked/task/ 로 이관 (ea9d30c 시점). depends_on TSK-20260421-49 (blocked) 미충족.
#       * 파일 비중첩 (File.test.jsx prodServerOk vs LogItem.test.jsx) 이므로 운영자 판단으로 depends_on 제거 재평가 가능 — blocked_reason 에 경로 명시.
#       * 이번 세션 drift reconcile: FR-01~05, FR-07, FR-09 에 [deferred: TSK-50 blocked] 태깅 + 변경 이력 1행 박제. ack 0건. stale=1→2.
#
# Phase 2 this session: deferred 태깅 적용 (shuffle-safety 5건, file-test-split 7건). 300줄 초과 spec 없음 → 분할 없음.
# Phase 3 this session:
#   - input queue (20.req/ready/): 0건 (비어있음).
#   - downstream threshold GREEN_PENDING_MAX=20 (기본). 현 green 3 < 20.
#   - 처리된 req: 0 / blocked: 0 / 신규 green: 0 / 갱신 green: 2 (shuffle-safety, file-test-split deferred 태깅).
#
# 현 ledger:
specs/30.spec/green/components/file-test-multi-scenario-split-spec.md - 2
specs/30.spec/green/common/test-isolation-shuffle-safety-cold-start-spec.md - 2
specs/30.spec/green/components/toaster-conditional-mount-or-helper-spec.md - 0
#
# Stale 경고: 없음 (전원 stale < 3). 다음 세션에 TSK-49/50 blocked 해제 없으면 stale=3 도달 → planner 측 승격 보류 재확인 필요.
# 다음 세션 유의:
#   - RULE-05 운영자가 TSK-49 해제 시 shuffle-safety spec 재개정 필요 (Layer 2 서사 추가). inspector 는 운영자 복귀 후 재carve 가능한 형태로 유지.
#   - RULE-05 운영자가 TSK-50 의 depends_on 을 TSK-49 에서 해제하면 TSK-50 독립 진행 가능.
#   - toaster spec FR-09 는 TSK-49 Layer 2 해소에 종속.
