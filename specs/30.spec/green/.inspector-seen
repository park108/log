# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-04-22 @ 46f4484 — reconcile 10/21 ack (flip 10 in tsconfig-test-ambient-globals.md, 11 WIP across 3 specs remaining as normal WIP).
# src-typescript-migration.md: 6 [ ] §동작 게이트 + 6 [ ] §수용 기준. 실측 HEAD=46f4484 — `.jsx` 46 / `.js` 런타임 26 / `.test.jsx` 21 / `.test.js` 15. FR-01/02 수렴 조건 (전원 0) 미달 — 정상 WIP. 파생 task: TSK-20260422-01~09 (Toaster/fixtures/Image/File/tsconfig/Comment/Search/Monitor/Log). stale_cycles=0.
# tsconfig-test-ambient-globals.md: reconcile ack 10/13 this session — TSK-20260422-05 (`f34419e`, HEAD 조상) 산출. `npm run typecheck 2>&1 | grep -cE "TS2304"` → 0 (43→0 수렴). §테스트 현황 FR-01/FR-04/FR-05 + §수용 기준 FR-01/FR-04/FR-05/NFR-01/NFR-02/NFR-03/NFR-04 flip. FR-02 (단일 수단 귀속 해석 여지) · FR-03 (CI typecheck step rc=1 잔존 = FR-01 범위 밖 island TS2769/TS2345 계열 = `foundation/typecheck-exit-zero` FR-01 수렴 대상) · FR-06 (§참고 line 114 `^4.1.4`·`^6.0.3` 실측 인용 해석 여지) 3건 보수적 marker 유지. stale_cycles=0.
# tooling.md: REQ-028 이력 [x] 유지 + REQ-053 FR-01~06 · NFR-03 7 [ ] 박제 유지. HEAD=46f4484 baseline 재측정 — vanilla `no-unused-vars` 1 hit @`eslint.config.js:74` / `@typescript-eslint/no-unused-vars` 0 hit / disable 주석 **2→8** 증가 (Comment TS island 전환 `46f4484` 커밋 이후 `src/Comment/CommentForm.tsx:19`, `src/Comment/CommentItem.tsx:28,30`, `src/Comment/api.mock.ts:9,11,13` 5건 추가 + 기존 `src/test-utils/msw.ts:41`, `src/Image/ImageItem.tsx:6` 2건). FR-01 (rule swap) 미달 — 정상 WIP. FR-03 (disable 회수) baseline 증가 — 후행 task 에서 일괄 회수 필요. stale_cycles=0.
# typecheck-exit-zero.md (new in green this session): REQ-054 흡수 γ 경로 채택 — 독립 spec 신설. 6 [ ] §테스트 현황 + 10 [ ] §수용 기준 (FR-01~06 + NFR-01~04). HEAD=46f4484 baseline — `npm run typecheck` rc=2 · `grep -cE "error TS"` 106 hit · TS2304 0 hit. 카테고리 TS2345 39 / TS2769 20 / TS2322 16 / TS2339 13 / 기타 18. 파일 분포 Toaster/Comment/File/Search/Image 상위. 정상 WIP (rc=0 수렴 방향). stale_cycles=0.
# reconcile this session: 10/21 ack (flip 10 in tsconfig-test-ambient-globals.md, 11 WIP in 3 specs remaining + 16 WIP new in typecheck-exit-zero.md as baseline).
