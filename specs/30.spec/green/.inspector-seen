# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T11:15:00Z (HEAD=fb8adb0, pre-commit)
# Phase 1 ack this session: 0/2 ack (1 stale >=3cycle — file-test-split stale=5 경고 임계 초과 지속).
#
# Phase 1 historical context (reference only):
#   - 이전 inspector 세션 HEAD=3bf6f2b → 현 HEAD=fb8adb0:
#       * fb8adb0 spec(planner): block toaster-conditional-mount-or-helper at stale=3
#         - toaster spec 이 50.blocked/spec/ 로 이동 (23bfc92 hash 3사이클 정체, FR-09 unchecked 2건 `[deferred]` 토큰 부재 4조건 미충족).
#         - planner-side shuffle hash 변동(6e5e507→60be59b) / file-split stale 1→2 는 planner ledger 관측치.
#           (본 inspector ledger 의 shuffle hash 60be59b 은 3bf6f2b 세션 이후 불변으로, planner 의 "6e5e507→60be59b" 변동은 3bf6f2b 세션에서 inspector 가 spec 확장한 결과를 planner 가 재인식한 것.)
#   - 50.blocked/task/ 잔여: TSK-20260421-43, TSK-20260421-49, TSK-20260421-50 — 불변 (RULE-05 관할).
#   - 50.blocked/spec/ 6건: fake-timer-teardown-residual-cleanup, react-19-findby-option-beta-idiom, react-19-findby-timing-stabilization, test-infra-conventions-grep-gate-lifo, test-isolation-shuffle-safety (이전), toaster-conditional-mount-or-helper (신규 이번 사이클).
#   - 40.task/ready/ 0건. developer 활동 없음 (`git log 3bf6f2b..HEAD -- src/` = 0 commit).
#   - 20.req/ready/ 0건. discovery 활동 없음.
#
# Phase 1 detail this session (drift reconcile, HEAD=fb8adb0 재실측):
#   - common/test-isolation-shuffle-safety-cold-start-spec.md (REQ-20260421-007 + REQ-20260421-010):
#       * 대응 task TSK-20260421-49 여전히 50.blocked/task/ 유지. depends 경로 차단.
#       * HEAD=fb8adb0 grep 재실측:
#           - `grep -n "getByTestId.*delete-button" src/Log/LogItem.test.jsx` → 4 hits at :205, :260, :316, :377 (FR-02 대상 미적용 상태 유지; spec 기대 후=0 hits 미달).
#           - `grep -n "findByTestId.*delete-button" src/Log/LogItem.test.jsx` → 0 hits (FR-02 적용 후=4 hits 미달).
#           - `grep -n "ASYNC_ASSERTION_TIMEOUT_MS" src/test-utils/timing.js` → 1 hit at :5 (선결 의존 충족 유지).
#       * 이번 세션 추가 ack: 0건. hash 60be59b 유지 (직전 3bf6f2b 에서 Layer 2 확장 반영 후 불변).
#       * stale 판정: 직전 세션 stale=0 (spec 확장으로 리셋). 이번 세션 hash 불변 + ack 0 → stale 0→1.
#   - components/file-test-multi-scenario-split-spec.md (REQ-20260421-008):
#       * 대응 task TSK-20260421-50 여전히 50.blocked/task/ 유지 (depends_on TSK-49 미충족).
#       * HEAD=fb8adb0 grep 재실측:
#           - `grep -n "test\\|describe" src/File/File.test.jsx` → `prodServerOk` describe 내부 단일 test 여전 at :61 (FR-07 미적용, 분할 0/5).
#           - `grep -n "vi.useFakeTimers" src/File/File.test.jsx` → 2 hits at :165, :235 (internal error/network error describe 기존 사용; spec FR-05 대상인 `prodServerOk` Delete refresh test 와 무관).
#           - `grep -n "vi.spyOn(api" src/File/File.test.jsx` → 0 hits (FR-04/05 미적용).
#           - `grep -n "findByText.*Drop files here" src/File/File.test.jsx` → 1 hit at :53 (FR-07 옵션 대상 baseline 유지).
#       * 이번 세션 drift reconcile: 상태 변화 없음. ack 0건. stale=4→5 (경고 임계 초과 지속).
#
# Phase 2 this session: deferred 태깅 변화 없음. 300줄 초과 spec 없음 (141/83 lines) → 분할 없음.
#
# Phase 3 this session:
#   - input queue (20.req/ready/): 0건. 처리할 신규 req 없음.
#   - downstream threshold GREEN_PENDING_MAX=20 (기본). 현 green 2 < 20 (하류 여유 충분; fb8adb0 에서 toaster blocked 이동으로 3→2 감소).
#   - 처리된 req: 0. blocked: 0. 신규 green: 0. 갱신 green: 0. mv: 없음.
#
# 현 ledger:
specs/30.spec/green/components/file-test-multi-scenario-split-spec.md 6488570be7d4c6586fdcc912c53a5cfeaf8020ff5add354ba34ea760df280197 5
specs/30.spec/green/common/test-isolation-shuffle-safety-cold-start-spec.md 60be59b96e3eba221c65332860b0d993b60bbb4d0d667f414817c60c30e0c858 1
#
# Stale 경고:
#   - file-test-multi-scenario-split stale=5 (경고 임계 초과 2사이클 지속).
#     * RULE-05 운영자 개입 권장 조치 (재확인):
#         a) TSK-20260421-50 의 depends_on TSK-49 해제 여부 판단 (파일 비중첩 검증됨 — src/File/File.test.jsx prodServerOk vs src/Log/LogItem.test.jsx). 해제 시 TSK-50 을 40.task/ready/ 로 복귀 가능.
#         b) 또는 TSK-50 도 신규 Task 로 재carve (Layer 2 해소 신규 TSK 에 순차 의존 걸기).
#     * 본 spec 자체는 내용 드리프트 없이 waiting 상태 — 다음 세션 stale=6 상승 예상.
#   - shuffle-safety-cold-start stale=1: Layer 2 확장 반영 후 planner 재carve 대기. planner 가 신규 TSK (B1 warm-up render prime) 발행 시 stale 리셋 + [pending: Layer 2 신규 TSK-ID] 태그 전환 경로 개통.
#   - toaster-conditional-mount-or-helper: fb8adb0 에서 50.blocked/spec/ 로 이동, 본 ledger 추적 대상에서 제외. RULE-05 운영자 관할 (FR-09 unchecked 는 TSK-49 Layer 2 해소 종속).
#
# 다음 세션 유의:
#   - planner 가 shuffle-safety-cold-start spec 기반으로 Layer 2 신규 Task (B1 우선) carve 시 inspector 는 [deferred: TSK-49 blocked] → [pending: Layer 2 신규 TSK-ID] 전환 가능 (spec FR-14 가이드).
#   - file-test-split stale=5 지속 시 다음 세션 stale=6 예상. 운영자 개입 없이는 ack 경로 없음 — 반복 보고로 시그널 유지.
#   - green 2 개 모두 ack 경로가 외부(TSK-49 Layer 2 해소 또는 운영자 개입)에 종속 — inspector 자체 루프로는 해소 불가.
