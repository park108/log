# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (HEAD=8b2cb30 at session start, Phase 1 reconcile 8/8 ack — components/common.md REQ-032 잔존 marker 8건 일괄 플립).
# Phase 1 ack this session: 8/8 — 8 [ ] marker @ components/common.md (테스트 현황 3 + 수용 기준 5) 전원 PASS (TSK-66/67/68 hash 모두 HEAD 조상 + DoD grep 게이트 재실행 PASS). a11y.md 13 [ ] marker 대응 task 부재 → 유지.
#
# drift marker 스캔 (2 spec @ 세션 시작 — common/a11y.md, components/common.md. 직전 세션 이후 planner 가 components/log.md·foundation/tooling.md green→blue 승격 완료 + foundation/ci.md blocked/spec 이동):
#   1. `components/common.md:165~189` — REQ-032 흡수 8 [ ] marker 묶음 (테스트 현황 FR-06/07/08 3 marker + 수용 기준 FR-01/04/06/07/08 5 marker). 본 세션에 TSK-66 (FR-01/06), TSK-67 (FR-04/07), TSK-68 (FR-08) 결과 박제 확인 → DoD grep 게이트 HEAD 재실행 전원 PASS → 8 marker 전원 `[x]` 플립. stale_cycles 1 → 0.
#      · TSK-66 `c5ad57d` (HEAD 조상 확인): (a) indexOf('#id_token=') 0 hit / (b) .hash 1 hit / (c) access_token=AAA&id_token=BBB 2 hit / (d) describe auth() Cognito 1 hit.
#      · TSK-67 `0fb0ca1` (HEAD 조상 확인): (a) maxAge:<n> 0 hit / (b) 'max-age': 3600 2 hit @ L141,150 / (c) describe auth() cookie persistence attr 1 hit @ L329 / (d) max-age=3600 2 hit / (e) maxAge= 2 hit.
#      · TSK-68 `8b2cb30` (HEAD 조상 확인): (a) describe isAdmin() env 미주입 1 hit @ L923 / (b) VITE_ADMIN_USER_ID_PROD 5 hit / (c) isAdmin()).toBe|toEqual(false) 8 hit / (d) REQ-20260421-032 FR-08 1 hit / (e) describe isAdmin 3 hit.
#      · hook-ack 보조: 3 result.md 모두 `npm run lint` PASS + `npm test -- --run` PASS (385 → 386 → 388) + `npm run build` PASS + "회귀 0" 박제. Must 주관 혼재 없음 (객관 grep 게이트 기반).
#   2. `common/a11y.md:149~167` — REQ-033 흡수 13 [ ] marker (§테스트 현황 8 + §수용 기준 5). 본 세션까지 대응 task carve/수행 없음 (60.done/task/**/result.md 에 REQ-033 키워드 0 hit). [ ] 유지. stale_cycles 0 → 1.
#      · planner 영역 대기: M1–M9 정상화 + M5/M10 결정 분기 + audit 테스트 (Should) 3축 carve 후보.
#
# Phase 1 ack 산출: ACK 8 (components/common.md 일괄), 비-ACK 0 평가단위. 분모 M = "이번 세션 평가 가능한 단위" 8 (a11y 는 task 부재로 평가 대상 외, stale 누적만 ledger 추적).
#   토큰 = `reconcile: 8/8 ack`.
#
# Phase 2 (deferred/split): 해당 없음. 300줄 초과 spec 없음 (a11y.md 195줄 / components/common.md 222줄).
#
# Phase 3 (신규 req 반영): 입력 큐 0건 — 처리 없음. 50.blocked/req 비어있음.
#
# 변동 spec sha:
#   - `common/a11y.md` sha 동일 (7dd9830f...) — 본문 무변경, marker stale_cycles 0 → 1.
#   - `components/common.md` sha 174929482f9a06ec2005b6af655f7c5b3351e235e2152f7a43a27fb2bd606fab — 본 세션 edit (8 marker [ ]→[x] + 변경 이력 1행 박제), stale_cycles 1 → 0.
#   - `components/log.md` — planner 가 blue 로 승격 완료 (ledger 에서 제거).
#   - `foundation/tooling.md` — planner 가 blue 로 승격 완료 (ledger 에서 제거).
#   - `foundation/ci.md` — planner 가 50.blocked/spec 으로 이동 완료 (ledger 에서 제거).
#
# 현 ledger:
specs/30.spec/green/common/a11y.md 7dd9830f7a699856a0668ae073d95793dccad7cdc55e4a0e477036cfe39d6390 1
specs/30.spec/green/components/common.md 174929482f9a06ec2005b6af655f7c5b3351e235e2152f7a43a27fb2bd606fab 0
#
# Stale 경고: 0 path ≥ 3 cycle. 직전 세션 `foundation/ci.md` 4 cycle 경고는 planner 가 본 세션 이전 blocked/spec 으로 이동하여 해소. a11y.md 13 marker 는 1 cycle 시작 — 임계치 이하.
#
# 입력 큐: 처리 후 0건 (0 처리).
# 하류 임계치: GREEN_PENDING_MAX=20, 현 green 파일 2 (common/a11y.md, components/common.md) < 20. 여유 18.
#
# 승격 가능성 (planner 관할, 참조용):
#   - `components/common.md` — 잔존 [ ] marker 0 (본 세션 8 marker 전원 플립). §변경 이력 TSK-66/67/68 3 커밋 박제 완료. **promote 가능** (green→blue, REQ-022/025/032 완결).
#   - `common/a11y.md` — 잔존 [ ] marker 13 (§테스트 현황 8 + §수용 기준 5). promote 대기 — 3 task carve (M1–M9 정상화 / M5·M10 결정 분기 / audit 테스트 Should) 통과 필요.
#
# 다음 세션 유의:
#   - 20.req 다시 차면 Phase 3 재개. 현재 10.followups 2건 대기 중 (discovery 영역): `20260421-0731-react-19-runtime-bump-reattempt-from-blocked.md`, `20260421-0746-ci-node-lts-cadence-runbook-from-blocked.md`.
#   - `common/a11y.md` REQ-033 13 marker 는 향후 세션에서 task 결과 박제 시 순차 ack. 2 세션 내 진척 없으면 stale 경고 (stale_cycles ≥ 3).
#   - components/common.md 는 planner 가 다음 30m 주기에 green→blue 승격 수행 가능.
