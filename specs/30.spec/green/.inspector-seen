# inspector Phase 1 drift reconcile seen-state
# format: <spec-path>\t<marker-hash>\t<stale_cycles>
# 2026-04-22 @ 57b6459 — reconcile 0/27 ack (no flip this session). HEAD 이후 developer 커밋 32502dd (Search island TSK-20260422-07 완료). src/Monitor (TSK-08) 는 작업 트리 중간 상태 (staged/untracked mix) — 커밋 미완료, 본 세션 reconcile 대상 아님 (inspector 영역 밖 staged 항목은 `git reset HEAD -- src/Monitor/` 언스테이지 처리 — RULE-02).
# src-typescript-migration.md: 6 [ ] §동작 게이트 + 6 [ ] §수용 기준 유지 (0 flip). 실측 HEAD=57b6459 — `.jsx` 전체 42 / 런타임 jsx 23 / test jsx 19, `.js` 런타임 22 / test js 14, `.ts` (non-d.ts) 20 / `.tsx` 24. FR-01/02 수렴 조건 (전원 0) 미달 — 정상 WIP. Search island 반영으로 jsx/js 수치는 감소 방향이나 여전히 0 미달. 파생 task: TSK-20260422-01~12. stale_cycles=1 (2 cycles 동안 ack 0 · WIP 불변).
# tsconfig-test-ambient-globals.md: 3 WIP 유지 (FR-02 본문 단일 수단 귀속 해석 여지 + §수용 기준 FR-02 수단 중립성 · FR-03 CI typecheck step rc=0 미달 = typecheck-exit-zero 관할 · FR-06 §참고 baseline 버전 인용 해석 여지). 실측 HEAD=57b6459 `npm run typecheck 2>&1 | grep -cE "TS2304"` → 0 hit 유지 (수렴 지속). stale_cycles=1.
# tooling.md: REQ-028 이력 [x] 유지 + REQ-053 FR-01~06 · NFR-03 7 [ ] 박제 유지 (0 flip). HEAD=57b6459 baseline 재측정 — vanilla `no-unused-vars` 1 hit @`eslint.config.js:74` / `@typescript-eslint/no-unused-vars` 0 hit / disable 주석 8 hit (`src/test-utils/msw.ts:41`, `src/Image/ImageItem.tsx:6`, `src/Comment/CommentForm.tsx:19`, `src/Comment/CommentItem.tsx:28,30`, `src/Comment/api.mock.ts:9,11,13`). 이전 세션 동일. FR-01 rule swap 미적용 — 정상 WIP (TSK-20260422-12 pending). stale_cycles=1.
# typecheck-exit-zero.md: 16 WIP (§테스트 현황 6 + §수용 기준 10) 유지 (0 flip). 실측 HEAD=57b6459 — `npm run typecheck; echo $?` rc=2 / `grep -cE "error TS"` **103 hit** (106→103 Search island 반영 후 3 감소). 디렉터리 분포 Search 28 / File 25 / Comment 21 / Toaster 19 / Image 10. FR-01/02/03 수렴 조건 미달 — 정상 WIP. stale_cycles=1.
# reconcile this session: 0/27 ack (4 specs 27 WIP 전원 유지). HEAD 이후 developer 커밋 1건 32502dd 가 error TS 3건 감소시켰으나 target 0 과 거리 큼.
