# inspector Phase 1 ledger. Format: <spec-path> <sha256|-> <stale_cycles>
# Updated: 2026-04-21T (HEAD=2e9f806, pre-commit)
# Phase 1 ack this session: 0/41 ack (dev 신규 커밋 0; planner 3건 a4636a7/2a0ff31/2e9f806 만 — src drift 근거 없음).
# marker-sync 건수: 7 (test-isolation FR-02/06/07/08/11/12/13 marker 정합 전환, [x] flip 은 FR-02 1건 only — 수용기준 L107 이미 PASS 박제분 혼재 해소).
#
# Phase 1 historical context (reference only):
#   - 직전 inspector 세션 HEAD=b005644 → 현 HEAD=2e9f806 (planner 3건: 2a0ff31 TSK-53 ledger → a4636a7 promote 2 green→blue + carve TSK-53/55 → 2e9f806 no-op stale 0→1).
#   - 직전 inspector ack: node-env-helper-migration Must 6/6 + Should 1/1 (TSK-20260420-37/a95f1bb), typescript-bootstrap Must 6/6 + Should 1/1 + NFR 1/3 (TSK-20260420-32/ceb9c60), msw-sibling-it 신규 carve (REQ-20260421-011).
#   - a4636a7 에서 두 ack-complete spec 이 green→blue 로 promote. 현 green 큐는 2건으로 감소 (shuffle-cold-start + msw-sibling).
#   - 50.blocked/** 현 상태: 전부 0건 (blocked/{req,spec,task} 비어있음).
#   - 40.task/ready/: 1건 (TSK-20260421-55 msw-test-sibling-it-shuffle-race). TSK-20260421-53 은 carve 메시지(a4636a7) 에는 있으나 파일 부재 — planner 관할 이슈, inspector 개입 대상 아님.
#   - 20.req/: 0건 (Phase 3 no-op).
#
# Phase 1 detail this session (drift reconcile + marker sync, HEAD=2e9f806 재검증):
#
#   [1] common/test-isolation-shuffle-safety-cold-start-spec.md:
#       * HEAD 변동 0 (dev 신규 커밋 없음), src drift 0.
#       * §테스트 현황 marker 7건 정합 전환 수행:
#         - FR-02 `[deferred: TSK-49 blocked]` → `[x]` (TSK-51/d798635 수용기준 L107 이미 PASS 박제분과 혼재 해소).
#         - FR-06 `[deferred: TSK-49 blocked; Layer 2 해소 후 재시도]` → `[pending: REQ-20260421-011 TSK-20260421-55]` (독립 spec carve-out, planner a4636a7).
#         - FR-07 `[deferred: TSK-49 blocked]` → `[deferred: 별건 task 미발행]` (scope-외 사유 명시).
#         - FR-08 `[deferred: TSK-49 blocked — Layer 2 race 미해소]` → `[pending: TSK-20260421-53 실측]`.
#         - FR-11 `[pending: Layer 2 신규 TSK-ID 발행]` → `[pending: TSK-20260421-53 (Layer 2 B3 fallback)]`.
#         - FR-12 `[pending: B1~B4 실측 결과 의존]` → `[pending: TSK-20260421-53 B3 실측 결과 의존]`.
#         - FR-13 `[pending: Layer 2 신규 TSK-ID]` → `[pending: TSK-52 B1 blocked → TSK-53 B3 fallback 실측]`.
#       * 변경 이력 1행 박제 (marker sync, HEAD=2e9f806).
#       * hash 변동: 21543b77 → bea52063. [x] flip 1건 (FR-02) 은 수용기준-테스트현황 혼재 해소 목적 (신규 ack 아님 — 이미 L107 에 박제됨).
#       * stale_cycles: 1 → 0 (marker sync 로 WIP 상태 갱신).
#
#   [2] common/msw-test-sibling-it-shuffle-race-dedicated-spec.md:
#       * HEAD 변동 0. WIP 전원 불변 (Must 5/5 + Should 2/2 + NFR 6/6 모두 `- [ ]`).
#       * 본 spec 의 FR-06 Should ("원본 green spec §FR-06 태깅 전환은 planner 가 신규 Task 발행 시점에 수행") 를 planner 가 미수행 — inspector 가 marker sync 로 보조 이행 (원본 spec L97 갱신).
#       * 변경 이력 1행 박제 (marker sync 참조).
#       * hash 변동: e956a40a → 4b22c6b0. ack 근거 없음 (src 변동 0, TSK-55 developer 실측 대기).
#       * stale_cycles: 1 유지 (marker-sync 는 stale reset 대상 아님 — WIP 불변).
#
# Phase 2 this session: 변화 없음.
#   - deferred 태깅 신규 전환 없음 (기존 FR-07 만 사유 명시 갱신).
#   - 300줄 초과 spec 없음 (shuffle-cold-start ~145줄, msw-sibling ~80줄).
#
# Phase 3 this session: no-op.
#   - input queue (20.req/): 0건 → 0건.
#   - downstream threshold GREEN_PENDING_MAX=20 (.claude/pipeline.json 없음). 현 green 2 < 20.
#   - 처리된 req: 0. blocked: 0. 신규 green: 0. 갱신 green: 2 (marker sync).
#
# 현 ledger:
specs/30.spec/green/common/test-isolation-shuffle-safety-cold-start-spec.md bea5206339c77c7dbebb414531177e06dc17da7f32174cb8485ba3663f5b0730 0
specs/30.spec/green/common/msw-test-sibling-it-shuffle-race-dedicated-spec.md 4b22c6b0ad4cc4db8fceca6eab4d280ee2d98e4942aace8612ed464c49d06fa0 1
#
# Stale 경고:
#   - shuffle-safety-cold-start stale=0 (marker sync 반영 — 갱신됨). TSK-53 실측 후 FR-08/11/12/13 flip 가능.
#   - msw-sibling-it-shuffle-race-dedicated stale=1 (< 3 임계, 경고 없음). TSK-55 developer 실측 대기.
#
# 다음 세션 유의:
#   - TSK-20260421-53 (Layer 2 B3 msw listen beforeAll) 은 planner carve 메시지(a4636a7)에는 있으나 40.task/ready/ 에 파일 부재 — planner 재발행 또는 developer 가 별도 경로로 소비했는지 확인 필요 (RULE-05 운영자 관할 또는 다음 planner 세션).
#   - TSK-20260421-55 (sibling-it) 소비 후 msw-sibling green Must 5/5 + Should 2/2 + NFR 6/6 전원 `[x]` flip 가능 — inspector ack 대기.
#   - 3cycle stale 초과 시 reconcile 보고에 `reconcile: N/M ack (K stale ≥3cycle)` 토큰 추가 (RULE-04).
